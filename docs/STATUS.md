# STATUS.md — 인수인계 문서

> 매 이터레이션 시작 시 DESIGN.md, 이 파일, INBOX.md 순으로 읽는다.
> 매 이터레이션 종료 시 이 파일을 "다음 사람(다음 세션)"에게 인수인계하듯 갱신한다.

## 마지막 갱신

- 일시: 2026-09-15 (93)
- 작성자: AI 에이전트. INBOX.md 2026-09-14 대량 피드백(아래 "다음 할 일 큐" 16번)
  중 독립적인 작은 항목 1개를 처리: "상점은 현재 5개의 방 기준, 2번째 4번째
  방에만 추가한다" — `dungeon_map.gd`의 `_room_options_for_index(idx)`에서 상점
  노출 판정을 `rng.randf() < SHOP_CHANCE`(60% 확률 노출)에서
  `SHOP_FIXED_ROOM_INDICES.has(idx)`(신규 상수 =[1, 3])로 교체해, 상점이 2번째/
  4번째 방에만 상시 노출되고 나머지 방(1/3/5번째)에는 아예 안 뜨도록 고정.
  event/story 노출은 기존 확률 유지, 라운드 마지막 방(전투 전용)도 기존대로 유지.
  QA 검증용 0-인자 디버그 훅 `_debug_show_room2()`/`_debug_show_room3()`을
  신규 추가해 `qa_out/dungeon_map_room1_shopfix.png`(1번째 방, 상점 없음)/
  `qa_out/dungeon_map_room2_shop.png`(2번째 방, 상점 버튼 노출)/`qa_out/
  dungeon_map_room3_noshop.png`(3번째 방, 상점 버튼 없음) 3개 상태로 확인,
  `dice_test.gd` 회귀 스위트 전체 PASS(결정성/순열 검증 로직이라 영향 없음).
  INBOX.md "처리됨"으로 이동, 12개 초과분(가장 오래된 1개, 커스터마이징 3단계
  안내 문구 항목)은 INBOX_ARCHIVE.md로, STATUS.md 완료 기록 10개 초과분(가장
  오래된 1개, (82) 업적 2종 추가)은 STATUS_ARCHIVE.md로 이관.

## 지금 위치

- **한 바퀴 플레이 가능**: 캐릭터 선택(5종 중 하나, 아래 참고) → 던전 맵(런
  5방, 전투/상점/특수 이벤트/스토리 이벤트 방 종류) → 전투(다이스 굴려 공방,
  승패 후 복귀)까지 이어짐. 전투는 항상 노출, **상점은 2026-09-15 (93)부터
  2번째/4번째 방에만 고정 노출**(`SHOP_FIXED_ROOM_INDICES`, 이전에는 방마다
  60% 확률 노출이었음), 특수 이벤트/스토리 이벤트는 여전히 방마다 확률적으로
  노출되고 노출된 것끼리는 방마다 결정적으로 순서도 섞임.
- **라운드 마지막 방(보스 방)은 전투만 가능(2026-09-14 (90) 버그 수정)**:
  라운드가 넘어가려면 반드시 보스를 이겨야 하는데(`_apply_room_advance()` ->
  `RunState.advance_round()`), 마지막 방에도 상점/특수 이벤트/스토리 이벤트가
  노출될 수 있어 전투를 건너뛰면 보스 없이 "런 클리어" 상태가 되어 라운드가
  영영 안 올라가는 버그가 있었음 — `dungeon_map.gd`의
  `_room_options_for_index()`에서 라운드 마지막 방(idx == TOTAL_ROOMS - 1)은
  상점/이벤트/스토리를 전부 노출 안 시키도록 고쳐 전투만 가능하게 강제함.
  아래 "완료 기록 (90)" 참고.
- **플레이어블 캐릭터 ([대형 기획 1], 5/5종 완료)**:
  `character_select.tscn`에서 카드 5개(화면 폭에 맞춰 카드 폭이 동적으로
  줄어듦) 중 하나를 골라 시작. "견습 모험가"(기믹 없음)/"광전사"(극단형 —
  공격+방어 다이스 전부 min/max만 나옴)/"수호자"(안정형 — 방어 다이스
  하나만 항상 고정값)/"폭발병"(폭발형 — 공격 다이스가 최댓값 면을 보여줄
  때마다 전투 중 스택이 쌓이고 3스택에서 다음 공격 한 턴만 1D20으로 굴림,
  몬스터 "고블린"의 anger_stack과 같은 메커니즘을 플레이어 공격턴에 적용)/
  "방패병"(인내형 — 방어 다이스가 최댓값 면을 보여줄 때마다 수호 스택이
  쌓이고 3스택에서 다음 방어 한 턴만 1D20으로 굴림, "폭발병"과 완전히 같은
  메커니즘을 플레이어 방어턴에 대칭 적용). 캐릭터 정의는
  `code/systems/character_profiles.gd`(`CharacterProfiles.PROFILES`). "광전사"/
  "수호자"는 `RunState.reset_run(character_id)`가 새 주머니를 만든 직후 한 번
  정적으로 다이스를 개조하지만(몬스터 기믹과 같은 방식 — 이후 다이스 개조로
  주머니가 바뀌어도 재적용되지 않음), "폭발병"/"방패병"은 정적 개조가 없고
  `combat_test.gd`의 전투 중 상태(각각 `player_explosive_stacks`/
  `player_explosive_pending`, `player_guard_stacks`/`player_guard_pending`)로
  턴마다 추적된다(덱 패널에는 표준 다이스 그대로 보이는 게 정상 — 효과는
  전투 로그에서만 드러남). 5종 전체의 밸런스/체감이 적당한지는 사람 플레이
  피드백 필요.
- **다이스 시스템**: `DiceBag`(주머니)+`CombatMath`, D4/D6/D8/D10/D12/D20
  지오메트리 전부 정확히 구현. 재질별(플라스틱/나무/유리/철제) 시각 색 구분.
  몬스터 다이스는 방 진행에 따라 D4→D6→D8로 스케일링.
- **커스터마이징**: 승리 시 얻는 "눈금 인벤토리"를 다이스 면과 교환하는 3단계
  방식(눈금 선택 → 다이스 선택 → 면 선택, 각 단계에 "(1/3)/(2/3)/(3/3)" 번호와
  안내 문구 있음)에 더해, "다이스 인벤토리" 흐름(상점/특수 이벤트/전투 승리
  보상의 "다이스 승급" 아이템으로 얻은 다이스를 인벤토리에서 골라 원하는
  자리와 교환)도 같은 `CustomizePanel` 오버레이 안에서 열 수 있음(1단계 화면의
  "다이스 인벤토리 (N개)" 버튼으로 진입). 어느 화면에서든 오버레이로 열 수
  있음. "인지하기 쉬운지"/"성장이 잘 느껴지는지" 최종 판단은 사람 플레이 필요.
- **눈금(pip) 공급원**: 전투 승리 보상(1개) + 특수 이벤트 방의 "눈금 주머니
  획득" 아이템(3~5개 한 번에).
- **다이스(die) 인벤토리 공급원**: 상점/특수 이벤트/전투 승리 보상의 "다이스 승급"
  아이템(kind=upgrade_die) — 얻으면 즉시 적용되지 않고 `RunState.die_inventory`에
  쌓여, 커스터마이징에서 플레이어가 직접 어느 다이스와 바꿀지 고른다. 인벤토리
  다이스가 양쪽 주머니 어디에도 승급시킬 자리가 없을 때는 그 사실을 안내하는
  문구가 2단계(교체할 자리 선택) 화면에 뜬다.
- **UI**: 공격/방어 덱 상시 표시 패널, 던전 맵 전체 미리보기(MapStrip, 방 종류별
  예상 보상 카테고리 아이콘 포함), 전투 다이스 결과를 이미지 칩(44px)으로 표시
  (+MAX 눈금 강조), 플레이어/몬스터 HP 막대(비율에 따라 초록/주황/빨강으로 색
  변화), 보상/상점/이벤트 아이템은 카드형 비주얼. combat_test의 "덱 보기" 토글
  패널은 `z_index=10`으로 다른 UI보다 항상 위에 그려지고, 승리 보상 화면이 뜨면
  자동으로 닫히며 보상 카드 영역도 덱 패널과 안 겹치게 폭이 좁혀져 있음.
- **업적 시스템 (골격 완성 + 12종 등록)**: `code/systems/achievement_manager.gd`
  (`AchievementManager`, Autoload)가 `user://achievements.json`에 해금 상태를
  영구 저장(RunState와 달리 새 런을 시작해도 안 지워짐). `character_select.tscn`의
  "업적" 버튼으로 `AchievementPanel`(잠금/해금 카드 목록 오버레이, 스크롤 지원)을
  열 수 있음. 등록된 12종: "첫 발걸음"(INBOX #25, 던전 시작)/"던전 클리어"(#1,
  라운드 1 완료)/"거인의 주사위"(#6, D20 보유 승리)/"부자"(골드 100 이상
  보유)/"무결점 승리"(전투 중 무피해 승리)/"기사회생"(HP 2 이하로 승리)/
  "오버킬"(몬스터 최대 체력 이상 데미지로 승리)/"눈금 수집가"(눈금
  인벤토리 10개 이상)/"다이스 수집가"(다이스 인벤토리 5개 이상)/"가득 찬
  주머니"(주머니가 MAX_DICE=6에 도달)/"단골 손님"(`RunState.
  shop_visits` 3회 이상, 상점 퇴장마다 증가)/"재질 수집가"(공격+방어 주머니에
  플라스틱/나무/유리/철제 4개 재질 다이스 동시 보유). 남은 항목은
  `achievement_manager.gd`의 `DEFINITIONS`에 항목만 추가하고 해당 조건
  지점에서 `AchievementManager.unlock(id)`를 호출하면 저장/UI가 자동으로
  따라오는 구조지만, 새 RunState 카운터만으로 되는 독립 항목은 거의 소진되어
  남은 것은 대부분 [대형 기획 1]/[대형 기획 2] 선행이 필요함. 아래 "다음 할
  일 큐" 13번 참고.
- **QA 도구**: 실제 창(화면 밖 좌표로 이동해 사람 작업 방해 안 함)으로 스크린샷,
  물리 정지 감지 옵션(`GAME_QA_SETTLE`), 마우스 클릭 시뮬레이션
  (`GAME_QA_CLICK_PATH`), 방 번호 강제 지정(`GAME_QA_ROOM_OVERRIDE`).
  `dice_test.gd`(스크린샷 없이 로직만 확인하는 텍스트 회귀 스위트)가
  `DiceBag`(개수 캡 포함)/`DiceItemPool`(다이스 인벤토리 획득 포함)/`EventItemPool` 판정 로직,
  `dungeon_map.gd`의 방 선택지 노출/순서 결정성, `story_event.gd`의 골드 손실
  클램프, `combat_test.gd`의 몬스터 난이도 스케일링 공식과 다이스 재질 배정,
  `customize_panel.gd`의 눈금 교환 + 다이스 인벤토리 교환, `shape_die_chip.gd`의
  다이스 면->칩 모양 대응, `shop.gd`/`event.gd`(다이스 아이템 + 눈금 아이템 +
  다이스 승급 아이템)/`story_event.gd`/`combat_test.gd`(방 진행 + 승리 보상 선택)/
  `customize_panel.gd`의 이중 적용/구매/진행/교환 방지 가드/`character_profiles.gd`+
  `run_state.gd`의 캐릭터 기믹 적용까지 함께 검증함. **`GAME_QA_CALL`에 인자가
  필요한 메서드를 넘기면 창이 멈출 수 있음 — 아래 "알려진 이슈" 참고, 새 QA
  훅은 항상 인자 없는 래퍼로 만들 것.**
- **몬스터별 다이스 특이 특징 (예시 3개 전부 구현 완료)**: "다크 나이트"
  몬스터(room4, D8 다이스)의 공격/방어 다이스가 `DiceBag.force_min_max_faces()`로
  중간값 없이 min/max로만 구성됨(하이리스크/로우리스크 기믹, 이름에 "[극단]"
  표시). "오크" 몬스터(room3, D6 다이스)의 공격/방어 다이스는 `DiceBag.
  force_fixed_value()`로 굴려도 항상 같은 값(면 개수 평균 반올림, D6은 4)만 나오게
  됨(이름에 "[고정값 4]" 표시). "고블린" 몬스터(room1, D4 다이스)는 공격 다이스가
  자신의 최댓값 면을 `ANGER_STACK_THRESHOLD`(=3)번 보여주면 다음 공격 한 턴만
  1D20으로 굴리는 "분노 스택" 기믹이 붙음(이름에 "[분노]" 표시 — `DiceBag.
  count_max_rolls()` + `combat_test.gd`의 전투 중 상태 `monster_anger_stacks`/
  `monster_anger_pending`). 어떤 몬스터에 이 기믹들을 배정하는 게 맞는지(성격
  기획 선행 필요)는 여전히 사람 기획 대기 — 아래 "알려진 이슈" 참고.
- **다이스 개수 캡 ("성장의 재미가 없다" 방향 1의 첫 조각)**: `DiceBag.MAX_DICE`
  (=6, 시작 3개의 2배, 잠정값)를 넘어서는 "다이스 추가" 아이템은 상점/특수 이벤트/
  전투 승리 보상 어디서든 대상 주머니가 캡에 도달하면 "주머니 가득 참 (최대 6개)"로
  비활성화됨(`DiceItemPool.is_applicable`/`unavailable_reason`). 캡에 도달한 뒤의
  성장 경로는 새로 안 만들고 이미 있는 "다이스 승급"(면 개수 교체)/"눈금 교환"을
  그대로 재사용. 캡 수치와 "교체로 성장한다"는 게 실제로 체감되는지는 사람 플레이
  피드백 필요 — 아래 "다음 할 일 큐" 0-A 참고.
- **보스 몬스터 + 라운드 반복 ([대형 기획 2], 3/3 조각 완료)**: 런의 마지막 방
  (room_index == `RunState.TOTAL_ROOMS - 1`, 지금은 4)의 몬스터는 `_monster_
  config_for_room()`에서 `is_boss=true`로 판정돼 공격 다이스 +2개/방어 다이스
  +1개/최대 HP ×2로 강화되고 이름 끝에 "[보스]" 태그가 붙음. 보스 처치 시
  `combat_test.gd`의 `_apply_room_advance()`가 승리한 방이 보스 방이었고
  마지막 라운드(3)가 아니면 `RunState.advance_round()`를 불러 `round_index`를
  올리고 `rooms_cleared`를 0으로 되돌린다 — 골드/다이스 주머니/인벤토리는
  그대로 유지되어 빌드가 이어진 채로 새 던전(라운드)이 시작된다(별도의 "다음
  라운드로" 확인 화면 없이 승리 -> "다음" 버튼 -> 새 라운드의 1번째 방으로
  한 번에 이어짐, `rooms_cleared_label`에 "라운드 N/3 · " 접두사로 방 개수가
  줄어든 이유를 알려줌). **마지막 라운드(3)의 보스를 잡으면 전용 "게임
  클리어!" 화면/문구로 이어짐(2026-09-10 (89) 완료)**: `dungeon_map.gd`의
  `_update_labels()`가 `RunState.is_run_complete() and RunState.is_last_round()`일
  때 "게임 클리어! 총 %d라운드 완주 (마지막 라운드 %d / %d 방 격파)" 문구를
  보여줌(중간 라운드 클리어 시의 "던전 클리어! (%d/%d 방 격파)" 문구와 구분).
  INBOX.md [대형 기획 2] 원 요청이 전부 처리됨 — 남은 것은 순수 밸런스 영역:
  보스 강화폭(+2/+1/×2)이 적당한지, 라운드가 올라갈수록 몬스터/보스가 더
  강해져야 하는지(INBOX.md 원문이 언급했으나 아직 미반영 — 지금은 라운드
  2/3도 라운드 1과 완전히 같은 room_index 기반 스케일링을 그대로 재사용)는
  사람 플레이 피드백 필요.
- **미착수 INBOX 항목**: "성장의 재미가 없다"의 남은 방향(캐릭터별 쿨다운 스킬)은
  캐릭터가 5종으로 늘었지만 아직 스킬 콘텐츠 자체를 새로 기획해야 하는 더 큰
  작업이라 손대지 않음(아래 "다음 할 일 큐" 0-A 참고). 몬스터별 성격/특징
  디자인은 여전히 순수하게 사람 기획 대기(2026-09-01부터 — 이미 구현한 다이스
  기믹 3종을 어느 몬스터에 배정하는 게 맞는지도 이게 먼저 나와야 결정 가능),
  AI가 혼자 진전시킬 수 없음 — 아래 "알려진 이슈" 참고. [대형 기획 1](플레이어블
  캐릭터)은 5/5종 완료(2026-09-09 (86)), [대형 기획 2](보스 + 3라운드 구조)도
  3/3 조각 완료(2026-09-10 (89)) — INBOX.md 원 요청 둘 다 전부 처리됨. 업적
  시스템(큐 13)은 이제 이 둘의 트리거 지점을 활용할 수 있는 상태지만 아직
  손대지 않음. **2026-09-14에 약 30개의 신규 피드백이 대량으로 추가됨**(키매핑/
  단축키, 캐릭터 선택 UX 개편, 캐릭터별 시작 주사위 차별화, 스킬/업적 아이콘,
  던전 맵 범례, 커스터마이징 팝업 겹침 버그, 상점 UX 다수, 보상 등급 S/A/B/C
  색상 체계, 특수 이벤트 스토리+리스크/리턴 개편 등) — 그중 라운드 진행 버그
  (위 참고, (90)에서 수정), 커스터마이징 팝업 겹침 버그((91)에서 수정), 던전 맵
  보상 아이콘 범례 + 상점 입장 버튼 하단 보유 골드 표시((92)에서 수정), 상점
  2번째/4번째 방 고정 노출((93)에서 수정, 아래 "완료 기록" 참고) 5건만
  처리했고, 나머지는 전부 아래 "다음 할 일 큐" 16번에 남아있어 다음
  이터레이션들이 하나씩 집어가야 함.
- 상세 이력은 아래 "완료 기록"(최근 10개)과, 그보다 오래된 것은
  `docs/STATUS_ARCHIVE.md`(매 이터레이션 읽지 않는 아카이브) 참고.

## 다음 할 일 큐 (우선순위 순)

이 순서를 반드시 지킬 필요는 없지만, 앞 단계가 뒤 단계의 전제가 되므로 대체로 순서대로
진행하는 것을 권장한다. 한 이터레이션에 한두 개만 진행할 것.

0. **(INBOX.md 신규 2026-09-03) UI 접근성/가시성 4종 — 1/4 부분 착수.** 전부 "전투/
   선택지 화면 어디서든 정보가 상시 보이거나, 조작이 더 직관적이어야 한다"는 계열의
   요청이라 함께 묶어뒀지만, 각각 독립적으로 착수 가능:
   - **던전 맵 전체 보기(슬더스 스타일)** → **부분 완료** (이번 이터레이션, 아래
     "완료 기록" 참고). `dungeon_map.tscn`에 `MapStrip`을 추가해 런 전체(TOTAL_ROOMS,
     5방)를 한 줄로 보여줌 — 각 방의 선택지(전투/상점/특수 이벤트/스토리 이벤트)가
     결정적 RNG(방 번호 기반 시드)로 미리 계산되므로 아직 안 간 방도 "무엇이 나올지"
     미리보기 가능. 지나온 방은 어둡게 "완료" 표시, 현재 방은 금색 테두리로 강조.
     **다만 "첫 번째 선택지에 따라 다음 선택지가 어떻게 바뀌는지"(분기형 노드 그래프 —
     고른 경로에 따라 이후 방 구성 자체가 달라지는 것)까지는 아님.** 지금은 방마다
     노출 여부가 서로 독립적이라 "무엇을 골랐는지"가 "다음 방에 뭐가 뜨는지"에 영향을
     주지 않음(순서만 이미 정해진 방들을 지나가는 구조). 진짜 분기(노드 그래프 +
     경로 선택)는 DESIGN.md가 확정한 "일단은 선형으로" 방향을 벗어나는 더 큰 구조
     변경이라 사람 설계 확인 후 착수하는 게 안전 — **여전히 미착수.**
   - **공격/방어 덱 상시 표시** → **완료** (아래 "완료 기록" 참고).
     `code/scenes/deck_panel.gd`(`DeckPanel`, view-only)를 `dungeon_map`/`shop`/
     `event`/`story_event` 4개 화면에는 상시 표시로, `combat_test.tscn`에는 화면
     여유 공간이 없어 우측 상단 "덱 보기" 토글 버튼으로 여닫는 오버레이로 붙임
     (완전한 "상시"는 아니지만 언제든 클릭 한 번으로 확인 가능). 남은 것은 사람이
     실제로 써보고 토글 방식이 충분한지, 아니면 정말 상시 노출이 필요한지 판단하는
     것뿐(레이아웃을 다시 짜서 상시 표시로 바꾸려면 다이스 뷰포트를 줄이는 등 더 큰
     작업이 필요함).
   - **커스터마이징 상호작용을 "교체"에서 "인벤토리 눈금 교환"으로** → **완료**
     (이번 이터레이션, 아래 "완료 기록" 참고). `RunState.pip_inventory`에 전투 승리
     보상으로 눈금이 쌓이고, `CustomizePanel`이 "눈금 선택 → 다이스 선택 → 면 선택 →
     즉시 교환(밀려난 값은 인벤토리로 복귀)" 흐름으로 바뀜. 남은 것은 눈금 보상량과
     3단계 순서의 체감(인지하기 쉬워졌는지)에 대한 사람 피드백뿐.
   - **어디서든 덱 열람 + 커스터마이징** → **완료 후 combat_test만 일부 되돌림
     (2026-09-03 (13), 아래 "완료 기록" 참고).** "덱 열람"은 이전 이터레이션에
     view-only로 커버됐고(그대로 유지), "커스터마이징"은 던전맵/상점/특수 이벤트/
     스토리 이벤트 4개 비전투 화면에서는 여전히 언제든 열 수 있다. 다만
     `combat_test.tscn`(전투 화면)은 사용자가 INBOX.md로 "커스터마이징은 전투 중에는
     불가능해야 한다"고 명시적으로 되돌려서, 전투가 끝나기 전(`battle_over=false`)에는
     "커스터마이징" 토글 버튼 자체가 안 보이고 눌러도 안 열리도록 막음 — 전투가 끝난
     뒤(승리 보상 화면 또는 패배 후)에는 그대로 열 수 있음. "덱 보기"는 전투 중에도
     계속 열 수 있음(이건 되돌리라는 지시 없었음).
   → "정보 상시 노출"(view), "커스터마이징 어디서든", "인벤토리 눈금 교환" 상호작용
   전환까지 4개 세부 항목 중 3개가 완료됨. 진짜 분기형 맵(고른 선택지에 따라 다음
   방 구성이 달라지는 노드 그래프)은 **사용자가 2026-09-09에 명시적으로 "만들지
   않는다 — 지금의 선형 구조를 유지한다"고 확정 답변** — 더 이상 검토 대상 아님.
   대신 "선택지마다 어떤 리턴(보상)을 주는지 미리 알기 어렵다"는 새 요청이 나와
   아래 0-A 큐로 옮김.

0-A. **(INBOX.md 신규 2026-09-09, 68~72에서 8개 처리·2개 남음) 대량 신규 피드백** —
   사용자가 한 번에 12개 항목을 남김. 다음 8개는 68~72에서 처리 완료(아래 "완료
   기록" 참고): 전투 승리 버튼 텍스트("던전으로 돌아가기"→"다음"), 덱보기 토글
   패널 z-order(다른 UI보다 항상 위), 결과 화면 등장 시 덱보기 자동 닫힘+레이아웃
   겹침 방지, 전투 다이스 값 칩 사이즈업(34→44px), 플레이어/몬스터 HP 막대 추가
   (비율에 따라 초록/주황/빨강), 특수 이벤트에 "눈금 주머니 획득"(3~5개) 아이템
   추가(70), 커스터마이징 3단계에 단계 번호+안내 문구 추가(71), 던전 맵 선택지에
   예상 보상 카테고리 아이콘 표시(72). 남은 항목(우선순위 순 아님, 서로 독립적으로
   착수 가능):
   - ~~**던전 맵 선택지에 예상 보상/효과 아이콘 표시**~~ → **완료됨** (2026-09-09
     (72), 아래 "완료 기록" 참고). `code/scenes/reward_icon.gd`(`RewardIcon`)로
     골드/눈금/다이스 3종 카테고리를 도형(동전 원 / 점 1개 주사위 면 / 점 3개
     주사위 면)으로 그려, 방 선택 버튼 옆과 MapStrip 방 타입 칩 양쪽에 표시.
     실제 골드/눈금 수치나 상점 판매 품목까지는 아니고 "카테고리"만 보여줌(상점은
     매번 다른 품목을 팔아 정확한 값을 미리 보여주기 어려우므로). 아이콘이 실제로
     "리턴을 예상하는 데" 도움이 되는지는 사람 플레이 피드백 필요.
   - ~~**커스터마이징(눈금 교환) UI/UX가 인지하기 어려움**~~ → **완료됨**
     (2026-09-09 (71), 아래 "완료 기록" 참고). `customize_panel.gd`의 3단계(눈금
     선택→다이스 선택→면 선택) 각각에 "(1/3)/(2/3)/(3/3)" 단계 번호와, 지금 뭘
     고르는 중이고 고르면 무슨 일이 일어나는지 설명하는 안내 문구를 추가함(흐름
     자체를 줄이는 더 큰 개편은 아님). 부수적으로 발견한 Label 오버플로우 버그도
     함께 고침. "실제로 인지하기 쉬워졌는지"는 사람 플레이 판단 필요 — 여전히
     어렵다면 3단계 흐름 자체를 재설계해야 할 수 있음.
   - ~~**눈금 획득 이벤트가 잘 안 뜨는 것 같다 + 눈금 여러 개를 주는 이벤트 추가**~~
     → **완료됨** (2026-09-09 (70), 아래 "완료 기록" 참고). 원인은 "특수 이벤트"
     아이템 풀에 눈금을 주는 항목이 아예 없었던 것 — `EventItemPool.ITEMS`에
     "눈금 주머니 획득"(`gain_pips`, 3~5개 한 번에) 아이템을 추가. 등장 확률(5종
     중 1개)과 3~5개 수치는 잠정값 — 실제 공급량이 충분히 늘었다고 느끼는지는
     사람 플레이 피드백 필요.
   - ~~**다이스 승급 아이템을 "교체"에서 "획득"으로 변경**~~ → **완료됨**
     (2026-09-09 (73), 아래 "완료 기록" 참고). `RunState.die_inventory`(pip_inventory와
     같은 패턴)를 신설해, "다이스 승급" 아이템(kind=upgrade_die)이 더 이상
     `DiceItemPool.apply()`로 즉시 가장 작은 다이스를 자동 교체하지 않고
     `apply_upgrade_gain()`으로 인벤토리에 쌓이기만 함. `CustomizePanel`에 "다이스
     인벤토리" 흐름(인벤토리에서 다이스 선택 → 넣을 자리 선택 → 즉시 교환, 실제
     승급되는 자리만 선택 가능)을 추가해 플레이어가 나중에 직접 교체를 결정. 남은
     것은 이 2단계 흐름이 실제로 "쉽게 인지되는지"와, 다이스 인벤토리에 여러 개가
     쌓였을 때 UX가 번잡하지 않은지 사람 플레이 피드백뿐. **2026-09-09 (74) 추가
     보완**: 2단계(교체할 자리 선택)에서 양쪽 주머니 모두 승급 가능한 자리가 없을
     때 버튼만 전부 비활성화되고 이유 설명이 없던 공백을 발견해 `_has_upgrade_
     target()` + 안내 문구로 메움(아래 "완료 기록" 참고).
   - **"성장의 재미가 없다"** — 2026-09-09에 사용자가 구체적인 방향 2개를 제안함:
     (1) 다이스 개수를 N개로 제한하고 그 고정 풀 안에서 다이스/눈금을 "교체"하는
     방식으로 성장을 표현(지금의 "계속 늘어나는" 방식 대신), (2) 몇 턴 쿨타임이
     있는 스킬을 플레이어가 보유(캐릭터별로 다른 스킬). (1)의 첫 조각(캡 도입 —
     주머니가 `DiceBag.MAX_DICE`=6에 도달하면 "다이스 추가" 아이템이 막힘)은
     **2026-09-09 (76)에 구현 완료** — 아래 "완료 기록" 참고. 다만 (1)이 요청한
     "고정 풀 안에서 교체"의 "교체" 절반은 아직 새로 만든 게 아니라 기존에 있던
     "다이스 승급"(면 개수 늘리기)/"눈금 교환" 기능을 그대로 재사용한 것뿐이라,
     "다이스 개수 대신 눈금/면 구성으로 성장한다"는 감각이 실제로 잘 전달되는지는
     사람이 캡에 도달할 때까지 플레이해봐야 판단 가능(5방 런에서 캡 6개까지 실제로
     도달하는지부터 확인 필요 — 캡 수치 자체도 잠정값). (2)(캐릭터별 쿨다운 스킬)는
     여전히 미착수 — 캐릭터가 1종뿐이라 스킬 콘텐츠 자체를 새로 기획해야 하는 더 큰
     작업이라 다음 이터레이션 이후로 남겨둠.
   - ~~"전투의 재미가 없다" — 다이스 값이 잘 안 보여서 쪼이는 맛이 덜하다 / 이미지로
     보여지는 부분이 덜한데, 주사위값 사이즈업·체력바 추가 등 정보를 텍스트보다
     이미지로 표현하는게 좋을 듯~~ → **구체적으로 지적된 부분(다이스 값 칩
     사이즈업, HP 막대 추가) 처리 완료** (2026-09-09 (69), 아래 "완료 기록" 참고).
     교환 결과 칩 34→44px, 플레이어/몬스터 HP 막대(비율별 색 변화) 추가. "쪼이는
     맛"이 실제로 개선됐다고 느끼는지는 최종적으로 사람이 플레이해보고 판단해야
     하는 영역 — 완전히 닫힌 항목은 아님.

1. **던전 맵 내용 채우기 (계속)** — "런에 끝이 있다"(`RunState.TOTAL_ROOMS=5`, 완료 시
   화면 전환), "방마다 다른(더 강한) 몬스터", ~~상점 방 종류 추가~~(완료됨 — 아래
   "완료 기록"의 "골드 + 상점 이벤트" 참고), ~~특수 이벤트 방 종류 추가~~(완료됨 —
   아래 "완료 기록"의 ""특수 이벤트" 던전 방 추가" 참고), ~~스토리 이벤트 방 종류
   추가~~(완료됨 — 아래 "완료 기록"의 ""스토리 이벤트" 던전 방 추가" 참고)는
   추가했지만, 방 선택지가 항상 "전투/상점/특수 이벤트/스토리 이벤트" 4개 고정으로만
   나열됨(무작위/기획된 조합이 아니라 매번 같은 4종류 중 확률 노출). 다음 단계 후보:
   - ~~3개 방 선택지가 매번 전부 노출되는 대신, 방마다 일부만 무작위로 제시되거나
     순서가 섞이는 것이 "레벨 디자인"에 더 가까울 수 있음~~ → **완료됨** (노출 확률은
     이전 이터레이션, 순서 섞기는 2026-09-08 (25), 아래 "완료 기록" 참고). "전투 방"은
     항상 노출(진행 보장) + 항상 첫 자리, "상점"/"특수 이벤트"/"스토리 이벤트"는 방마다
     각각 60%/50%/50% 확률로 독립 노출되고, 노출된 것끼리는 방마다 결정적 RNG로 표시
     순서도 섞임(버튼 목록과 MapStrip 칩 순서가 항상 일치). 확률 자체가 적당한지, 순서
     섞기가 실제로 "레벨 디자인" 체감에 도움이 되는지는 사람 피드백 필요.
   - "스토리 이벤트"의 시나리오는 이제 8종(낡은 보물상자/떠돌이 상인/휴식처/수상한
     도박꾼/무너진 제단/숨겨진 샛길/부상당한 여행자/버려진 야영지)이고, 골드
     증감폭(±5~25, 확률 50~60%)은 감으로 잡은 잠정값 — 실제로 플레이해보고 텍스트
     톤이나 골드 임팩트가 적당한지, 8종으로도 여전히 반복 플레이 시 질리는지는
     사람 피드백 필요 (더 늘릴지는 피드백을 보고 판단).
   - 신규 "특수 이벤트" 방의 아이템 3종(D8 추가/D12 추가/가장 작은 다이스->D10 승급)과
     등장 빈도(매 방마다 상시 노출)는 감으로 잡은 잠정값 — 실제로 플레이해보고 너무
     자주/강하게 다면체 다이스를 얻을 수 있는 건 아닌지 사람 피드백 필요.
   - 상점 가격(`shop.gd`의 `ITEM_COSTS`)과 골드 보상(`combat_test.gd`의
     `GOLD_REWARD_BASE`/`GOLD_REWARD_PER_ROOM`)은 감으로 잡은 잠정값 — 실제 플레이해보고
     너무 쉽게/어렵게 아이템을 살 수 있는 건 아닌지 사람 피드백 필요.
   - 몬스터 난이도 스케일링 공식(`combat_test.gd`의 `_monster_config_for_room()` —
     공격 2방마다 +1개, 방어 3방마다 +1개, HP 매 방 +3)은 감으로 잡은 잠정값. 실제
     플레이해보고 체감 난이도 곡선이 적당한지, 후반 방에서 다이스 개수가 늘어난
     시각적 레이아웃(안 겹치는지)이 괜찮은지 사람 피드백 필요
   - ~~몬스터별로 다른 이름/다이스 색 표시~~ → 완료됨 (이름 5종 순환 + 다이스 색 구분,
     아래 "완료 기록" 참고).
   - ~~몬스터 주머니는 여전히 항상 DiceBag.new(4, ...)(D4만)로 생성돼 실제로는 몬스터
     다이스가 D6/D8로 보일 일이 없음~~ → **완료됨** (이전 이터레이션, 아래 "완료 기록"
     참고). `_monster_dice_sides_for_room()`으로 room 0-1: D4, 2-3: D6, 4+: D8 스케일링
     추가. ~~물리적으로(다이스 개수/크기가 커진 방에서 벽 밖으로 안 나가는지) 실제
     배틀로 검증된 적은 없었음~~ → **검증 완료** (이번 이터레이션, 아래 "완료 기록"의
     `GAME_QA_ROOM_OVERRIDE` 항목 참고). room4(공격 4D8/방어 2D8)까지 실제 정지
     감지 경로로 진행시켜 확인 — 벽 밖으로 나가는 다이스 없음. 남은 것: 이 스케일링이
     기존 개수/HP 스케일링과 겹쳐 후반 난이도가 과도해지는 건 아닌지 사람 피드백
     필요(감으로 잡은 구간값, 이건 물리적 검증과 별개로 여전히 사람 판단 영역).
     몬스터별로 "의도적으로" 다른 다이스 형태(예: 특정 몬스터만 유독 큰 다이스)를
     쓰는 것까지는 아님 — 지금은 방 번호에만 연동되고 몬스터 개성과는 무관(몬스터
     다이스 특이 특징 큐 9와 연결될 수 있는 부분).
   - `TOTAL_ROOMS=5`는 감으로 잡은 잠정값 — 실제 플레이해보고 체감 길이가 적당한지
     사람 피드백 필요
2. **전투 씬 다듬기 / 후속 검토**
   - 지금은 사람이 실제로 플레이해봐야 판단 가능한 부분이 많음 (INBOX.md 피드백 대기):
     정지 감지 이후 교환 속도(체감 템포)가 적당한지, "물리 다이스 눈 ≠ 실제 판정값"
     불일치가 체감상 이상한지 등.
   - ~~다이스 개수가 늘어나면 안 겹치고 잘 보이는지~~ → **스폰 위치가 벽 밖으로
     나가는 문제는 고침**(그리드 레이아웃, 한 줄에 4개까지만 놓고 넘치면 다음 줄로),
     **"겹쳐서 정보가 안 보이는지"도 검증 완료**(2026-09-04 (18), 아래 "완료 기록"
     참고). `GAME_QA_ROOM_OVERRIDE=10`으로 다이스 10개(공격 7 + 방어 3)가 한 교환에
     동시에 구르는 상황을 실제 배틀 경로로 만들어 확인 — 벽 밖 이탈 없음, 공격/방어
     색 구분은 다이스가 뒤섞여도 유지됨, 실제 판정값은 항상 2D 도형 칩에서 읽으므로
     3D 다이스가 촘촘해져도 정보 손실은 없음. **남은 것은 딱 하나, 그리고 이건
     AI가 판단할 수 없는 영역**: 카메라 배율상 다이스 뭉치가 화면에서 작게 보이는
     것이 "손맛"으로 충분히 만족스러운지 — 크래시나 정보 손실이 아니라 순수한 느낌의
     문제라 사람이 직접 플레이해보고 INBOX.md에 남겨줘야 판단 가능함(불만족스러우면
     카메라 줌/다이스 크기/두 그룹 간 x축 간격 확대 등으로 대응 가능).
   - ~~다이스 정지 감지 임계값이 다이스 개수가 늘어나면 재검증 필요~~ → **재검증 및
     근본 개선 완료** (아래 "완료 기록" 참고). 정지 판정을 "전체 공용 카운터"에서
     "다이스별 독립 카운터"로 바꿔, 14개(교환당 15개)에서도 매번 4초 안전장치로
     빠지던 것이 2.7~3.4초 안에 자연 정지 감지되도록 개선됨. 남은 것은 실제 플레이에서
     한 주머니가 그 정도로 커질 수 있는지(현재 아이템 시스템상 5방 런에서는 현실적으로
     어려움), 그리고 다이스가 많을 때 서로 겹쳐 구르는 "과정"의 시각적 체감 — 둘 다
     사람 판단/피드백 영역.
3. **다이스 개조(빌드업) 아이템 시스템 다듬기** — 1차 버전(`systems/dice_item_pool.gd`,
   아이템 4종: 다이스 추가/승급/약한 면 강화/모든 면 통일, 전투 승리 후 2개 중 선택)과
   사용자 지정 "커스터마이징"(특정 다이스의 특정 면을 골라 1..면 개수 범위의 값으로
   교체 — 이전 이터레이션의 "+3 누적, 상한 없음" 방식에서 INBOX.md 지시로 변경됨)은
   구현됨. 다음 후보:
   - ~~"다이스 승급"류 아이템(상점 D6 승급/특수 이벤트 D10 대승급)이 승급 대상(더
     작은 다이스)이 하나도 없을 때 가장 작은 다이스를 골라 같은 면 개수로 재교체하면서
     그 다이스의 커스터마이징된 면 값을 표준값으로 조용히 리셋시키던 버그~~ →
     **완료됨** (2026-09-04 (17), 아래 "완료 기록" 참고). 이제 실제로 면 개수가
     늘어나는 다이스가 있을 때만 교체하고, 없으면 아무 일도 하지 않음.
   - ~~승급 대상이 없을 때 아이템을 헛되이 소모하게 되는 건 남아있음 — 더 나은 처리는
     UI에서 애초에 이런 상황엔 아이템을 제시하지 않는 것~~ → **완료됨** (2026-09-04
     (19), 아래 "완료 기록" 참고). `DiceItemPool.is_applicable(item, bag)`로 승급
     대상이 없는 주머니 쪽 버튼을 비활성화("승급 대상 없음")하도록 전투 보상/상점/
     특수 이벤트 3곳 모두 반영.
   - ~~그래도 양쪽 주머니 모두 대상이 없으면 2개짜리 보상 슬롯 하나가 통째로
     비활성화된 아이템으로 낭비됨~~ → **완료됨** (2026-09-07 (20), 아래 "완료 기록"
     참고). `random_choices(n, attack_bag, defense_bag)`가 양쪽 다 적용 불가능한
     아이템은 후보에서 아예 제외(부족해지면 폴백)하도록 개선.
   - ~~아이템 종류가 3개뿐~~ → 4종으로 늘림(이번 이터레이션, "모든 면 통일" 추가 —
     DESIGN.md가 예시로 들었던 "모든 면을 6으로 만들기"를 반영). 강도가 감으로 잡은
     값(D4→D6 고정, 매번 최저면/전체면→최댓값, 상점가 25골드)이라 실제로 플레이해보고
     밸런스가 괜찮은지 사람 피드백 필요.
   - 커스터마이징이 이제 면 개수로 상한이 걸려서(D4는 4를 못 넘음) 예전만큼 "사기
     주사위"가 쉽게 나오진 않음 — 다만 "다이스 승급"(면 개수 자체를 늘리는 아이템)과
     조합하면 승급 후 다시 커스터마이징해서 상한을 계속 올릴 수 있음. 이 조합이 실제로
     "사기 주사위 재미"를 잘 살리는지는 사람 플레이 피드백 필요.
   - ~~INBOX.md는 "강화 카드"라는 표현을 썼는데(주사위 눈이 그려진 카드 형태 UI
     이미지), 지금 구현은 버튼 목록 형태라 시각적으로 "카드"처럼 보이지 않음~~ →
     **금테 두른 카드 패널로 개선 완료** (2026-09-07 (22), 아래 "완료 기록" 참고).
     `code/scenes/item_card_style.gd`(`ItemCardStyle`)로 승리 보상/상점/특수 이벤트
     3개 화면의 아이템 목록이 카드 형태(제목/설명/가격/버튼을 담은 금테 패널)가 됨.
     "주사위 눈이 그려진" 부분(카드 안에 다이스 면 이미지 미리보기)도 **2026-09-07
     (23)에 추가 완료** — 결과 다이스가 확정적인 아이템(다이스 추가/승급)에 한해
     `ShapeDieChip` 면 미리보기를 카드에 넣음. **2026-09-08 (24)에 나머지 두 종류
     (약한 면 강화/모든 면 통일)까지 마저 채움** — 이 둘은 카드 생성 시점이 아니라
     "어느 주머니에 적용할지"가 정해져야 결과가 결정되므로, 버튼 옆에
     `DiceItemPool.preview_effect(item, bag)`으로 그 순간 계산한 미리보기(전/후 값
     또는 통일값)를 붙임(아래 완료 기록 참고). 이제 4종 아이템 전부 카드에 결과
     미리보기가 있음. 남은 것은 실제로 보고 미리보기가 유용한지, 칩 크기(16px)가
     적당한지 사람 피드백뿐.
   - ~~지금은 "물리로 보이는 DieD4"가 항상 정사면체(D4) 모양~~ → **완료됨** (이전
     이터레이션, 아래 "완료 기록" 참고). `code/dice/die_d4.gd`(class_name `Die`)가
     `sides`에 맞춰 D4/D6/D8을 정확한 모양으로, D10/D12는 둥근 임시 근사로 스폰함.
   - ~~D10의 정확한 지오메트리(정오각 사다리 십이면체)가 아직 없음~~ → **완료됨**
     (이전 이터레이션, 아래 "완료 기록" 참고). `_build_pentagonal_trapezohedron()`
     추가로 실제 D10 주사위와 같은 위상 구조를 갖춘 모양이 됨.
   - ~~D12(정십이면체)의 정확한 지오메트리가 아직 없음~~ → **완료됨** (이전
     이터레이션, 아래 "완료 기록" 참고). `_build_dodecahedron()` 추가로 오각형 면
     12개짜리 실제 정십이면체 모양을 갖춤.
   - ~~D20 등 나머지 다면체는 여전히 SphereMesh 근사~~ → **완료됨** (이번 이터레이션,
     아래 "완료 기록" 참고). `_build_icosahedron()` 추가로 정삼각형 면 20개짜리 실제
     정이십면체 모양을 갖춤 — 이제 D4/D6/D8/D10/D12/D20 전부 정확한 지오메트리
     완비(D100 등만 여전히 SphereMesh 근사, 지금 아이템 풀에 주는 곳이 없어 급하지
     않음).
   - ~~지금 아이템 풀은 D8/D10/D12까지만 주고 D20을 주는 아이템은 아직 없음~~ →
     **완료됨** (이번 이터레이션, 아래 "완료 기록" 참고). `EventItemPool`에 "다면체
     주사위 획득 (D20)" 아이템 추가. 남은 것은 등장 빈도/체감 강도가 적당한지 사람
     피드백뿐(다른 특수 이벤트 아이템들과 같은 확률로 무작위 2개 중 노출되므로 D20이
     상대적으로 드묾 — 4종 중 1개).
   - ~~D8/D10/D12/D20 모양이 화면에서 D4와 시각적으로 뚜렷이 구별되는지(작은 화면
     크기 + 현재 조명에서 다들 "회색 삐죽삐죽한 조각"으로 보일 수 있음)~~ →
     **재질별 시각 색 추가로 개선 완료** (이번 이터레이션, 아래 "완료 기록" 참고).
     재질(플라스틱/나무/유리/철제)에 `visual_color`를 추가해 D4=연회백,
     D8=갈색(나무), D10=옅은 청록 반투명(유리), D12/D20=은회색(철제)으로 색이
     확실히 갈림 — `qa_out/combat_test_material_swatch.png` 참고. 남은 것은 이
     색 배정이 실제로 "재질스럽게" 설득력 있어 보이는지 사람 피드백뿐. 참고로 지금
     아이템 풀은 D8/D10/D12까지만 주고 D20을 주는 아이템은 아직 없음(구현은 준비돼
     있으니 나중에 아이템/등급을 추가하면 바로 쓸 수 있음).
   - 몬스터는 개조 대상이 아님(DESIGN.md 확정 — 몬스터 주머니는 고정), 그대로 유지.
   - 패배 시 `RunState.reset_run()`으로 다이스 개조 내역도 함께 초기화됨(런 실패 시
     빌드가 리셋되는 로그라이크 관례를 따름) — 이게 맞는 방향인지도 사람 판단 필요.
4. **사운드 에셋 실제 교체** — 이번 이터레이션에서 `code/dice/procedural_sound.gd`로
   합성한 임시 타격음이 실제 에셋 부재 시 자동 재생되도록 바뀌어서 "완전 무음"은
   해결됨(아래 "완료 기록" 참고). ~~유리/나무/철제 등 다른 재질을 추가할 때 재질별로
   다른 합성음이 필요한지~~ → 재질 자체(플라스틱/나무/유리/철제 4종, bounce/friction +
   재질별 합성음)는 추가 완료됨(이번 이터레이션, 아래 "완료 기록" 참고). 남은 것:
   - 실제 녹음/제작된 `.wav`/`.ogg`로 교체하면 각 `res://resources/materials/*.tres`의
     `impact_sound`만 채우면 되고 코드 변경은 불필요.
   - 지금 4종 합성음이 실제로 재질별로 "그럴듯하게" 구별되어 들리는지는 사람이
     플레이해보고 판단해야 함 (사람이 실제로 들어야 판단 가능한 영역).
   - 재질을 플레이어가 직접 고르거나 획득하는 시스템은 없음 — 지금은 다이스 면
     개수에 따라 코드가 임의로 배정(D4/D6=플라스틱, D8=나무, D10=유리, D12/D20=철제).
     이 매핑이 적당한지, 아니면 재질을 별도 아이템/상점 선택지로 만들지는 사람
     판단 필요.
5. ~~(INBOX.md 신규) 특수 이벤트에서 다이스 개수/다면체 늘리기~~ → **완료됨** (이번
   이터레이션, 아래 "완료 기록" 참고). `systems/event_item_pool.gd` +
   `scenes/event.gd`(+`.tscn`)로 던전 맵에 "특수 이벤트" 방 추가, D8/D10/D12급
   아이템을 무료로 획득 가능. 남은 것은 등장 빈도/강도 밸런스 사람 피드백뿐 (큐 1에
   남겨둠).
6. ~~(INBOX.md 신규) 던전 입장 시 캐릭터 선택~~ → **부분 완료** (이번 이터레이션,
   아래 "완료 기록" 참고). `scenes/character_select.tscn`이 새 main_scene이 되어
   "던전 시작" 버튼으로 진입하는 흐름은 완성됨. 남은 것:
   - **실제 일러스트 에셋이 없음** — 지금은 `character_portrait_placeholder.gd`가
     `_draw()`로 그리는 도형 조립 실루엣(라벤더 단발머리 + 분홍 원피스)일 뿐. 사람이
     실제 스프라이트/일러스트를 구하거나 그려서 교체해야 함 (AI는 이미지 생성 도구가
     없어 진짜 아트를 만들 수 없음). 스타일 방향이 이거면 되는지도 사람 확인 필요.
   - 캐릭터가 1종뿐이라 지금은 "선택"이 아니라 "확인" 버튼에 가까움 — 정말 여러
     캐릭터 중 고르는 화면이 필요하면 카드 여러 개를 나열하는 구조로 확장해야 함
     (사람 지시 없으면 이대로 유지, DESIGN.md에도 "능력 차별화 없는 1종"이 확정값).
7. ~~(INBOX.md 신규) 전투 스프라이트 + 표정 연출~~ → **부분 완료** (이번 이터레이션,
   아래 "완료 기록" 참고). `scenes/character_portrait_placeholder.gd`(플레이어,
   기존 캐릭터 선택 화면과 공유)와 신규 `scenes/monster_portrait_placeholder.gd`
   (몬스터, 뿔 달린 블롭 실루엣)를 `combat_test.tscn`에 배치하고, 공방 결과/승리/패배에
   따라 표정(neutral/happy/hurt/sad/angry)이 바뀌도록 `combat_test.gd`에 배선함.
   남은 것:
   - **실제 일러스트/스프라이트 에셋이 없음** — 지금은 도형 조립 플레이스홀더일 뿐
     (캐릭터 선택 화면과 동일한 한계). "몬스터 무스메"(의인화된 미소녀) 컨셉은 전혀
     반영 안 됨 — 지금 몬스터 실루엣은 뿔 달린 블롭이라 무스메와 거리가 멂. 사람이
     실제 아트 방향을 정하거나 리소스를 구해서 교체해야 함.
   - 표정은 5종(neutral/happy/hurt/sad/angry)뿐이고 전환도 순간적(트랜지션 없음) —
     체감이 어떤지 사람 피드백 필요.
   - 몬스터별 성격(큐 8)이 아직 없어서 표정 변화가 모든 몬스터에 획일적으로 적용됨.
8. **(INBOX.md 신규) 몬스터별 성격 디자인** — 사용자가 "추후 기획, 할 일 거리에 저장"
   이라고 명시. 지금 `MONSTER_PROFILES`(`combat_test.gd`)는 이름/다이스 색만 있고
   성격/말투 등은 없음 — 나중에 기획 문서(DESIGN.md)에 먼저 구체화된 뒤 반영해야 함.
9. ~~(INBOX.md 신규) 몬스터별 다이스 특이 특징~~ → **완료됨** (예시 3개 전부 구현,
   아래 "완료 기록" 참고). 2026-09-09 사용자가 구체 예시 3개를 줌: (1) 고정값
   다이스(굴리지 않고 항상 같은 값) → "오크"(2026-09-09 (77), `DiceBag.
   force_fixed_value()`), (2) min/max 전용 다이스(중간값 없음, 하이리스크/
   로우리스크) → "다크 나이트"(2026-09-09 (75), `DiceBag.force_min_max_faces()`),
   (3) 분노 스택 → 몇 개 쌓이면 다음 턴 20면체 굴림 → "고블린"(2026-09-09 (78),
   `DiceBag.count_max_rolls()` + `combat_test.gd`의 전투 중 상태
   `monster_anger_stacks`/`monster_anger_pending`, 최댓값 면이 나올 때마다 스택 +1,
   3스택에 다음 공격 한 턴만 1D20). 남은 것은 순수하게 사람 판단 영역:
   - 어떤 몬스터에 어떤 기믹을 배정할지는 몬스터 성격 기획(큐 8)이 먼저 나와야
     "외형/특성에 어울리는" 배정이 가능함 — 지금 "고블린"(분노)/"다크 나이트"
     (min/max)/"오크"(고정값)에 준 기믹은 전부 성격 기획 없이 임의로 고른 임시값
     (재배정 가능).
   - 분노 스택 임계치(3)와 "최댓값 면 = 분노 유발 조건"이라는 해석이 실제로
     체감 좋은 빈도로 발동하는지(D4 다이스 2개 기준 한 턴에 최댓값이 나올 확률
     약 44% — 감으로 잡은 몬스터별 첫 배정일 뿐 밸런스 확정 아님)는 사람 플레이
     피드백 필요.
10. ~~(INBOX.md 신규) 골드 + 상점 이벤트~~ → **완료됨** (이번 이터레이션, 아래 "완료
    기록" 참고). `RunState.gold`, 전투 승리 골드 지급, `scenes/shop.gd` 구현.
    남은 것은 가격/보상 밸런스 사람 피드백뿐 (큐 1에 남겨둠).
11. ~~**(INBOX.md 신규, 부분 완료) 레벨 디자인된 여러 스테이지**~~ → **완료됨**
    (아래 "완료 기록" 참고). "일단은 선형으로 점차 강해지게, 스텝 바이 스텝으로
    수평적 선택지를 넓힌다"는 방향 확인. 던전 맵이 "전투 방" / "상점" / "특수 이벤트" /
    "스토리 이벤트" 네 선택지를 보여주는 것, 그 선택지가 방마다 확률적으로 일부만
    노출되는 것, 순수 텍스트형/스토리형 선택지 이벤트(다이스와 무관)까지 전부 채워짐.
    남은 것은 확률/밸런스가 적당한지 사람 피드백뿐(큐 1로 이관).
12. ~~(INBOX.md 신규 2026-09-02) 다이스 눈을 이미지로 표시~~ → **완료됨** (이번
    이터레이션, 아래 "완료 기록" 참고). "단순 나열된 사각형 각 면에 숫자" 방식(전개도
    방식 대신)으로 구현 — 절차적 StyleBoxFlat 칩이라 별도 아트 에셋 불필요. 남은 것은
    실제로 "어떻게 개조하면 어떤 주사위가 될지 예상하기 쉬운지" 사람 피드백뿐.
13. **(INBOX.md 신규 2026-09-09, [대형 기획 3] 부분 처리됨) 업적 시스템 — 남은
    항목 추가.** `code/systems/achievement_manager.gd`(`AchievementManager`)와
    `code/scenes/achievement_panel.gd`(`AchievementPanel`) 골격은 2026-09-09 (79)에
    완성됨. 등록 12종(#25/#1/#6 + (80)의 "부자"/"무결점 승리"/"기사회생"/"오버킬" +
    (81)의 "눈금 수집가"/"다이스 수집가"/"가득 찬 주머니" + (82)의 "단골 손님"/
    "재질 수집가") 외에 INBOX.md가 원래 제안했던 30종 목록 중 남은 항목들
    (원문이 보존돼 있지 않아 정확한 목록/개수는 알 수 없음 — 예시로 기억하는 것만:
    라운드 2/3 클리어·보스 격파 계열은 [대형 기획 2] 보스 구조가 먼저 있어야
    트리거 지점이 생김)을 몇 개씩 나눠서 `DEFINITIONS`에 추가하고 해당 조건
    지점에서 `AchievementManager.unlock(id)`를 호출하는 작업만 하면 됨(저장/UI는
    이미 자동으로 따라옴, `_bag_has_d20`/`_is_flawless_win` 같은 "판정 전용 순수
    함수 + dice_test.gd 단위 검증" 패턴을 그대로 재사용하면 됨). **다만 (82)
    이후로는 "새 RunState 카운터만 추가하면 되는" 독립 항목(상점 이용 횟수, 재질
    보유 등)이 거의 소진된 상태** — 남은 후보 대부분이 캐릭터별 보스 클리어
    5종처럼 [대형 기획 1]과 [대형 기획 2]가 **둘 다** 있어야 트리거 지점이
    생기는 것들이었는데, [대형 기획 1](5종 캐릭터, 2026-09-09)과 [대형 기획 2]
    (라운드/보스 구조, 2026-09-10 (89))가 **둘 다 이제 완료돼** 트리거 지점
    자체는 이제 다 갖춰짐 — 예를 들면 `RunState.round_index`/보스 처치 시점을
    조합해 "라운드 2/3 클리어"/"최종 보스(3라운드) 격파"/"광전사로 첫 클리어"
    같은 캐릭터·라운드 조합 업적을 바로 추가할 수 있는 상태(다음 이터레이션
    후보). 해금 "순간"에 화면에 토스트/팝업 알림을 줄지(지금은 목록을 직접
    열어야만 확인 가능)는 사람 판단 필요 — 아직 안 만듦.
14. ~~(INBOX.md 신규 2026-09-09, [대형 기획 1]) 플레이어블 캐릭터 — 5종 중
    선택~~ → **완료됨(2026-09-09 (84)+(85)+(86)), 5/5종.**
    `code/systems/character_profiles.gd`(`CharacterProfiles.PROFILES`)에
    캐릭터 데이터 구조(id/이름/설명/시작 기믹/머리·원피스 색)를 정의하고,
    `character_select.tscn`을 카드 여러 개 중 하나를 고르는 선택 UI(카드
    폭이 캐릭터 수에 맞춰 동적으로 줄어들어 화면 밖으로 안 잘림,
    `CARD_WIDTH_MAX`/`ROW_MARGIN`)로 완성. 등록된 5종: "견습
    모험가"(기존, 기믹 없음)/"광전사"(극단형, `DiceBag.force_min_max_faces()`를
    공격+방어 둘 다에 적용)/"수호자"(안정형, `DiceBag.
    force_fixed_value_for_die()`로 방어 다이스 딱 하나만 고정값)/"폭발병"
    (폭발형, `combat_test.gd`의 전투 중 상태로 공격 다이스 최댓값 스택을
    추적해 3스택에서 다음 공격 1D20)/"방패병"(인내형, 폭발병과 대칭 구조로
    방어 다이스 최댓값 스택을 추적해 3스택에서 다음 방어 1D20, 2026-09-09
    (86)에 다섯 번째 컨셉으로 AI가 제안·구현) — "폭발병"/"방패병" 둘 다 정적
    다이스 개조가 아니라 `run_state.gd`의 match 문에는 안 걸리고
    combat_test.gd가 직접 처리. "견습/광전사/수호자"는
    `RunState.reset_run(character_id)`가 새 주머니를 만든 직후 정적으로
    기믹을 적용하고, 인자 없이 부르는 기존 호출부(패배 후 재시작 등)는 직전
    캐릭터를 유지. 캐릭터별 실루엣은 기존 `character_portrait_placeholder.gd`를
    색상만 다르게 재사용(`set_palette()`). 남은 것은 5종 전체의 강화폭/체감
    밸런스가 적당한지 사람 플레이 피드백뿐 — INBOX.md 원 요청은 전부 처리됨.
15. ~~(INBOX.md 신규 2026-09-09, [대형 기획 2]) 던전 마지막 보스 + 3라운드 구조~~
    → **완료됨(3/3 조각, 2026-09-10 (89)).** 마지막 방(room_index ==
    `RunState.TOTAL_ROOMS - 1`)의 몬스터를 "보스"로 취급해 HP×2/공격+2개/
    방어+1개로 강화하는 것(2026-09-09 (83)), `RunState`의 `round_index`/
    `TOTAL_ROUNDS`/`is_last_round()`/`advance_round()`(2026-09-09 (87)), 보스
    처치 시 다음 라운드로 재진입(2026-09-10 (88)), 3라운드 전부 클리어 시
    "게임 클리어!" 전용 문구(2026-09-10 (89))까지 전부 반영됨 — 위 "완료 기록"
    참고. **여전히 사람 판단이 필요한 것(구현 완료와 별개):** 보스 강화폭
    (+2/+1/×2)이 적당한지, 라운드가 올라갈수록 보스/몬스터가 더 강해져야
    하는지(INBOX.md 원문이 언급했던 부분, 아직 미반영 — 지금은 라운드 2/3도
    라운드 1과 완전히 같은 몬스터 스케일링을 그대로 재사용)는 사람 플레이
    피드백 필요.
16. **(INBOX.md 신규 2026-09-14, 대량 피드백 — 5/약 30개만 처리됨) 남은 항목들.**
    "방 다섯개 클리어 후 라운드가 안 올라간다" 버그(2026-09-14 (90)),
    "커스터마이징 팝업이 보상 아이콘에 뚫리는" 버그(2026-09-14 (91)), 던전 맵
    보상 아이콘 범례 + 상점 입장 버튼 하단 보유 골드 표시(2026-09-15 (92)),
    상점 2번째/4번째 방 고정 노출(2026-09-15 (93))만
    처리됨(위 "완료 기록" 참고). 나머지는 서로 독립적으로
    착수 가능한 항목들이라 다음 이터레이션들이 하나씩(또는 작게 묶어서)
    골라 진행하면 됨 — 원문 그대로 INBOX.md "남은 이슈"에 남아있으니 반영 시
    거기서 항목을 지우지 말고 처리됨으로 옮길 것. 대략적인 분류:
    - **키보드 조작/단축키**: 전체 조작을 키보드로도 가능하게, 단축키가 있는
      버튼에는 단축키 표시.
    - **캐릭터 선택 화면 UX 개편**: 외형+이름만 간략히 나열하고, 패널 선택 시
      오른쪽에 상세 정보(캐릭터 설명/초기 다이스/보유 스킬) 패널을 띄우는
      구조로 변경.
    - **캐릭터별 시작 주사위 차별화**: 지금은 5종 다 기믹만 다르고 시작
      다이스 자체(면 개수/구성)는 표준 D4x3+D4x3로 동일 — 캐릭터 개성에 맞게
      시작 주사위 구성 자체를 다르게.
    - **아이콘 추가 2건**: 캐릭터 스킬 아이콘, 업적 유형별 아이콘(골드 계열은
      금화 아이콘, 다이스 계열은 다이스 아이콘 등).
    - ~~**던전 맵 보상 아이콘 범례**~~ → **완료됨** (2026-09-15 (92), 위 "완료
      기록" 참고). `dungeon_map.gd`의 `_setup_reward_legend()`가 골드 라벨
      오른쪽에 RewardIcon(골드/눈금/다이스) + 텍스트 라벨 범례를 상시 표시.
    - ~~**커스터마이징 팝업 겹침 버그**~~ → **완료됨** (2026-09-14 (91), 위
      "완료 기록" 참고). `CustomizePanel.open()`에 `move_to_front()`를 추가해
      패널이 항상 부모의 마지막 자식(= 최상단)으로 그려지도록 고침 — 이후에
      추가되는 다른 형제 노드(보상 아이콘 등)가 팝업을 뚫고 보이는 문제를
      5개 사용처(dungeon_map/shop/event/story_event/combat_test) 공통으로 막음.
    - **상점 UX 다수** (아래 둘은 완료, 나머지는 미착수): ~~입장 버튼 하단에
      보유 골드 표시(선택 무의미함을 미리 알림)~~ → **완료됨** (2026-09-15 (92),
      위 "완료 기록" 참고). `dungeon_map.gd`의 `_shop_gold_label`이 "상점 입장"
      버튼 아래에 보유 골드를 표시하고, 최저가(10골드)도 못 살 정도면 "(구매
      가능한 품목 없음)" 경고 문구 + 붉은색으로 표시. ~~5방 기준 2번째/4번째
      방에만 상점 고정 노출~~ → **완료됨** (2026-09-15 (93), 위 "완료 기록"
      참고). `_room_options_for_index()`의 상점 판정을 확률(SHOP_CHANCE)에서
      `SHOP_FIXED_ROOM_INDICES`(=[1, 3]) 고정으로 교체. 나머지 미착수: 골드
      부족 품목은 "골드 부족" 버튼 남발 대신 패널 색상 변경으로 비활성 표시
      (품목 상단에 골드 부족/판매됨 상태 표시), 골드 부족한 선택지가 있는
      화면(2택 등)에서는 아예 그 선택지를 비활성화해 다른 쪽으로 유도, 품목
      카드와 "던전으로 돌아가기" 버튼이 겹치지 않게, 이미지를 키우고 이미지
      위주로 설명해 직관성 향상(보상 팝업과 같은 방향).
    - **전투 화면**: 몬스터 HP바 하단에 그 몬스터의 스킬/전투 정보 표시(개발용,
      나중에 제거/간소화 가능).
    - **보상 팝업 개편**: 설명 텍스트가 길어 안 읽힘 — 이미지를 키우고 이미지
      위주로 설명. 보상 등급(S/A/B/C 4단계) 신설, 카드에 등급 표시 + 등급별
      색상(녹색<파란색<보라색<노란색)을 테두리/타이틀에 적용.
    - **커스터마이징 UX 전면 개편**: 지금 흐름이 헷갈린다는 피드백 — 인벤토리
      및 덱 구성(내 덱에 세팅된 주사위/여분 주사위/보유 눈금)이 먼저 한눈에
      보이는 구조로 바꾸고, 전투 제외 아무 때나 접근/교체 가능하게(이미 아무
      때나 접근은 가능 — "한눈에 보이는 구조"가 핵심 요청).
    - **특수 이벤트 개편(가장 큰 덩어리)**: 지금 보상이 전투 보상과 별 차이
      없거나 리스크 없이 리턴만 큼 — (1) 임의의 스토리(예: 트랩에 손을 뻗어
      D20을 집는 상황)를 부여하고 리스크/리턴을 선택하게, (2) D&D 난이도
      체크처럼 주사위를 굴려 성공률을 따지게, (3) 캐릭터별로 공격/방어와
      독립된 "이벤트 주사위" 1개를 두어 특수 이벤트 특화 캐릭터도 가능하게
      (이벤트 주사위는 일반 눈금으로 커스터마이징 불가 — 로마 숫자 표기 등으로
      구분), (4) 특수 이벤트 선택지 옆 아이콘은 보상을 스포하지 않는 "물음표"
      아이콘 하나로 통일. 이 넷은 서로 얽혀 있어 순서/설계를 먼저 정하고
      착수하는 게 안전 — 사람 설계 확인 권장.
    - **캐릭터 스킬 이벤트 신설**: 캐릭터에 스킬을 부여하는 이벤트 추가(직업
      공용 이벤트 + 직업 고유 이벤트).
    - **캐릭터 정보 상시 열람**: 캐릭터 정보/보유 스킬을 아무 때나 볼 수 있게.
    - **DESIGN.md 기획 문서화 (기획자용)**: 현재 적용된 캐릭터(시작 스킬/시작
      다이스/고유 스킬 등)와 특수 이벤트(대상/내러티브/보상 등)를 DESIGN.md에
      기술 — 사용자가 "기획자인 나에게 전달하는 용도"라고 명시. 코드 구현이
      아니라 문서화 작업이라 다른 항목들과 성격이 다름 — 위 항목들(특히
      캐릭터별 시작 주사위, 특수 이벤트 개편)이 실제로 바뀔 때마다 같이
      갱신하는 게 효율적일 수 있음.

## 완료 기록

- **2026-09-15 (93)**: 큐 16(INBOX.md 2026-09-14 대량 피드백)에서 "상점은 현재
  5개의 방 기준, 2번째 4번째 방에만 추가한다"를 처리. `dungeon_map.gd`의
  `_room_options_for_index(idx)`에서 상점 노출 판정을 `rng.randf() < SHOP_CHANCE`
  (60% 확률 노출)에서 `SHOP_FIXED_ROOM_INDICES.has(idx)`(신규 상수, =[1, 3])로
  교체 — idx는 0부터 시작하므로 "2번째/4번째 방"에만 상시 노출되고 나머지 방(1/3/
  5번째)에는 아예 안 뜨도록 고정. event/story 노출 판정(EVENT_CHANCE/STORY_CHANCE
  확률 굴림)은 그대로 유지, 라운드 마지막 방(5번째, idx=4)은 기존처럼 보스 전투만
  가능하도록 이미 막혀 있어(2026-09-14 (90) 수정) 이번 변경과 무관하게 그대로
  유지됨. `_roll_room_choices()`(실제 진행)와 `_make_map_node()`(MapStrip 미리보기)
  둘 다 `_room_options_for_index()` 하나를 공유하는 구조라 별도 분기 추가 없이
  MapStrip의 방 타입 칩에도 자동 반영됨. QA 검증을 위해 0-인자 디버그 훅
  `_debug_show_room2()`(rooms_cleared=1)/`_debug_show_room3()`(rooms_cleared=2)를
  신규 추가(기존 `_debug_show_boss_room()`과 같은 패턴). `qa_out/
  dungeon_map_room1_shopfix.png`(1번째 방, 상점 없음)/`qa_out/
  dungeon_map_room2_shop.png`(2번째 방, "상점 입장 (2번째 방)" 버튼 노출)/
  `qa_out/dungeon_map_room3_noshop.png`(3번째 방, 상점 버튼 없음 — 전투/특수
  이벤트/스토리 이벤트만)로 3개 상태 모두 겹침/크래시 없이 의도대로 표시됨을
  확인. `dice_test.gd`의 `_check_dungeon_map_room_options`(결정성 + order가 항상
  shop/event/story 순열인지 검증)는 노출 여부 값 자체가 아니라 "같은 idx는 항상
  같은 결과"와 "order 배열 구성"만 확인하는 구조라 고정 노출로 바뀌어도 영향
  없이 회귀 스위트 전체 PASS. INBOX.md "처리됨"으로 이동. 남은 것은 상점을
  2/4번째 방에 고정한 것이 실제로 밸런스상 적당한 타이밍인지 사람 플레이
  피드백뿐.
- **2026-09-15 (92)**: 큐 16(INBOX.md 2026-09-14 대량 피드백)에서 서로 독립적인
  작은 항목 2개를 처리. **(1) "던전 맵에서 각 보상 아이콘이 무엇을 의미하는지
  아이콘 텍스트 또는 범례가 필요할 것 같다"**: `dungeon_map.gd`에 `_setup_reward_
  legend()`를 신규 추가 — `_ready()`에서 한 번만 호출해, 골드 라벨(`GoldLabel`,
  y=112~142) 오른쪽 빈 공간(x=560)에 `RewardIcon`(골드/눈금/다이스 3종) + 텍스트
  라벨("골드"/"눈금"/"다이스") 쌍을 HBoxContainer로 한 줄에 고정 표시한다. 방마다
  달라지는 정보가 아니라 "아이콘 자체가 뜻하는 카테고리"라 정적으로 한 번 그리면
  충분하고 이후 갱신이 필요 없다. **(2) "상점 입장 버튼 하단에 보유 골드량을
  표시해준다. 해당 선택지가 의미가 없음을 보여줘야 한다"**: `_shop_gold_label`
  (신규 Label)을 `_setup_shop_gold_label()`에서 만들고, `_update_labels()`가
  매번 `RunState.gold` 기준으로 텍스트/색을 갱신한다 — `shop.gd`의 `ITEM_COSTS`
  최저가(`boost_weak_face`=10골드)를 `MIN_SHOP_ITEM_COST` 상수로 복제해, 그
  가격도 못 살 만큼 골드가 부족하면 "보유 골드: N (구매 가능한 품목 없음)" 문구를
  붉은색(0.8,0.4,0.4)으로, 아니면 평소 회색으로 "보유 골드: N"만 보여준다(shop.gd
  의 가격표가 바뀌면 이 상수도 같이 맞춰야 하는 잠정 매핑 — 다른 재질/카운터
  잠정 매핑들과 같은 이유로 class_name 없이 각자 씬에 귀속돼 정적 참조 대신
  값만 복제함). 상점 버튼은 방마다 노출 여부/순서가 바뀌어 위치가 고정이 아니므로
  `_layout_visible_buttons()`에서 다른 버튼/보상 아이콘 줄과 함께 매번 위치를
  다시 계산 — "상점" 타입일 때만 라벨을 보이게 하고 버튼 바로 아래(offset_bottom+2,
  높이 18)에 배치하며, 다음 버튼과의 간격을 `SHOP_GOLD_LABEL_GAP`(22px)만큼
  늘려 겹치지 않게 했다. 최악의 경우(버튼 5개: 전투/상점/특수 이벤트/스토리
  이벤트/커스터마이징 + 상점 라벨까지 전부 표시)도 계산상 마지막 버튼 하단이
  y=672로 화면 높이 720 안에 들어옴을 확인. `qa_out/dungeon_map.png`(방 1,
  골드 0 → 범례 정상 표시 + 상점 라벨 경고 문구), `qa_out/dungeon_map_room2.png`
  (방 3, 버튼 4개 + 상점 라벨이 event 버튼과 customize 버튼 사이에 겹침 없이
  배치), `qa_out/dungeon_map_boss_room.png`(방 5=보스 방, 상점 버튼이 없어
  라벨도 함께 숨겨짐 — 잔상 없음)로 3개 상태 모두 화면에서 확인. `dice_test.gd`
  회귀 스위트는 씬 트리 없이 순수 함수만 호출하는 구조라 이번 UI 전용 변경의
  영향을 받지 않음(전체 PASS 재확인). INBOX.md 두 항목 모두 "처리됨"으로 이동,
  12개 초과분(가장 오래된 2개, 밸런스 수치 보류/덱 토글 확정 답변)은
  `docs/INBOX_ARCHIVE.md`로 이관. 남은 것: 범례/경고 문구가 실제로 "인지"에
  도움이 되는지는 사람 플레이 피드백 영역.
- **2026-09-14 (91)**: INBOX.md 2026-09-14 대량 피드백 중 "던전 맵 1/3 라운드,
  방 0/5 상태에서 커스터마이징 팝업을 띄웠는데, 전투 방 입장 보상 아이콘이
  팝업 창을 뚫고 나온다"를 처리(큐 16 "커스터마이징 팝업 겹침 버그" 항목).
  **원인**: `dungeon_map.tscn`에서 `CustomizePanel`은 씬 파일에 정의된 마지막
  자식 노드지만, `dungeon_map.gd`의 `_ready()`가 `_setup_map_strip()` →
  `_setup_reward_icons()` → ... 순서로 실행되면서 `_setup_reward_icons()`가
  방 선택 버튼 옆에 붙는 보상 아이콘 줄(HBoxContainer)을 `add_child(row)`로
  런타임에 추가한다 — 이 호출이 `CustomizePanel`보다 늦게 실행되므로, 그
  아이콘 줄들이 씬 트리에서 `CustomizePanel`보다 뒤(= 나중에 그려짐 = 위)에
  위치하게 된다. Godot의 CanvasItem은 형제 노드 중 트리상 나중에 오는 쪽이
  나중에 그려져 위에 보이므로, 팝업이 열려 있어도 그 뒤에 추가된 보상
  아이콘들이 팝업 배경(90% 불투명 검정) 위에 그대로 떠서 "팝업을 뚫고
  나온" 것처럼 보였다. **수정**: `code/scenes/customize_panel.gd`의
  `open()` 시작 부분에 `move_to_front()` 한 줄을 추가 — 패널이 열릴 때마다
  자신을 부모의 마지막 자식으로 옮겨(= 항상 최상단에 그려지도록) 강제한다.
  이 패널은 `dungeon_map`/`shop`/`event`/`story_event`/`combat_test` 5개
  화면에서 공용으로 쓰이는 단일 스크립트라, 어느 화면에서 이후에 어떤 형제
  노드를 추가하든(지금의 보상 아이콘뿐 아니라 앞으로 생길 다른 오버레이
  요소도) 이 한 줄로 함께 방지된다. 로직 변경이 없는 순수 렌더 순서 수정이라
  `dice_test.gd` 회귀 스위트는 대상 밖(스크린샷으로만 검증). 수정 전/후를
  직접 대조해 확인: `scripts/qa_shot.sh dungeon_map 30 <out>
  _debug_open_customize`로 같은 상태를 두 번 찍어(`git stash`로 수정 전 임시
  복원 후 1회, 복원 취소 후 1회) 비교 — 수정 전 스크린샷에는 "전투 방 입장"
  버튼 줄 오른쪽에 금화/다이스/눈금 아이콘 3개가 팝업 위에 원래 밝기 그대로
  떠 있었고, 수정 후에는 같은 위치의 버튼/아이콘이 팝업의 90% 불투명 배경에
  가려 희미하게만 비침(다른 UI 요소와 같은 정상적인 가림 상태)을 확인했다.
  INBOX.md는 "처리됨"으로 이동.
- **2026-09-14 (90)**: docs/feedback/INBOX.md에 2026-09-14일자로 대량(약 30개)의
  새 플레이테스트 피드백이 추가됨 — 그중 "방 다섯개 클리어 하고, 새 런으로
  돌게 되는데, 라운드가 진행되어야 한다" 항목을 실제 버그일 가능성이 있다고
  보고 코드부터 확인, 실제 버그로 확인해 수정. **원인**: 라운드의 마지막 방
  (`RunState.rooms_cleared == TOTAL_ROOMS - 1`, 지금은 4)의 몬스터만
  `combat_test.gd`의 `_monster_config_for_room()`이 "보스"로 강화하고,
  보스를 물리쳐야만(`_apply_room_advance()`) `RunState.advance_round()`가 불려
  라운드가 넘어가도록 [대형 기획 2]가 배선돼 있었다. 그런데 이 마지막 방에도
  다른 방과 똑같이 상점/특수 이벤트/스토리 이벤트가 확률적으로 노출될 수
  있었고, `shop.gd`/`event.gd`/`story_event.gd`는 전투와 무관하게 각자
  `RunState.rooms_cleared += 1`만 할 뿐 보스/`advance_round()`를 전혀 모른다 —
  그래서 플레이어가 마지막 방에서 전투 대신 상점/이벤트/스토리를 고르면 보스를
  만나지도 않은 채 `rooms_cleared`가 `TOTAL_ROOMS`에 도달해버려(`round_index`는
  1에 그대로 머문 채) "런 클리어" 상태가 되고, `dungeon_map.gd`의 "새 런 시작"
  버튼이 `RunState.reset_run()`을 불러 라운드 진행 없이 처음부터 다시 시작하는
  것처럼 보였다(사용자가 보고한 정확한 증상과 일치). **수정**: `dungeon_map.gd`의
  `_room_options_for_index(idx)`에 `idx == RunState.TOTAL_ROOMS - 1`이면
  shop/event/story를 전부 `false`(순서 목록도 빈 배열)로 즉시 반환하는 분기를
  추가 — 라운드의 마지막 방은 이제 "전투" 선택지만 노출되어(보스를 반드시
  거쳐야만 방을 통과할 수 있음) 이 경로 자체가 막힌다. `_roll_room_choices()`
  (실제 진행)와 `_make_map_node()`(MapStrip 미리보기) 둘 다 이 함수 하나를
  그대로 쓰는 구조라 별도 분기 추가 없이 미리보기에도 자동 반영됨. QA 전용
  `_debug_show_boss_room()`(`rooms_cleared = TOTAL_ROOMS - 1`로 맞추는 0-인자
  래퍼, `_debug_show_round2()`와 같은 패턴) 훅을 신규 추가. `dice_test.gd`의
  기존 `_check_dungeon_map_room_options`(idx 0..9에서 order가 항상 shop/event/
  story 순열이어야 한다는 검증)가 idx=4(보스 방)에서 깨질 것을 발견해, 이
  idx만 "3종 전부 미노출 + order 빈 배열"을 기대하는 예외로 갱신(다른 idx는
  기존 검증 그대로 유지) — 회귀 스위트 전체 PASS. `qa_out/
  dungeon_map_boss_room_combat_only.png`(`_debug_show_boss_room()` 경로,
  5번째 방에 "전투" 선택지만 보이고 겹침 없음)와 `qa_out/
  dungeon_map_room1_smoke_after_fix.png`(1번째 방은 기존처럼 상점/특수
  이벤트/스토리 이벤트가 그대로 확률 노출되는 회귀 없음, MapStrip에서도
  5번째 방 칩만 "전투" 하나로 줄어든 것을 확인)로 실제 화면 검증. **참고**:
  이미 진행 중이던 런에서 이 버그로 라운드가 안 넘어간 채 리셋된 세이브는
  되돌릴 방법이 없음(설계상 `RunState`는 영속 저장이 아니라 세션 메모리) —
  이번 수정 이후 새로 시작하는 런부터 정상 동작. 이번에 반영 못 한 대량의
  나머지 2026-09-14 INBOX 항목(키매핑/단축키, 캐릭터 선택 UX 개편, 보상 등급
  S/A/B/C, 커스터마이징 UX 개편, 특수 이벤트 스토리+리스크/리턴 개편 등)은
  각각 별도 이터레이션이 필요한 크기라 이번엔 손대지 않음 — 아래 "다음 할 일
  큐" 16번 참고.
- **2026-09-09 (83)**: 큐 15([대형 기획 2] 던전 마지막 보스 + 3라운드 구조)의
  가장 작은 착수 조각(라운드 구조 없이 마지막 방 몬스터 스탯만 강화)을 진행.
  `combat_test.gd`의 `_monster_config_for_room()`에 `is_boss :=
  room_index == RunState.TOTAL_ROOMS - 1` 판정을 추가해, 해당 방(지금은 4)의
  공격 다이스 +2개/방어 다이스 +1개/최대 HP ×2(감으로 잡은 잠정 강화값)를
  적용하고 이름 끝에 "[보스]" 태그를 붙임(다른 다이스 기믹 태그와 같은 표기
  패턴 — 5번째 방은 원래도 "다크 나이트"[극단] 기믹이 있어 최종 이름이 "다크
  나이트 [극단] [보스]"로 두 태그가 함께 표시됨, 서로 독립적으로 계산되어
  충돌 없음). `dice_test.gd`의 기존 room 0..6 스케일링 공식 검증 루프가 이
  보정을 반영 못 해 깨질 뻔한 것을 `idx == RunState.TOTAL_ROOMS - 1`일 때만
  기대값에 보정을 더하도록 수정하고, `is_boss`/이름 태그 자체를 확인하는 신규
  검증도 추가. 기존 min/max 기믹 검증의 room4 이름 기대값도 "다크 나이트
  [극단] [보스]"로 갱신. 회귀 스위트 전체 PASS(error/leak/orphan 없음, 업적
  정의 12개는 이번에 변경 없음). `qa_out/combat_test_boss_room4.png`
  (`GAME_QA_ROOM_OVERRIDE=4`, `GAME_QA_SETTLE=1`)로 실제 전투 화면에서 "다크
  나이트 [극단] [보스] HP: 44/44" 라벨과 방어 다이스 3개(1,8,8 — defense_count
  1+int(4/3)+1=3과 일치)가 겹침/벽 이탈 없이 표시됨을 확인,
  `qa_out/dungeon_map_boss_smoke.png`로 던전 맵도 크래시 없이 로드됨을 확인.
  이번 조각은 "보스가 세다"까지만 — 라운드 반복(보스 처치 후 새 던전으로
  재진입)과 "3라운드 클리어 = 최종 클리어" 엔딩은 다음 조각으로 남김(아래
  "다음 할 일 큐" 15번 참고). 강화폭(+2/+1/×2)이 적당한지는 사람 플레이
  피드백 필요.
- **2026-09-09 (84)**: 큐 14([대형 기획 1] 플레이어블 캐릭터 4종 추가, 총 5종 중
  선택)의 가장 작은 착수 조각을 진행 — 캐릭터 선택 UI를 카드 1개("확인")에서
  여러 카드 중 하나를 고르는 형태로 확장하고, 신규 캐릭터 2종(기존 "견습
  모험가" 포함 총 3종)에 몬스터 다이스 기믹과 같은 API를 재사용한 시작 기믹을
  부여. 신규 `code/systems/character_profiles.gd`(`CharacterProfiles`)에
  프로필 배열(id/name/desc/gimmick/hair_color/dress_color)을 정의 —
  "광전사"(극단형, `gimmick: min_max_only` — `DiceBag.force_min_max_faces()`를
  공격+방어 주머니 둘 다에 적용, 몬스터 "다크 나이트"와 같은 함수 재사용),
  "수호자"(안정형, `gimmick: fixed_defense_die` — INBOX.md 예시 문구가 "다이스
  하나"라고 명시해 몬스터 "오크"의 주머니 전체 고정과 구분되도록
  `DiceBag`에 신규 `force_fixed_value_for_die(die_index, value)`를 추가해 방어
  다이스 0번째 하나만 고정, 나머지는 표준 유지). `code/systems/run_state.gd`에
  `character_id`(기본값 "novice")를 추가하고 `reset_run(new_character_id: String
  = "")`으로 시그니처 변경 — 인자를 안 주면(기존 3개 호출부: 패배 후 재시작 등)
  현재 character_id를 그대로 유지한 채 새 주머니에 기믹을 재적용해 "캐릭터
  선택 화면을 다시 거치지 않아도 고른 캐릭터가 유지"되게 함.
  `code/scenes/character_portrait_placeholder.gd`의 머리/원피스 색을
  `HAIR_COLOR`/`DRESS_COLOR` 상수에서 `@export var hair_color`/`dress_color`로
  바꿔(+`set_palette()`) 같은 실루엣 그리기 코드를 캐릭터별 색 구분에 재사용.
  `code/scenes/character_select.gd`/`.tscn`을 단일 `CardPanel`에서
  `dungeon_map.gd`의 `_build_map_strip()`과 같은 "빈 컨테이너 + 코드로 동적
  생성" 패턴으로 재작성 — `CharacterProfiles.PROFILES`를 순회해 카드를 만들고,
  클릭한 카드는 업적 패널의 "해금" 카드와 같은 금테로 강조(`_refresh_selection_
  highlight()`). `dice_test.gd`에 신규 `_check_character_profiles`(get_profile
  폴백 2종, reset_run(berserker)의 공격+방어 다이스 전체가 1/4만 나오는지,
  reset_run(guardian)의 방어 다이스 0번째만 고정값(3)이고 1/2번째는 표준
  유지+공격 다이스는 무관한지, 인자 없는 reset_run()이 직전 캐릭터를 유지하며
  기믹을 재적용하는지) 추가로 회귀 스위트 전체 PASS(신규 캐릭터 검증 6개 포함).
  `qa_out/character_select_cards.png`(카드 3개 겹침 없이 표시, 기본 선택 "견습
  모험가"), `qa_out/character_select_berserker_selected.png`(카드 클릭 시
  금테가 이동), `qa_out/dungeon_map_berserker_deck.png`·`dungeon_map_guardian_
  deck.png`(선택 → 던전 시작 → 실제 덱 패널까지 이어지는 전체 경로로, 광전사는
  공격+방어 둘 다 1/1/4/4만, 수호자는 방어 다이스 하나만 3/3/3/3이고 나머지는
  1/2/3/4 그대로임을 화면에서 확인)로 4개 화면 검증,
  `qa_out/combat_test_smoke_after_char.png`로 기존 combat_test/dungeon_map
  화면도 회귀 없음을 확인. 캐릭터 5종 중 3종만 구현 — 나머지 2종(INBOX.md가
  예시로 든 "폭발형"은 몬스터 "고블린"의 분노 스택처럼 매 턴 상태 추적과 턴
  로직 자체를 바꿔야 해서 더 큰 작업이라 이번엔 제외, 그리고 다섯 번째 컨셉
  1종)은 아래 "다음 할 일 큐" 14번에 남김. 부수적으로 QA 훅 사용법 관련 위험을
  하나 발견 — 아래 "알려진 이슈" 참고(`GAME_QA_CALL`에 인자가 필요한 메서드를
  넘기면 창이 멈출 수 있음, 인자 없는 래퍼로 우회).
- **2026-09-09 (85)**: 큐 14([대형 기획 1] 플레이어블 캐릭터)의 남은 조각 중
  "폭발형"을 구현해 4/5종 완료. 몬스터 "고블린"의 anger_stack 기믹(공격 다이스가
  자기 최댓값 면을 보여줄 때마다 스택이 쌓이고, `ANGER_STACK_THRESHOLD`(3)에서
  다음 공격 한 턴만 1D20으로 굴림)과 완전히 같은 구조를 플레이어 공격턴에
  적용 — `character_profiles.gd`에 캐릭터 "폭발병"(`gimmick: "explosive_stack"`,
  주황 계열 팔레트)을 추가하고, `combat_test.gd`에 `player_dice_gimmick`
  (`_ready()`에서 `CharacterProfiles.get_profile(RunState.character_id)`로 읽음)/
  `player_explosive_stacks`/`player_explosive_pending`(`EXPLOSIVE_STACK_
  THRESHOLD`=3, `EXPLOSIVE_DICE_SIDES`=20, 몬스터 상태와 같은 상수값이지만
  독립적으로 정의) 전투 중 상태를 신설했다. `_do_exchange()`의 atk_bag 선택
  분기와, 몬스터 anger_stack 집계 블록 바로 아래에 병렬 구조의 플레이어
  집계 블록(`is_player_attacking and player_dice_gimmick == "explosive_stack"`)을
  추가 — `used_explosive_dice`로 폭발 굴림 자체가 다시 스택을 쌓지 않도록
  구분하는 것까지 anger_stack과 동일. 정적 다이스 개조가 아니므로
  `run_state.gd._apply_character_gimmick()`의 match 문에는 걸리지 않고
  `_: pass`로 통과함을 주석으로 명시(character_profiles.gd 클래스 주석도
  갱신). 작업 중 QA 스크린샷(`character_select_4cards.png`)에서 카드가 4개로
  늘자 고정 `CARD_WIDTH=360`(3개 기준 계산)이 총 폭 1500px를 만들어 화면
  (1280px) 오른쪽이 잘려나가는 회귀를 발견 — `character_select.gd`를 고쳐
  `CARD_WIDTH_MAX`(360, 상한)와 `ROW_MARGIN`(20, 좌우 여백)으로 카드 폭을
  `min(CARD_WIDTH_MAX, (1280 - ROW_MARGIN*2 - (count-1)*GAP) / count)`로
  동적 계산하도록 바꿔, 캐릭터가 5종이 돼도(다음 조각) 화면 밖으로 안 잘리게
  했다(`_make_card()`의 모든 `CARD_WIDTH` 참조를 매개변수 `card_width`로 교체).
  `dice_test.gd`의 `_check_character_profiles`에 신규 검증 2개(get_profile
  (explosive).gimmick == explosive_stack, reset_run(explosive) 후 공격/방어
  주머니가 표준 D4x3 그대로인지 — explosive_stack이 정적 개조를 안 하는 것의
  방증) 추가로 회귀 스위트 전체 PASS. `qa_out/character_select_4cards_fixed.png`
  (카드 4개 겹침/잘림 없이 표시)/`combat_test_explosive_dice.png`(
  `_debug_show_explosive_dice()`로 폭발 스택 임계치 도달 상태를 만들어 D20
  다이스와 "폭발 직전! 다음 공격은 20면체 주사위로 터진다" 로그 문구가 겹침
  없이 표시됨을 확인)/`dungeon_map_explosive_deck.png`(선택 → 던전 시작 →
  실제 덱 패널까지 전체 경로, 정적 개조가 없으므로 공격/방어 다이스가
  표준 1/2/3/4 그대로임을 확인 — "덱 패널에는 안 보이고 전투 로그에서만
  드러나는 기믹"이라는 설계가 실제로 그렇게 보임)/
  `combat_test_smoke_after_explosive.png`(기존 화면 회귀 없음)로 검증.
  남은 것: 다섯 번째 캐릭터 컨셉/기믹은 여전히 미정(아래 "다음 할 일 큐" 14번),
  4종 전체의 강화폭/체감 밸런스는 사람 플레이 피드백 필요.
- **2026-09-09 (86)**: 큐 14([대형 기획 1] 플레이어블 캐릭터)의 마지막 남은
  조각 — 다섯 번째 캐릭터 컨셉을 AI가 제안해 구현, **5/5종 완료**. "폭발병"
  (explosive_stack, 공격 다이스 최댓값 스택 → 3스택에서 다음 공격 1D20)과
  대칭 구조로 "방패병"(`gimmick: "guard_stack"`)을 추가 — 방어 다이스가
  최댓값 면을 3번 보여주면 다음 방어 한 턴만 1D20으로 굴려 굳건해짐.
  `character_profiles.gd` PROFILES에 항목 추가, `combat_test.gd`에
  `player_guard_stacks`/`player_guard_pending`/`GUARD_STACK_THRESHOLD`(3)/
  `GUARD_DICE_SIDES`(20) 신설 — `_do_exchange()`의 def_bag 선택을 기존 단순
  삼항식(`monster_defense_bag if is_player_attacking else RunState.
  player_defense_bag`)에서 if/elif/else로 확장해 guard_pending일 때 1D20
  임시 주머니를 쓰도록 하고, 몬스터 방어턴(플레이어 방어턴) 다음에
  explosive_stack 블록과 나란한 스택 집계 블록을 추가. `run_state.gd`
  주석 갱신(guard_stack도 정적 개조 없이 match의 `_: pass`로 통과),
  `character_select.gd`에 `_debug_select_shieldbearer`/`_debug_start_run_
  as_shieldbearer`, `combat_test.gd`에 `_debug_show_guard_dice()` QA 래퍼
  추가(모두 기존 explosive 계열 래퍼와 같은 패턴). `dice_test.gd`에 신규
  검증 2개(get_profile(shieldbearer).gimmick, reset_run(shieldbearer)이
  다이스 주머니를 표준 그대로 유지하는지) 추가로 회귀 스위트 전체 PASS.
  `qa_out/character_select_5cards.png`(카드 5개 겹침/잘림 없이 표시 — 기존
  (85)에서 고친 동적 카드 폭 계산이 5종에서도 실제로 작동함을 확인)/
  `combat_test_guard_dice.png`(수호 스택 임계치 도달 → D20 다이스 + "수호
  태세 완성!" 로그 문구, 겹침 없음)/`dungeon_map_shieldbearer_deck.png`
  (선택 → 시작 → 덱 패널까지 전체 경로, 정적 개조가 없으므로 공격/방어
  다이스가 표준 1/2/3/4 그대로임을 확인)/
  `combat_test_smoke_after_shieldbearer.png`(정지 감지 경로로 기존 화면
  회귀 없음)로 검증. 작업 중 `character_profiles.gd`의 PROFILES 배열에
  새 항목을 추가하는 Edit 시도가 두 번 연속 "String to replace not
  found"로 실패했는데, 원인은 Read 도구 출력을 손으로 옮겨적으며 탭 들여쓰기
  개수를 실제보다 한 칸 더 넣은 것이었다 — `od -c`로 원본 바이트를 직접
  확인한 뒤에야 정확한 탭 수(객체 키 2탭, 닫는 `},`/여는 `{` 1탭, 배열을
  닫는 `]` 0탭)를 알 수 있었다. 앞으로 Edit이 반복 실패하면 Read 표시를
  더 정확히 옮겨적으려 하기보다 Bash(`od -c`)로 원본 바이트를 바로 확인할
  것. [대형 기획 1]은 이제 완전히 끝났고(INBOX.md 원 요청 전부 처리), 남은
  것은 5종 전체의 강화폭/체감 밸런스에 대한 사람 플레이 피드백뿐.
- **2026-09-09 (87)**: 큐 15([대형 기획 2] 던전 마지막 보스 + 3라운드 구조)의
  남은 조각 (a)/(b)/(c) 중 (a)만 진행 — `code/systems/run_state.gd`에
  `TOTAL_ROUNDS`(=3, 잠정값)/`round_index`(1부터 시작)/`is_last_round()`/
  `advance_round()`를 신규 추가. `reset_run()`은 항상 `round_index`를 1로
  되돌리고, `advance_round()`는 `round_index`만 올리고 `rooms_cleared`를 0으로
  되돌리며 골드/다이스 주머니/눈금·다이스 인벤토리는 그대로 유지한다(라운드가
  바뀌어도 지금까지 쌓은 빌드가 이어지고, 죽거나 3라운드를 전부 클리어했을
  때만 `reset_run()`으로 처음부터 다시 시작한다는 설계 — 클래스 주석에 근거
  기록). 마지막 라운드에서 `advance_round()`를 불러도 아무 일도 하지 않도록
  가드. **의도적으로 이번엔 여기까지만** — 아직 `combat_test.gd`/
  `dungeon_map.gd` 어디도 이 필드/메서드를 호출하지 않아 실제 라운드 진행은
  동작하지 않는다(다음 조각 (b)가 배선). `code/scenes/dice_test.gd`에
  `_check_round_progress()` 신규 검증(reset_run이 round_index를 1로 되돌리는지,
  advance_round가 round_index/rooms_cleared/골드/다이스 주머니를 각각 올바르게
  다루는지, is_last_round 참/거짓 양쪽, 마지막 라운드에서 advance_round가
  아무 효과 없는지) 추가로 회귀 스위트 전체 PASS. 화면 배선이 없어 새로 보여줄
  것은 없지만, `qa_out/dungeon_map_round_smoke.png`/`combat_test_round_smoke.png`
  로 두 핵심 화면이 이번 변경으로 깨지지 않고 그대로 로드/정지됨을 확인(회귀
  없음 확인이 목적). 다음 조각(b)은 위 "다음 할 일 큐" 15번 참고.
- **2026-09-10 (88)**: 큐 15([대형 기획 2] 던전 마지막 보스 + 3라운드 구조)의
  남은 조각 (b) "보스 처치 시 새 던전 맵(다음 라운드)으로 재진입" 진행. 이번
  세션 시작 시 `code/scenes/combat_test.gd`에 이전 세션이 커밋 없이 남겨둔
  `monster_is_boss` 필드(할당만 되고 아무 데서도 쓰이지 않는 상태)가 있어 이를
  이어받았다. `_apply_room_advance()`가 승리 시 `RunState.rooms_cleared += 1`
  직후 "이번 방이 보스 방이었고(`monster_is_boss`) 아직 마지막 라운드가
  아니면(`not RunState.is_last_round()`)" `RunState.advance_round()`를 호출하도록
  분기 추가. `advance_round()` 자체가 `rooms_cleared`를 0으로 되돌리므로, 당초
  이전 세션이 계획해뒀던 "dungeon_map.gd에 '라운드 클리어! 다음 라운드로' 확인
  화면을 새로 만든다"는 방향은 불필요해짐을 확인 — 승리 처리 시점에 이미
  라운드가 넘어가 있는 상태로 dungeon_map.tscn에 진입하므로, 그 화면은 그냥
  "새 라운드의 1번째 방"으로 자연스럽게 보인다(방 목록도 `rooms_cleared` 기반
  결정적 RNG라 자동으로 새로 그려짐). `dungeon_map.gd`의 `is_run_complete()`
  분기 로직(`_on_combat_button_pressed()`/`_update_labels()`)은 전혀 손대지
  않음 — 마지막 라운드 보스를 잡으면 `advance_round()`가 스스로 가드로 아무
  일도 안 해 `rooms_cleared`가 `TOTAL_ROOMS`에 남으므로 기존 "던전 클리어!"
  분기가 그대로 최종 클리어 화면으로 맞물린다. 다만 라운드가 넘어가면 "클리어한
  방"이 5->0으로 줄어드는 이유를 알 수 있도록 `rooms_cleared_label`에 "라운드
  N/3 · " 접두사를 추가(정상 진행/최종 클리어 분기 둘 다). `code/scenes/
  dice_test.gd`에 `_check_combat_boss_round_advance()` 신규 검증 3케이스(일반
  방 승리는 `round_index` 불변·`rooms_cleared`만 증가, 보스 방 승리+마지막
  라운드 아님은 `round_index+1`·`rooms_cleared=0`, 보스 방 승리+마지막
  라운드는 `advance_round()`의 자체 가드로 변화 없음) 추가로 회귀 스위트 전체
  PASS. `dungeon_map.gd`에 QA 전용 `_debug_show_round2()` 훅을 추가해 `qa_out/
  dungeon_map_round2.png`(라운드 2/3 · 클리어한 방: 0/5, 방 목록/공격·방어
  주머니 패널과 겹침 없이 표시)로 실제 화면을 확인, `qa_out/dungeon_map_
  round1_smoke.png`(라운드 1/3 기본 상태)와 `combat_test_round_smoke2.png`로
  이번 변경이 두 핵심 화면을 깨뜨리지 않았음도 함께 확인. 남은 조각은 (c)
  "3라운드 전부 클리어 = 최종 클리어" 전용 화면/문구 하나뿐 — 위 "다음 할 일
  큐" 15번 참고.
*(이터레이션 89 이전의 더 오래된 완료 기록은 `docs/STATUS_ARCHIVE.md`에
보관돼 있음 — 이 파일에는 최근 10개만 유지해 매 이터레이션 읽기 비용을 줄임,
2026-09-15 정리.)*

## 알려진 이슈 / 막힌 것

- **`scripts/qa_shot.sh`의 `GAME_QA_CALL`(4번째 인자)에 인자가 필요한 메서드
  이름을 넘기면 창이 멈춰서 프로세스가 안 끝날 수 있음(2026-09-09 (84) 실제로
  겪음).** `code/qa/visual_qa.gd`가 `scene.call(_qa_call)`을 인자 없이 호출하는데,
  대상 메서드가 필수 매개변수를 요구하면(예: `_debug_select_character(id:
  String)`) GDScript 런타임 오류가 나면서 창(실제 창, headless 아님)이 그대로
  멈춘 채 안 닫힘 — 프로세스를 강제 종료(`taskkill`/`kill`)하려 해도 이 세션
  권한 모드에서 사람 승인 없이는 실행이 막혀 있어 무인 세션에서는 회수할 방법이
  없다(좀비 Godot 창이 화면 밖에 하나 남을 수 있음, 사람이 다음에 PC를 보면 수동
  종료 필요). **교훈: `GAME_QA_CALL`로 넘길 디버그 훅은 항상 인자 없는(0-arity)
  래퍼로 만들 것** — 이미 있는 `_debug_start_run_as_berserker()` 같은 패턴처럼,
  인자가 필요한 동작은 값마다 별도의 인자 없는 래퍼 메서드를 새로 만들어서 넘길
  것(`code/scenes/character_select.gd`의 `_debug_select_berserker()`/
  `_debug_select_guardian()` 참고). 앞으로 새 QA 훅을 만들 때 이 패턴을 반드시
  지킬 것 — 재발 방지.
- **다음 진행을 위해 사람의 플레이테스트/피드백 또는 설계 결정이 필요한 항목이
  남아있음(단, 전부 완전히 막힌 것은 아님).** "성장의 재미가 없다"(2026-09-09)의
  방향 1(다이스 개수 캡+교체) 첫 조각은 2026-09-09 (76)에 구현했지만, 캡 수치(6)와
  "교체로 성장한다"는 감각이 실제로 체감되는지는 사람이 5방 런에서 캡까지
  도달해가며 플레이해봐야 판단 가능(위 "다음 할 일 큐" 0-A 참고). 방향 2(캐릭터별
  쿨다운 스킬)는 캐릭터가 5종으로 늘었지만(2026-09-09 (86)) 스킬 콘텐츠 자체를
  새로 기획해야 하는 더 큰 작업이라 여전히 미착수. 2026-09-01 몬스터별 성격 디자인은 순수하게 사람 기획이
  먼저 나와야 하는 항목(AI가 대신 만들 수 없음) — 이게 나와야 몬스터 다이스
  기믹(큐 9)을 어떤 몬스터에 배정할지도 근거를 갖고 결정 가능. 다음 할 일 큐에
  남은 다른 항목들도 대부분 "밸런스/체감이 적당한지 사람이 실제로 플레이해봐야
  판단 가능한 영역"으로 수렴한 상태(예: 몬스터 난이도 곡선, 상점 가격, 재질
  합성음, 카메라 줌/다이스 크기). 사람이 직접 플레이해보고 INBOX 항목에 구체적인
  설명을 추가하거나, 다음 할 일 큐의 밸런스 항목들에 피드백을 남겨주는 것이 다음
  단계로 나아갈 유일한 경로임.
- **`.claude/settings.json`(과 `settings.local.json`) 쓰기가 이 세션에서 항상
  "승인 필요"로 거부됨** — `Write`/`Edit`가 `permissions.allow`에 일반 허용되어
  있어도 이 두 설정 파일만은 예외적으로 매번 실제 승인을 요구하는 것으로 보임(자기
  자신에게 권한을 부여하는 것을 막는 안전장치로 추정). 사람이 없는 무인 세션에서는
  승인을 받을 방법이 없어 이 파일들을 못 고침. 그 결과 `scripts/qa_shot.sh`를
  `Util/` 밑으로 완전히 옮기지 못하고 루트에 예외로 남겨둠(위 "완료 기록"의 "프로젝트
  폴더 재구성" 참고). **2026-09-09 (27, 31, 35, 37) 총 4개의 서로 다른 세션에서
  동일하게 재현되어 일시적 문제가 아니라 구조적 제약으로 확정 — 앞으로는 이 항목을
  더 이상 재시도하지 말 것.** 사람이 직접 `.claude/settings.json`의
  `"Bash(scripts/qa_shot.sh *)"`를 `"Bash(Util/scripts/qa_shot.sh *)"`로 바꿔주면,
  다음 세션에서 `scripts/qa_shot.sh`를 `Util/scripts/qa_shot.sh`로 옮기고 루트에서
  완전히 제거할 수 있음.
- **`GAME_QA_FRAME`(렌더 프레임 카운트)과 실제 물리/게임 진행 시간이 실행마다 다르게
  디커플링될 수 있음** → **완화됨** (2026-09-03 (9), 위 "완료 기록" 참고).
  `GAME_QA_SETTLE=1`(`scripts/qa_shot.sh`의 5번째 인자)을 켜면 `combat_test.gd`
  같이 `_qa_is_settled()`를 구현한 씬에서는 다이스가 실제로 멈춘 뒤에 캡처하므로
  "다이스가 한창 구르는 중간 순간을 찍는" 문제는 없앨 수 있다. 다만 "전투가 몇 번째
  교환까지 진행됐는가"(프레임 수 대비 실제 진행된 교환 횟수)는 여전히 실행마다
  달라질 수 있음 — 이건 프레임 카운트 자체의 한계라기보다 "그 시점에 정확히 어떤
  상태를 보고 싶은지"에 달린 문제라, 특정 교환 시점을 정확히 잡고 싶으면 여전히
  스크린샷을 열어 확인하거나 `GAME_QA_CALL` 디버그 훅으로 상태를 직접 확인하는 게
  안전함. 아래 원본 서술은 발견 당시 기록 그대로 보존:
  `_debug_show_mixed_dice_shapes`를
  `GAME_QA_FRAME=30`으로 두 번 연속 실행했는데 둘 다 전투가 이미 "승리!"까지 끝난
  상태에서 디버그 콜이 실행됨 — 원래 이 헬퍼는 "스폰 직후 1프레임" 정도를 가정하고
  만들어졌던 것(코드 주석 참고)인데, 실제로는 30 프레임 시점에 이미 여러 번의
  교환(각 교환마다 최대 4초까지 걸리는 정지 감지 대기 포함)이 끝나 있었음. 같은
  씬을 `GAME_QA_FRAME=5`로 실행하면 아직 첫 교환이 진행 중인 상태로 잡히는 것으로
  보아, "프레임 번호"가 곧 "그 프레임 수만큼의 실시간"을 보장하지 않고(오프스크린
  창 렌더링 속도가 실행마다 달라 물리 틱 수 대비 렌더 프레임 수 비율이 변하는
  것으로 추정) 실행마다 달라지는 것으로 보임. QA용 디버그 헬퍼로 "전투 진행 중인
  특정 상태"를 잡고 싶다면 `GAME_QA_FRAME` 값만 믿지 말고 실제 스크린샷을 열어
  원하는 상태인지 확인할 것. 게임 로직 자체는 정지 감지로 실제 정지 후 진행하므로
  문제 없음 — 아래 항목과 같은 계열의 "타이밍은 매번 다르지만 로직은 안전함" 현상.
- 물리 시뮬레이션 특성상 다이스가 멈추는 시점이 매번 달라짐 → `GAME_QA_FRAME`을
  고정값으로 쓰면 다이스가 아직 구르는 중일 수 있음 (QA 스크린샷 자체는 여전히 고정
  프레임에 찍으므로, 어느 시점을 찍든 "게임 진행 중인 어느 한 순간"을 보게 될 뿐 —
  게임 로직 자체는 정지 감지로 실제 정지 후 진행하므로 문제 없음).
- ~~다이스가 14개 이상으로 한 주머니에 몰리면 정지 감지가 사실상 항상 4초 안전장치로
  빠짐~~ → **해결됨** (이번 이터레이션, 위 큐 2 참고). "전체 공용 카운터" 방식을
  "다이스별 독립 카운터" 방식으로 바꿔, 서로 맞닿은 다이스 중 하나가 접촉 잔진동으로
  순간 튀어도 이미 조용해진 다른 다이스들의 카운터까지 함께 리셋되지 않도록 함.
  14개(교환당 15개)로 재검증한 결과 2.7~3.4초 안에 자연 정지 감지됨(이전에는 항상
  4초). 남은 것은 다이스가 많을 때 서로 겹쳐 구르는 "과정"의 시각적 체감(사람 판단
  필요) 뿐.
- **물리 다이스의 착지 면과 실제 판정값이 무관함.** `combat_test.gd`는 화면에 굴러가는
  `DieD4`를 순수 연출로만 쓰고, 실제 공격/방어 합계는 `DiceBag.roll()`의 RNG로 별도
  계산한다 (물리 다이스가 어느 면으로 착지했는지 읽는 기능이 없음 — D4 정사면체는
  "위를 향한 면"이 모호해서 판독 자체가 까다로움). 플레이어가 보기에 "다이스 눈이랑
  로그의 숫자가 안 맞는다"고 느낄 수 있음. 사람이 실제로 플레이해보고 위화감이 있는지
  INBOX.md에 남겨줘야 판단 가능한 영역.
- ~~`res://scenes/dungeon.tscn`은 2D+3D 파이프라인 검증용 스파이크 씬으로 남아있고,
  `res://scenes/combat_test.tscn`이 새로 전투 전용 씬으로 분리됨. 던전 방 이동 등
  본게임 구조가 생기면 두 씬의 이름/역할을 정리해야 함.~~ → **해결됨** (이번
  이터레이션). 본게임 구조(캐릭터 선택 → 던전 맵 → 전투/상점/이벤트)가 이미 다 갖춰진
  뒤라 `code/scenes/dungeon.tscn`(2D+3D 파이프라인 검증이 끝난 뒤로는 아무 코드/씬도
  참조하지 않던 스파이크 씬)을 `git rm`으로 제거함. 아래 "완료 기록" 참고.
- ~~D4 충돌 사운드는 배선만 되어 있고 실제 재생되는 소리가 없음~~ → **부분
  해결됨** (이번 이터레이션, 위 큐 4 참고). `plastic.tres`의 `impact_sound`는
  여전히 비어 있지만, `code/dice/die_d4.gd`가 이제 실제 에셋이 없으면
  `code/dice/procedural_sound.gd`로 합성한 임시 타격음을 자동으로 대신 재생한다.
  남은 것: 이 합성음이 실제로 들어봤을 때 괜찮게 들리는지, 실제 녹음/제작 에셋으로
  교체할지는 여전히 사람이 실제로 들어봐야 판단 가능한 영역 (INBOX.md 피드백 필요).
  교체하려면 `res://resources/materials/plastic.tres`의 `impact_sound`만 채우면
  되고(그쪽이 항상 우선함) 코드 수정은 불필요.
- ~~전투가 끝나면(승/패) 화면이 그 상태로 멈춘 채 끝남~~ → 해결됨. 승/패 후
  `NextButton`으로 `dungeon_map.tscn`으로 돌아갈 수 있음 (위 완료 기록 참고).
- ~~`scripts/qa_shot.sh`는 `--headless`를 쓰지 않고 실제 창을 띄운다. `GAME_QA_FRAME`을
  크게 잡아(예: 1500) 창이 화면에 오래(수십 초) 떠 있으면, 그동안 마우스가 실제로
  클릭 가능한 버튼 위에 있을 경우 우발적으로 클릭되어 씬이 전환될 수 있음 (당시
  실제로 겪음 — 다만 결과적으로 버튼 동작 검증에는 도움이 됐음). QA 목적이라면
  필요한 최소 프레임만 쓰는 것이 안전.~~ → **사실상 해결된 것으로 보임** (2026-09-07
  (21), git 히스토리 교차 확인 — 재현 테스트로 직접 확인한 것은 아님). 이 항목은
  2026-09-01(`9321bfd`)에 기록됐는데, 바로 다음날 2026-09-02(`e1a7d35` "QA 캡처 시
  창을 화면 밖으로 이동해 사람 작업 방해 방지")에 `code/qa/visual_qa.gd`의
  `_ready()`가 QA 모드 진입 시 `get_window().position = Vector2i(-4000, -4000)`로
  창을 화면 밖 좌표로 옮기는 코드를 다른 목적(QA 창이 포그라운드로 튀어나와 사람의
  작업을 방해하지 않게)으로 추가했고, 그 뒤로 이 known issue와 다시 연결해 확인한
  적이 없어 계속 "미해결"로 남아있었음. 화면 밖 좌표로 이동한 창은 극단적인 다중
  모니터 배치가 아닌 한 실제 마우스가 물리적으로 그 위치에 있을 수 없으므로, 부수
  효과로 이미 해결됐을 가능성이 높음 — 다만 실제로 우발적 클릭을 재현해서 확인한
  것은 아니므로, 혹시 다시 이 문제를 겪으면 이 판단이 틀렸다는 뜻이니 이 취소선을
  되돌리고 다시 조사할 것.
- ~~던전 맵에는 아직 "전투 방" 하나만 있고 반복 재입장만 가능함~~ → 부분 해결됨.
  이제 "전투 방" / "상점" 2개 선택지가 있음. 비전투 선택지 이벤트, 3번째 이상의 방
  종류는 여전히 미구현 (다음 할 일 큐 1, 11번 참고).

---
*이 파일 갱신을 건너뛰면 다음 세션은 아무 기억 없이 처음부터 다시 파악해야 한다.
반드시 "무엇을 했고, 무엇이 남았고, 어디서 막혔는지"를 남길 것.*
