# STATUS.md — 인수인계 문서

> 매 이터레이션 시작 시 DESIGN.md, 이 파일, INBOX.md 순으로 읽는다.
> 매 이터레이션 종료 시 이 파일을 "다음 사람(다음 세션)"에게 인수인계하듯 갱신한다.

## 마지막 갱신

- 일시: 2026-09-09 (26)
- 작성자: AI 에이전트. 작업 트리는 세션 시작 시 이미 깨끗한 상태(직전 이터레이션 25가
  정상 커밋 완료)였음. INBOX.md 미처리 항목은 여전히 사람의 추가 기획이 필요한 2개
  (몬스터 성격/다이스 특이 특징)뿐이고, 다음 할 일 큐도 남은 항목이 전부 사람의
  밸런스/설계 피드백이 선행돼야 하는 상태라 코드 재감사를 하다가 `story_event.gd`의
  실제 표시 오류(골드가 부족해 손실이 클램프될 때 결과 문구가 클램프 전 요청값을
  그대로 보여주던 것)를 찾아 고침. 자세한 내용은 아래 "완료 기록" 참고.

## 지금 위치

- **한 바퀴 플레이 가능**: 캐릭터 선택 → 던전 맵(런 5방, 전투/상점/특수
  이벤트/스토리 이벤트 방 종류, 전투 제외 3종은 방마다 확률적으로 노출되고
  노출된 것끼리는 방마다 결정적으로 순서도 섞임) → 전투(다이스 굴려 공방, 승패 후
  복귀)까지 이어짐.
- **다이스 시스템**: `DiceBag`(주머니)+`CombatMath`, D4/D6/D8/D10/D12/D20
  지오메트리 전부 정확히 구현. 재질별(플라스틱/나무/유리/철제) 시각 색 구분.
  몬스터 다이스는 방 진행에 따라 D4→D6→D8로 스케일링.
- **커스터마이징**: 승리 시 얻는 "눈금 인벤토리"를 다이스 면과 교환하는
  3단계 방식(눈금 선택 → 다이스 선택 → 면 선택). 어느 화면에서든 오버레이로
  열 수 있음(`CustomizePanel`).
- **UI**: 공격/방어 덱 상시 표시 패널, 던전 맵 전체 미리보기(MapStrip), 전투
  다이스 결과를 이미지 칩으로 표시(+MAX 눈금 강조), 보상/상점/이벤트 아이템은
  카드형 비주얼(4종 아이템 전부 결과 다이스 면 미리보기 포함 — 확정적 결과는
  카드 자체에, 주머니 상태에 따라 달라지는 결과는 버튼 옆에, 이터레이션 23-24).
- **QA 도구**: 실제 창(화면 밖 좌표로 이동해 사람 작업 방해 안 함)으로 스크린샷,
  물리 정지 감지 옵션(`GAME_QA_SETTLE`), 마우스 클릭 시뮬레이션
  (`GAME_QA_CLICK_PATH`), 방 번호 강제 지정(`GAME_QA_ROOM_OVERRIDE`).
- **미착수(사람 기획 필요)**: 몬스터별 성격/특징 디자인, 다이스에 몬스터별
  특이 특징 부여 — 아래 "다음 할 일 큐" 참고.
- 상세 이력은 아래 "완료 기록"(최근 10개)과, 그보다 오래된 것은
  `docs/STATUS_ARCHIVE.md`(매 이터레이션 읽지 않는 아카이브) 참고.

## 다음 할 일 큐 (우선순위 순)

이 순서를 반드시 지킬 필요는 없지만, 앞 단계가 뒤 단계의 전제가 되므로 대체로 순서대로
진행하는 것을 권장한다. 한 이터레이션에 한두 개만 진행할 것.

0. **(INBOX.md 신규 2026-09-03) UI 접근성/가시성 4종 — 1/4 부분 착수.** 전부 "전투/
   선택지 화면 어디서든 정보가 상시 보이거나, 조작이 더 직관적이어야 한다"는 계열의
   요청이라 함께 묶어뒀지만, 각각 독립적으로 착수 가능:
   - **던전 맵 전체 보기(슬더스 스타일)** → **부분 완료** (이번 이터레이션, 아래
     "완료 기록" 참고). `dungeon_map.tscn`에 `MapStrip`을 추가해 런 전체(TOTAL_ROOMS,
     5방)를 한 줄로 보여줌 — 각 방의 선택지(전투/상점/특수 이벤트/스토리 이벤트)가
     결정적 RNG(방 번호 기반 시드)로 미리 계산되므로 아직 안 간 방도 "무엇이 나올지"
     미리보기 가능. 지나온 방은 어둡게 "완료" 표시, 현재 방은 금색 테두리로 강조.
     **다만 "첫 번째 선택지에 따라 다음 선택지가 어떻게 바뀌는지"(분기형 노드 그래프 —
     고른 경로에 따라 이후 방 구성 자체가 달라지는 것)까지는 아님.** 지금은 방마다
     노출 여부가 서로 독립적이라 "무엇을 골랐는지"가 "다음 방에 뭐가 뜨는지"에 영향을
     주지 않음(순서만 이미 정해진 방들을 지나가는 구조). 진짜 분기(노드 그래프 +
     경로 선택)는 DESIGN.md가 확정한 "일단은 선형으로" 방향을 벗어나는 더 큰 구조
     변경이라 사람 설계 확인 후 착수하는 게 안전 — **여전히 미착수.**
   - **공격/방어 덱 상시 표시** → **완료** (아래 "완료 기록" 참고).
     `code/scenes/deck_panel.gd`(`DeckPanel`, view-only)를 `dungeon_map`/`shop`/
     `event`/`story_event` 4개 화면에는 상시 표시로, `combat_test.tscn`에는 화면
     여유 공간이 없어 우측 상단 "덱 보기" 토글 버튼으로 여닫는 오버레이로 붙임
     (완전한 "상시"는 아니지만 언제든 클릭 한 번으로 확인 가능). 남은 것은 사람이
     실제로 써보고 토글 방식이 충분한지, 아니면 정말 상시 노출이 필요한지 판단하는
     것뿐(레이아웃을 다시 짜서 상시 표시로 바꾸려면 다이스 뷰포트를 줄이는 등 더 큰
     작업이 필요함).
   - **커스터마이징 상호작용을 "교체"에서 "인벤토리 눈금 교환"으로** → **완료**
     (이번 이터레이션, 아래 "완료 기록" 참고). `RunState.pip_inventory`에 전투 승리
     보상으로 눈금이 쌓이고, `CustomizePanel`이 "눈금 선택 → 다이스 선택 → 면 선택 →
     즉시 교환(밀려난 값은 인벤토리로 복귀)" 흐름으로 바뀜. 남은 것은 눈금 보상량과
     3단계 순서의 체감(인지하기 쉬워졌는지)에 대한 사람 피드백뿐.
   - **어디서든 덱 열람 + 커스터마이징** → **완료 후 combat_test만 일부 되돌림
     (2026-09-03 (13), 아래 "완료 기록" 참고).** "덱 열람"은 이전 이터레이션에
     view-only로 커버됐고(그대로 유지), "커스터마이징"은 던전맵/상점/특수 이벤트/
     스토리 이벤트 4개 비전투 화면에서는 여전히 언제든 열 수 있다. 다만
     `combat_test.tscn`(전투 화면)은 사용자가 INBOX.md로 "커스터마이징은 전투 중에는
     불가능해야 한다"고 명시적으로 되돌려서, 전투가 끝나기 전(`battle_over=false`)에는
     "커스터마이징" 토글 버튼 자체가 안 보이고 눌러도 안 열리도록 막음 — 전투가 끝난
     뒤(승리 보상 화면 또는 패배 후)에는 그대로 열 수 있음. "덱 보기"는 전투 중에도
     계속 열 수 있음(이건 되돌리라는 지시 없었음).
   → "정보 상시 노출"(view), "커스터마이징 어디서든", "인벤토리 눈금 교환" 상호작용
   전환까지 4개 세부 항목 중 3개가 완료됨. 남은 유일한 후보는 진짜 분기형 맵(고른
   선택지에 따라 다음 방 구성이 달라지는 노드 그래프) — DESIGN.md가 확정한 "일단은
   선형으로" 방향을 벗어나는 큰 구조 변경이라 사람 설계 확인이 먼저 필요함.
1. **던전 맵 내용 채우기 (계속)** — "런에 끝이 있다"(`RunState.TOTAL_ROOMS=5`, 완료 시
   화면 전환), "방마다 다른(더 강한) 몬스터", ~~상점 방 종류 추가~~(완료됨 — 아래
   "완료 기록"의 "골드 + 상점 이벤트" 참고), ~~특수 이벤트 방 종류 추가~~(완료됨 —
   아래 "완료 기록"의 ""특수 이벤트" 던전 방 추가" 참고), ~~스토리 이벤트 방 종류
   추가~~(완료됨 — 아래 "완료 기록"의 ""스토리 이벤트" 던전 방 추가" 참고)는
   추가했지만, 방 선택지가 항상 "전투/상점/특수 이벤트/스토리 이벤트" 4개 고정으로만
   나열됨(무작위/기획된 조합이 아니라 매번 같은 4종류 중 확률 노출). 다음 단계 후보:
   - ~~3개 방 선택지가 매번 전부 노출되는 대신, 방마다 일부만 무작위로 제시되거나
     순서가 섞이는 것이 "레벨 디자인"에 더 가까울 수 있음~~ → **완료됨** (노출 확률은
     이전 이터레이션, 순서 섞기는 2026-09-08 (25), 아래 "완료 기록" 참고). "전투 방"은
     항상 노출(진행 보장) + 항상 첫 자리, "상점"/"특수 이벤트"/"스토리 이벤트"는 방마다
     각각 60%/50%/50% 확률로 독립 노출되고, 노출된 것끼리는 방마다 결정적 RNG로 표시
     순서도 섞임(버튼 목록과 MapStrip 칩 순서가 항상 일치). 확률 자체가 적당한지, 순서
     섞기가 실제로 "레벨 디자인" 체감에 도움이 되는지는 사람 피드백 필요.
   - "스토리 이벤트"의 시나리오는 이제 8종(낡은 보물상자/떠돌이 상인/휴식처/수상한
     도박꾼/무너진 제단/숨겨진 샛길/부상당한 여행자/버려진 야영지)이고, 골드
     증감폭(±5~25, 확률 50~60%)은 감으로 잡은 잠정값 — 실제로 플레이해보고 텍스트
     톤이나 골드 임팩트가 적당한지, 8종으로도 여전히 반복 플레이 시 질리는지는
     사람 피드백 필요 (더 늘릴지는 피드백을 보고 판단).
   - 신규 "특수 이벤트" 방의 아이템 3종(D8 추가/D12 추가/가장 작은 다이스->D10 승급)과
     등장 빈도(매 방마다 상시 노출)는 감으로 잡은 잠정값 — 실제로 플레이해보고 너무
     자주/강하게 다면체 다이스를 얻을 수 있는 건 아닌지 사람 피드백 필요.
   - 상점 가격(`shop.gd`의 `ITEM_COSTS`)과 골드 보상(`combat_test.gd`의
     `GOLD_REWARD_BASE`/`GOLD_REWARD_PER_ROOM`)은 감으로 잡은 잠정값 — 실제 플레이해보고
     너무 쉽게/어렵게 아이템을 살 수 있는 건 아닌지 사람 피드백 필요.
   - 몬스터 난이도 스케일링 공식(`combat_test.gd`의 `_monster_config_for_room()` —
     공격 2방마다 +1개, 방어 3방마다 +1개, HP 매 방 +3)은 감으로 잡은 잠정값. 실제
     플레이해보고 체감 난이도 곡선이 적당한지, 후반 방에서 다이스 개수가 늘어난
     시각적 레이아웃(안 겹치는지)이 괜찮은지 사람 피드백 필요
   - ~~몬스터별로 다른 이름/다이스 색 표시~~ → 완료됨 (이름 5종 순환 + 다이스 색 구분,
     아래 "완료 기록" 참고).
   - ~~몬스터 주머니는 여전히 항상 DiceBag.new(4, ...)(D4만)로 생성돼 실제로는 몬스터
     다이스가 D6/D8로 보일 일이 없음~~ → **완료됨** (이전 이터레이션, 아래 "완료 기록"
     참고). `_monster_dice_sides_for_room()`으로 room 0-1: D4, 2-3: D6, 4+: D8 스케일링
     추가. ~~물리적으로(다이스 개수/크기가 커진 방에서 벽 밖으로 안 나가는지) 실제
     배틀로 검증된 적은 없었음~~ → **검증 완료** (이번 이터레이션, 아래 "완료 기록"의
     `GAME_QA_ROOM_OVERRIDE` 항목 참고). room4(공격 4D8/방어 2D8)까지 실제 정지
     감지 경로로 진행시켜 확인 — 벽 밖으로 나가는 다이스 없음. 남은 것: 이 스케일링이
     기존 개수/HP 스케일링과 겹쳐 후반 난이도가 과도해지는 건 아닌지 사람 피드백
     필요(감으로 잡은 구간값, 이건 물리적 검증과 별개로 여전히 사람 판단 영역).
     몬스터별로 "의도적으로" 다른 다이스 형태(예: 특정 몬스터만 유독 큰 다이스)를
     쓰는 것까지는 아님 — 지금은 방 번호에만 연동되고 몬스터 개성과는 무관(몬스터
     다이스 특이 특징 큐 9와 연결될 수 있는 부분).
   - `TOTAL_ROOMS=5`는 감으로 잡은 잠정값 — 실제 플레이해보고 체감 길이가 적당한지
     사람 피드백 필요
2. **전투 씬 다듬기 / 후속 검토**
   - 지금은 사람이 실제로 플레이해봐야 판단 가능한 부분이 많음 (INBOX.md 피드백 대기):
     정지 감지 이후 교환 속도(체감 템포)가 적당한지, "물리 다이스 눈 ≠ 실제 판정값"
     불일치가 체감상 이상한지 등.
   - ~~다이스 개수가 늘어나면 안 겹치고 잘 보이는지~~ → **스폰 위치가 벽 밖으로
     나가는 문제는 고침**(그리드 레이아웃, 한 줄에 4개까지만 놓고 넘치면 다음 줄로),
     **"겹쳐서 정보가 안 보이는지"도 검증 완료**(2026-09-04 (18), 아래 "완료 기록"
     참고). `GAME_QA_ROOM_OVERRIDE=10`으로 다이스 10개(공격 7 + 방어 3)가 한 교환에
     동시에 구르는 상황을 실제 배틀 경로로 만들어 확인 — 벽 밖 이탈 없음, 공격/방어
     색 구분은 다이스가 뒤섞여도 유지됨, 실제 판정값은 항상 2D 도형 칩에서 읽으므로
     3D 다이스가 촘촘해져도 정보 손실은 없음. **남은 것은 딱 하나, 그리고 이건
     AI가 판단할 수 없는 영역**: 카메라 배율상 다이스 뭉치가 화면에서 작게 보이는
     것이 "손맛"으로 충분히 만족스러운지 — 크래시나 정보 손실이 아니라 순수한 느낌의
     문제라 사람이 직접 플레이해보고 INBOX.md에 남겨줘야 판단 가능함(불만족스러우면
     카메라 줌/다이스 크기/두 그룹 간 x축 간격 확대 등으로 대응 가능).
   - ~~다이스 정지 감지 임계값이 다이스 개수가 늘어나면 재검증 필요~~ → **재검증 및
     근본 개선 완료** (아래 "완료 기록" 참고). 정지 판정을 "전체 공용 카운터"에서
     "다이스별 독립 카운터"로 바꿔, 14개(교환당 15개)에서도 매번 4초 안전장치로
     빠지던 것이 2.7~3.4초 안에 자연 정지 감지되도록 개선됨. 남은 것은 실제 플레이에서
     한 주머니가 그 정도로 커질 수 있는지(현재 아이템 시스템상 5방 런에서는 현실적으로
     어려움), 그리고 다이스가 많을 때 서로 겹쳐 구르는 "과정"의 시각적 체감 — 둘 다
     사람 판단/피드백 영역.
3. **다이스 개조(빌드업) 아이템 시스템 다듬기** — 1차 버전(`systems/dice_item_pool.gd`,
   아이템 4종: 다이스 추가/승급/약한 면 강화/모든 면 통일, 전투 승리 후 2개 중 선택)과
   사용자 지정 "커스터마이징"(특정 다이스의 특정 면을 골라 1..면 개수 범위의 값으로
   교체 — 이전 이터레이션의 "+3 누적, 상한 없음" 방식에서 INBOX.md 지시로 변경됨)은
   구현됨. 다음 후보:
   - ~~"다이스 승급"류 아이템(상점 D6 승급/특수 이벤트 D10 대승급)이 승급 대상(더
     작은 다이스)이 하나도 없을 때 가장 작은 다이스를 골라 같은 면 개수로 재교체하면서
     그 다이스의 커스터마이징된 면 값을 표준값으로 조용히 리셋시키던 버그~~ →
     **완료됨** (2026-09-04 (17), 아래 "완료 기록" 참고). 이제 실제로 면 개수가
     늘어나는 다이스가 있을 때만 교체하고, 없으면 아무 일도 하지 않음.
   - ~~승급 대상이 없을 때 아이템을 헛되이 소모하게 되는 건 남아있음 — 더 나은 처리는
     UI에서 애초에 이런 상황엔 아이템을 제시하지 않는 것~~ → **완료됨** (2026-09-04
     (19), 아래 "완료 기록" 참고). `DiceItemPool.is_applicable(item, bag)`로 승급
     대상이 없는 주머니 쪽 버튼을 비활성화("승급 대상 없음")하도록 전투 보상/상점/
     특수 이벤트 3곳 모두 반영.
   - ~~그래도 양쪽 주머니 모두 대상이 없으면 2개짜리 보상 슬롯 하나가 통째로
     비활성화된 아이템으로 낭비됨~~ → **완료됨** (2026-09-07 (20), 아래 "완료 기록"
     참고). `random_choices(n, attack_bag, defense_bag)`가 양쪽 다 적용 불가능한
     아이템은 후보에서 아예 제외(부족해지면 폴백)하도록 개선.
   - ~~아이템 종류가 3개뿐~~ → 4종으로 늘림(이번 이터레이션, "모든 면 통일" 추가 —
     DESIGN.md가 예시로 들었던 "모든 면을 6으로 만들기"를 반영). 강도가 감으로 잡은
     값(D4→D6 고정, 매번 최저면/전체면→최댓값, 상점가 25골드)이라 실제로 플레이해보고
     밸런스가 괜찮은지 사람 피드백 필요.
   - 커스터마이징이 이제 면 개수로 상한이 걸려서(D4는 4를 못 넘음) 예전만큼 "사기
     주사위"가 쉽게 나오진 않음 — 다만 "다이스 승급"(면 개수 자체를 늘리는 아이템)과
     조합하면 승급 후 다시 커스터마이징해서 상한을 계속 올릴 수 있음. 이 조합이 실제로
     "사기 주사위 재미"를 잘 살리는지는 사람 플레이 피드백 필요.
   - ~~INBOX.md는 "강화 카드"라는 표현을 썼는데(주사위 눈이 그려진 카드 형태 UI
     이미지), 지금 구현은 버튼 목록 형태라 시각적으로 "카드"처럼 보이지 않음~~ →
     **금테 두른 카드 패널로 개선 완료** (2026-09-07 (22), 아래 "완료 기록" 참고).
     `code/scenes/item_card_style.gd`(`ItemCardStyle`)로 승리 보상/상점/특수 이벤트
     3개 화면의 아이템 목록이 카드 형태(제목/설명/가격/버튼을 담은 금테 패널)가 됨.
     "주사위 눈이 그려진" 부분(카드 안에 다이스 면 이미지 미리보기)도 **2026-09-07
     (23)에 추가 완료** — 결과 다이스가 확정적인 아이템(다이스 추가/승급)에 한해
     `ShapeDieChip` 면 미리보기를 카드에 넣음. **2026-09-08 (24)에 나머지 두 종류
     (약한 면 강화/모든 면 통일)까지 마저 채움** — 이 둘은 카드 생성 시점이 아니라
     "어느 주머니에 적용할지"가 정해져야 결과가 결정되므로, 버튼 옆에
     `DiceItemPool.preview_effect(item, bag)`으로 그 순간 계산한 미리보기(전/후 값
     또는 통일값)를 붙임(아래 완료 기록 참고). 이제 4종 아이템 전부 카드에 결과
     미리보기가 있음. 남은 것은 실제로 보고 미리보기가 유용한지, 칩 크기(16px)가
     적당한지 사람 피드백뿐.
   - ~~지금은 "물리로 보이는 DieD4"가 항상 정사면체(D4) 모양~~ → **완료됨** (이전
     이터레이션, 아래 "완료 기록" 참고). `code/dice/die_d4.gd`(class_name `Die`)가
     `sides`에 맞춰 D4/D6/D8을 정확한 모양으로, D10/D12는 둥근 임시 근사로 스폰함.
   - ~~D10의 정확한 지오메트리(정오각 사다리 십이면체)가 아직 없음~~ → **완료됨**
     (이전 이터레이션, 아래 "완료 기록" 참고). `_build_pentagonal_trapezohedron()`
     추가로 실제 D10 주사위와 같은 위상 구조를 갖춘 모양이 됨.
   - ~~D12(정십이면체)의 정확한 지오메트리가 아직 없음~~ → **완료됨** (이전
     이터레이션, 아래 "완료 기록" 참고). `_build_dodecahedron()` 추가로 오각형 면
     12개짜리 실제 정십이면체 모양을 갖춤.
   - ~~D20 등 나머지 다면체는 여전히 SphereMesh 근사~~ → **완료됨** (이번 이터레이션,
     아래 "완료 기록" 참고). `_build_icosahedron()` 추가로 정삼각형 면 20개짜리 실제
     정이십면체 모양을 갖춤 — 이제 D4/D6/D8/D10/D12/D20 전부 정확한 지오메트리
     완비(D100 등만 여전히 SphereMesh 근사, 지금 아이템 풀에 주는 곳이 없어 급하지
     않음).
   - ~~지금 아이템 풀은 D8/D10/D12까지만 주고 D20을 주는 아이템은 아직 없음~~ →
     **완료됨** (이번 이터레이션, 아래 "완료 기록" 참고). `EventItemPool`에 "다면체
     주사위 획득 (D20)" 아이템 추가. 남은 것은 등장 빈도/체감 강도가 적당한지 사람
     피드백뿐(다른 특수 이벤트 아이템들과 같은 확률로 무작위 2개 중 노출되므로 D20이
     상대적으로 드묾 — 4종 중 1개).
   - ~~D8/D10/D12/D20 모양이 화면에서 D4와 시각적으로 뚜렷이 구별되는지(작은 화면
     크기 + 현재 조명에서 다들 "회색 삐죽삐죽한 조각"으로 보일 수 있음)~~ →
     **재질별 시각 색 추가로 개선 완료** (이번 이터레이션, 아래 "완료 기록" 참고).
     재질(플라스틱/나무/유리/철제)에 `visual_color`를 추가해 D4=연회백,
     D8=갈색(나무), D10=옅은 청록 반투명(유리), D12/D20=은회색(철제)으로 색이
     확실히 갈림 — `qa_out/combat_test_material_swatch.png` 참고. 남은 것은 이
     색 배정이 실제로 "재질스럽게" 설득력 있어 보이는지 사람 피드백뿐. 참고로 지금
     아이템 풀은 D8/D10/D12까지만 주고 D20을 주는 아이템은 아직 없음(구현은 준비돼
     있으니 나중에 아이템/등급을 추가하면 바로 쓸 수 있음).
   - 몬스터는 개조 대상이 아님(DESIGN.md 확정 — 몬스터 주머니는 고정), 그대로 유지.
   - 패배 시 `RunState.reset_run()`으로 다이스 개조 내역도 함께 초기화됨(런 실패 시
     빌드가 리셋되는 로그라이크 관례를 따름) — 이게 맞는 방향인지도 사람 판단 필요.
4. **사운드 에셋 실제 교체** — 이번 이터레이션에서 `code/dice/procedural_sound.gd`로
   합성한 임시 타격음이 실제 에셋 부재 시 자동 재생되도록 바뀌어서 "완전 무음"은
   해결됨(아래 "완료 기록" 참고). ~~유리/나무/철제 등 다른 재질을 추가할 때 재질별로
   다른 합성음이 필요한지~~ → 재질 자체(플라스틱/나무/유리/철제 4종, bounce/friction +
   재질별 합성음)는 추가 완료됨(이번 이터레이션, 아래 "완료 기록" 참고). 남은 것:
   - 실제 녹음/제작된 `.wav`/`.ogg`로 교체하면 각 `res://resources/materials/*.tres`의
     `impact_sound`만 채우면 되고 코드 변경은 불필요.
   - 지금 4종 합성음이 실제로 재질별로 "그럴듯하게" 구별되어 들리는지는 사람이
     플레이해보고 판단해야 함 (사람이 실제로 들어야 판단 가능한 영역).
   - 재질을 플레이어가 직접 고르거나 획득하는 시스템은 없음 — 지금은 다이스 면
     개수에 따라 코드가 임의로 배정(D4/D6=플라스틱, D8=나무, D10=유리, D12/D20=철제).
     이 매핑이 적당한지, 아니면 재질을 별도 아이템/상점 선택지로 만들지는 사람
     판단 필요.
5. ~~(INBOX.md 신규) 특수 이벤트에서 다이스 개수/다면체 늘리기~~ → **완료됨** (이번
   이터레이션, 아래 "완료 기록" 참고). `systems/event_item_pool.gd` +
   `scenes/event.gd`(+`.tscn`)로 던전 맵에 "특수 이벤트" 방 추가, D8/D10/D12급
   아이템을 무료로 획득 가능. 남은 것은 등장 빈도/강도 밸런스 사람 피드백뿐 (큐 1에
   남겨둠).
6. ~~(INBOX.md 신규) 던전 입장 시 캐릭터 선택~~ → **부분 완료** (이번 이터레이션,
   아래 "완료 기록" 참고). `scenes/character_select.tscn`이 새 main_scene이 되어
   "던전 시작" 버튼으로 진입하는 흐름은 완성됨. 남은 것:
   - **실제 일러스트 에셋이 없음** — 지금은 `character_portrait_placeholder.gd`가
     `_draw()`로 그리는 도형 조립 실루엣(라벤더 단발머리 + 분홍 원피스)일 뿐. 사람이
     실제 스프라이트/일러스트를 구하거나 그려서 교체해야 함 (AI는 이미지 생성 도구가
     없어 진짜 아트를 만들 수 없음). 스타일 방향이 이거면 되는지도 사람 확인 필요.
   - 캐릭터가 1종뿐이라 지금은 "선택"이 아니라 "확인" 버튼에 가까움 — 정말 여러
     캐릭터 중 고르는 화면이 필요하면 카드 여러 개를 나열하는 구조로 확장해야 함
     (사람 지시 없으면 이대로 유지, DESIGN.md에도 "능력 차별화 없는 1종"이 확정값).
7. ~~(INBOX.md 신규) 전투 스프라이트 + 표정 연출~~ → **부분 완료** (이번 이터레이션,
   아래 "완료 기록" 참고). `scenes/character_portrait_placeholder.gd`(플레이어,
   기존 캐릭터 선택 화면과 공유)와 신규 `scenes/monster_portrait_placeholder.gd`
   (몬스터, 뿔 달린 블롭 실루엣)를 `combat_test.tscn`에 배치하고, 공방 결과/승리/패배에
   따라 표정(neutral/happy/hurt/sad/angry)이 바뀌도록 `combat_test.gd`에 배선함.
   남은 것:
   - **실제 일러스트/스프라이트 에셋이 없음** — 지금은 도형 조립 플레이스홀더일 뿐
     (캐릭터 선택 화면과 동일한 한계). "몬스터 무스메"(의인화된 미소녀) 컨셉은 전혀
     반영 안 됨 — 지금 몬스터 실루엣은 뿔 달린 블롭이라 무스메와 거리가 멂. 사람이
     실제 아트 방향을 정하거나 리소스를 구해서 교체해야 함.
   - 표정은 5종(neutral/happy/hurt/sad/angry)뿐이고 전환도 순간적(트랜지션 없음) —
     체감이 어떤지 사람 피드백 필요.
   - 몬스터별 성격(큐 8)이 아직 없어서 표정 변화가 모든 몬스터에 획일적으로 적용됨.
8. **(INBOX.md 신규) 몬스터별 성격 디자인** — 사용자가 "추후 기획, 할 일 거리에 저장"
   이라고 명시. 지금 `MONSTER_PROFILES`(`combat_test.gd`)는 이름/다이스 색만 있고
   성격/말투 등은 없음 — 나중에 기획 문서(DESIGN.md)에 먼저 구체화된 뒤 반영해야 함.
9. **(INBOX.md 신규) 몬스터별 다이스 특이 특징** — 몬스터 외형/특성에 어울리는 다이스
   기믹(예: 특정 면 확률 조작, 특수 효과 등)을 부여하자는 아이디어. 지금 몬스터는 전부
   "표준 D4 다이스 개수만 다른" 동일 구조라 실제 개성은 없음. 구체 기믹은 아직 미정 —
   사람 지시나 몬스터 성격 기획(큐 8)이 먼저 필요할 수 있음.
10. ~~(INBOX.md 신규) 골드 + 상점 이벤트~~ → **완료됨** (이번 이터레이션, 아래 "완료
    기록" 참고). `RunState.gold`, 전투 승리 골드 지급, `scenes/shop.gd` 구현.
    남은 것은 가격/보상 밸런스 사람 피드백뿐 (큐 1에 남겨둠).
11. ~~**(INBOX.md 신규, 부분 완료) 레벨 디자인된 여러 스테이지**~~ → **완료됨**
    (아래 "완료 기록" 참고). "일단은 선형으로 점차 강해지게, 스텝 바이 스텝으로
    수평적 선택지를 넓힌다"는 방향 확인. 던전 맵이 "전투 방" / "상점" / "특수 이벤트" /
    "스토리 이벤트" 네 선택지를 보여주는 것, 그 선택지가 방마다 확률적으로 일부만
    노출되는 것, 순수 텍스트형/스토리형 선택지 이벤트(다이스와 무관)까지 전부 채워짐.
    남은 것은 확률/밸런스가 적당한지 사람 피드백뿐(큐 1로 이관).
12. ~~(INBOX.md 신규 2026-09-02) 다이스 눈을 이미지로 표시~~ → **완료됨** (이번
    이터레이션, 아래 "완료 기록" 참고). "단순 나열된 사각형 각 면에 숫자" 방식(전개도
    방식 대신)으로 구현 — 절차적 StyleBoxFlat 칩이라 별도 아트 에셋 불필요. 남은 것은
    실제로 "어떻게 개조하면 어떤 주사위가 될지 예상하기 쉬운지" 사람 피드백뿐.

## 완료 기록

- **2026-09-09 (26)**: INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는
  이번에도 사람의 추가 기획이 필요해 착수 불가였고, "다음 할 일 큐"의 나머지
  항목들도 전부 사람의 밸런스/설계 피드백이 선행돼야 하는 상태라(이전 여러
  이터레이션과 같은 패턴), 코드 재감사를 하다가 실제 표시 오류를 하나 찾아 고침.
  1. **버그**: `code/scenes/story_event.gd`의 `_on_choice_pressed()`가 골드 손실
     결과 문구를 "요청한 delta" 그대로 표시하고 있었음. `RunState.gold = max(0,
     RunState.gold + delta)`로 골드가 0 밑으로 안 내려가게 클램프하는 것과
     별개로, 결과 라벨은 클램프 전의 `delta`를 그대로 썼다 — 예를 들어 보유
     골드가 3인데 -10 손실 선택지를 고르면 실제로는 3만 잃고 0이 되는데
     ("보유 0"은 맞게 표시됨), 문구는 "골드 10 잃었다..."로 실제와 다른 숫자를
     보여줬다. `shop.gd`는 애초에 `RunState.gold < cost`를 먼저 확인해 감당 못할
     구매 자체를 막아서 이 문제가 없지만(구매 버튼이 아예 비활성화됨),
     `story_event.gd`의 "gamble"/"delta" 선택지는 사전 확인 없이 곧바로 적용하는
     구조라 저골드 상태에서만 드러나는 문제였음.
  2. **수정**: `gold_before`를 적용 전에 기록해두고, 클램프 적용 후
     `actual_delta := RunState.gold - gold_before`를 계산해 그 값으로 결과 문구를
     만들도록 바꿈. 골드가 충분해서 클램프가 발동하지 않는 일반적인 경우는
     `actual_delta == delta`라 기존 동작과 완전히 동일 — 저골드 엣지 케이스에만
     영향을 주는 최소 수정.
  3. **검증**: QA 전용 훅 `_debug_force_low_gold_loss()`를
     `code/scenes/story_event.gd`에 추가(골드를 3으로 강제하고 "떠돌이 상인"
     시나리오(delta -10)의 choice_a를 강제 선택). `bash scripts/qa_shot.sh
     story_event 5 qa_out/story_event_low_gold_loss.png
     _debug_force_low_gold_loss` 스크린샷으로 "골드 3 잃었다... (보유 0)"이
     정확히 표시되는 것을 확인(수정 전이었다면 "골드 10 잃었다..."로 나왔을
     상황). `bash scripts/qa_shot.sh story_event 5 qa_out/story_event_baseline.png
     _debug_pick_choice_a`로 클램프가 발동하지 않는 일반 케이스(골드 0→5 획득)도
     기존과 동일하게 표시되는 것을 재확인해 회귀 없음을 검증. `bash
     scripts/qa_shot.sh dice_test`로 기존 판정 스위트 전체 PASS 재확인(이번 변경은
     story_event 화면 로직만 건드려 판정 스위트와는 무관하지만, 관례대로 재확인).
  → 이 감사 중 `shop.gd`의 골드 차감 로직도 같은 관점(보상/구매인데 조용히
  손해나는 경우가 있는지)으로 다시 확인했고 문제 없음을 확인. `dungeon_map.gd`의
  방 노출/순서 결정 로직도 다시 훑었으나 새 문제는 못 찾음.
- **2026-09-08 (25)**: 던전 맵 방 선택지 "순서 섞기" — 이전 이터레이션이 노출 확률까지만
  구현하고 "순서가 섞이는 것까지는 다루지 않았는데 이게 필요한지는 사람 피드백 필요"로
  남겨뒀던 간극을 채움. INBOX.md에 새 지시가 없고 큐의 다른 항목은 전부 사람의 추가
  기획/실제 아트·오디오 에셋/밸런스 피드백이 선행돼야 해서 착수 불가였는데, 이 항목만
  유일하게 코드로 바로 진행 가능한 잔여 큐였음.
  1. **`code/scenes/dungeon_map.gd`의 `_room_options_for_index(idx)`에 `"order"` 키
     추가**: 기존에 상점/특수 이벤트/스토리 이벤트 노출 여부(`shop`/`event`/`story`
     불리언)를 계산하던 것과 같은 시드(`idx * 104729 + 7`)의 `RandomNumberGenerator`를
     이어서 써서, `["shop","event","story"]` 배열을 Fisher-Yates로 섞은 결과를
     `"order"`로 함께 반환한다. "전투 방"은 진행을 보장하는 항상 노출 + 항상 첫 자리라
     섞기 대상에서 제외.
  2. **버튼 순서(`_layout_visible_buttons`)와 MapStrip 칩 순서(`_make_map_node`) 둘 다
     이 `order`를 따르도록 교체**: 기존엔 "상점 → 특수 이벤트 → 스토리 이벤트" 고정
     순서로만 나열했던 것을, `type_button_map`/`chip_specs` 딕셔너리 + `for t in
     opts.order` 루프로 바꿈. 새 멤버 변수 `_room_order: Array[String]`을
     `_roll_room_choices()`에서 함께 저장해 버튼 레이아웃이 매번 같은 값을 씀. 노출
     여부와 순서가 같은 `_room_options_for_index(idx)` 한 함수에서 나오므로, 실제
     들어간 방의 버튼 순서와 MapStrip 미리보기의 칩 순서가 항상 일치함(미리보기와
     실제 결과가 어긋나지 않는다는 기존 설계 원칙을 그대로 유지).
  3. **검증**: `bash scripts/qa_shot.sh dungeon_map 30 qa_out/dungeon_map_room_order.png`
     (1번째 방 — 상점만 노출, 버튼/칩 정상), `bash scripts/qa_shot.sh dungeon_map 30
     qa_out/dungeon_map_room1_order.png _debug_advance_rooms`(3번째 방으로 진행 —
     "특수 이벤트"가 "상점"보다 먼저 노출되는 방을 확인, MapStrip 칩 순서(특수 이벤트→
     상점)와 버튼 순서(전투→특수 이벤트 입장→상점 입장→커스터마이징)가 정확히 일치함을
     스크린샷으로 확인, 레이아웃 겹침/잘림 없음) 2개 스크린샷 눈으로 확인.
     `bash scripts/qa_shot.sh dice_test`로 기존 판정 스위트 전체 PASS 재확인(순수
     UI/노출 로직 변경이라 판정 로직에는 영향 없음을 재확인하는 차원).
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김. 이번 순서 섞기가
  실제로 "레벨 디자인" 체감에 도움이 되는지는 사람 피드백 필요.
- **2026-09-08 (24)**: 직전 이터레이션(23)이 남겨둔 간극 — "약한 면 강화"/"모든 면
  통일" 아이템은 카드 생성 시점엔 대상 다이스가 정해지지 않아 결과 미리보기가
  없었음 — 을 채움. 세션 시작 시 이 작업의 코드가 이미 커밋 없이 작업 트리에
  존재했음(이전 세션이 중간에 끊김) — 검토 후 의도대로 마무리.
  1. **`code/systems/dice_item_pool.gd`에 `preview_effect(item, bag) -> Dictionary`
     신규 추가**: 기존 `_boost_weakest_face()`/`_uniformize_worst_die()`가 각자
     들고 있던 "가장 개선이 필요한 다이스/면 찾기" 로직을 `_locate_weakest_face(bag)`/
     `_max_face_value(faces)` 두 헬퍼로 공통화한 뒤, `preview_effect()`가 이 헬퍼로
     bag을 바꾸지 않고(순수 조회) `apply()`가 실제로 낼 결과(`boost_weak_face`→
     전/후 값, `uniform_faces`→통일값+면 개수)를 미리 계산해 반환한다. `add_die`/
     `upgrade_die`는 이미 카드 레벨에서 처리되므로 `null` 반환.
  2. **`code/scenes/item_card_style.gd`에 `build_effect_preview(item, bag)` 신규
     추가**: `preview_effect()` 결과를 받아 칩 1~2개 + 화살표/배수 텍스트로 된 고정
     크기 미리보기 `Control`을 만든다(대상 다이스 면 개수가 커져도 카드가 안 깨지게
     항상 작게 구성). `combat_test.gd`(승리 보상)/`shop.gd`(상점)/`event.gd`(특수
     이벤트) 3개 화면 모두 공격/방어 버튼을 만들 때 각각 그 주머니를 넘겨 버튼 바로
     위에 이 미리보기를 붙임.
  3. **`shop.gd`의 2행 카드 그리드 간격 조정**: 미리보기가 붙은 카드(약한 면 강화/
     모든 면 통일, 2행)는 Godot의 최소 크기 강제 규칙 때문에 지정한 240px보다
     실제 높이가 커짐 — 기존 `row_step = card_height + 20`(260)을 그대로 쓰면 2행
     카드 하단이 화면 하단 버튼과 맞닿을 뻔해서 `row_step = 235`로 줄임(1행 카드는
     미리보기가 없어 자연 높이가 240보다 작으므로 겹치지 않음, 스크린샷으로 확인).
  4. **아이템 이름 정리**: "다이스 승급 (가장 작은 다이스 -> D6)" 등 긴 이름을
     "다이스 승급 (-> D6)"으로 줄임 — 설명 문장에 "면 개수가 가장 작은 다이스"가
     이미 있어 제목에서는 중복.
  5. **검증**: `code/scenes/dice_test.gd`에 회귀 테스트 5개 추가 — `preview_effect()`
     계산값이 실제 `apply()` 결과와 일치하는지(수식 직접 검산: D4x3 주머니에서
     `boost_weak_face` 전/후 1→4, 적용 후 `min_possible()`=4 일치 / `uniform_faces`
     통일값 4, 적용 후 `min_possible()`=6 일치), bag을 변경하지 않는지(순수 조회),
     `add_die`류는 `null`을 반환하는지. `bash scripts/qa_shot.sh dice_test`로 기존
     판정 스위트 전체(신규 포함) PASS 확인. `bash scripts/qa_shot.sh shop 5
     qa_out/shop_effect_preview.png`(4개 카드 전부 미리보기 정상 표시, 2행 카드가
     1행/하단 버튼과 안 겹침), `bash scripts/qa_shot.sh event 5
     qa_out/event_effect_preview.png`, `bash scripts/qa_shot.sh combat_test 1500
     qa_out/combat_test_effect_preview.png "" 1`(둘 다 이번엔 무작위로 add_die/
     upgrade_die만 뽑혀 새 미리보기 자체는 화면에 안 나왔지만 카드 레이아웃 회귀
     없음 확인 — 새 미리보기 렌더링 자체는 4개 카드가 항상 다 보이는 shop 화면에서
     확인됨, 같은 `build_effect_preview()` 함수를 공유하므로 다른 화면에서도 같은
     결과가 보장됨) 스크린샷 전부 눈으로 확인.
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
- **2026-09-07 (23)**: 아이템 카드에 결과 다이스 면 미리보기(ShapeDieChip) 추가.
  1. **`code/scenes/item_card_style.gd`에 `_build_result_die_preview(item)` 신규
     추가**: `item["kind"]`가 `"add_die"`(결과 `sides`)/`"upgrade_die"`(결과
     `new_sides`)면, 그 면 개수만큼 `HFlowContainer` 안에 `ShapeDieChip`(값 1..N,
     `ShapeDieChip.shape_sides_for_dice_sides()`로 모양 결정)을 나열해 반환한다.
     그 외 종류(`boost_weak_face`/`uniform_faces`)는 결과가 그 시점 주머니 상태에
     따라 달라져 카드 생성 시점에 확정할 수 없으므로 `null`을 반환한다.
     `build_card()`가 설명 라벨 다음에 이 미리보기를 있으면 넣도록 한 줄 추가.
  2. **여백 확보**: 카드 설명 라벨의 `custom_minimum_size.y`를 54→40으로 줄이고
     (실제 텍스트가 더 길면 Label이 스스로 확장되므로 잘리지 않음, 최소 예약 높이만
     줄인 것), 미리보기 줄이 들어갈 공간을 만들기 위해 세 호출부의 `card_height`를
     늘림: `combat_test.gd`(승리 보상) 230→260, `shop.gd`(2x2 그리드, 세로 공간이
     가장 빠듯함) 230→240, `event.gd`(카드가 한 줄뿐이라 여유가 가장 큼) 230→300.
     각 화면의 화면 좌표(로그 라벨/커스터마이징 버튼/다음 방 버튼 등 다른 UI 요소)와
     안 겹치는 선에서 여유를 계산해 값을 정함 — 특히 event.gd는 D8/D10/D12/D20까지
     다루는 `EventItemPool`을 쓰므로(D20은 면 20개, `HFlowContainer`가 자동으로
     2줄로 줄바꿈) 가장 넉넉하게 잡음.
  3. **시각 검증**: `bash scripts/qa_shot.sh combat_test 1500
     qa_out/combat_test_reward_preview.png "" 1`(승급 카드에 D6 면 6개 미리보기,
     "모든 면 통일" 카드는 미리보기 없이 정상), `bash scripts/qa_shot.sh shop 5
     qa_out/shop_preview.png`(D4/D6 미리보기, 2x2 그리드 안 겹침), `bash
     scripts/qa_shot.sh shop 5 qa_out/shop_no_upgrade_preview.png
     _debug_force_no_upgrade_target`(골드 999 + 승급 대상 없음 상태에서도 미리보기와
     "승급 대상 없음" 비활성 버튼이 함께 정상 표시), `bash scripts/qa_shot.sh event 5
     qa_out/event_preview.png`(D10/D12 미리보기), `bash scripts/qa_shot.sh event 5
     qa_out/event_d20_preview.png _debug_force_offer_d20`(가장 극단적인 경우 — D20
     면 20개가 2줄로 자동 줄바꿈돼도 카드 밖으로 넘치거나 버튼과 안 겹침) 총 6개
     스크린샷 전부 눈으로 확인 — 크래시 없음, 모든 카드가 패널 테두리 안에 정상
     렌더, 우측 DeckPanel과 안 겹침. `bash scripts/qa_shot.sh dice_test`로 기존 판정
     스위트 전체 PASS 재확인(순수 UI 변경이라 판정 로직 영향 없음 재확인 차원). 남은
     것: 미리보기 칩 크기(16px)가 실제로 알아보기 편한지, 정보량이 유용한지는 사람
     피드백 필요.
- **2026-09-07 (22)**: 승리 보상/상점/특수 이벤트 아이템 목록을 카드형 비주얼로 개선.
  1. **`code/scenes/item_card_style.gd`(`ItemCardStyle`) 신규 추가**: `FaceChipStyle`
     (`code/scenes/face_chip_style.gd`)과 같은 패턴의 정적 헬퍼 클래스. `build_card(item,
     extra_label_text="")`가 PanelContainer(금테 StyleBoxFlat, 둥근 모서리, 어두운
     배경) 안에 VBoxContainer(제목 라벨 → 선택적 보조 문구 라벨 → 설명 라벨 →
     늘어나는 spacer → 버튼을 담을 VBoxContainer)를 구성해 `{"card", "vbox",
     "button_row"}` 딕셔너리로 반환한다. 호출부가 `card.position`/`card.size`를
     지정해 씬에 `add_child()`하고, `button_row.add_child(btn)`으로 버튼을 넣으면
     카드 폭에 맞춰 자동으로 늘어난다(PanelContainer가 자식 VBoxContainer를 자기
     rect에 맞춰 자동으로 채우는 Godot Container 기본 동작을 이용 — 카드 크기만
     명시하면 내부 레이아웃은 컨테이너가 알아서 계산).
  2. **세 호출부 교체**: `combat_test.gd`의 `_show_reward_ui()`(승리 보상, 카드 2개
     가로 배치, 카드 400x230), `shop.gd`의 `_rebuild_items()`(4개, 2열x2행 그리드,
     카드 320x230, `ItemCardStyle.build_card(item, "%d 골드" % cost)`로 가격을 보조
     문구로 표시), `event.gd`의 `_rebuild_items()`(2개, 가로 배치, 카드 320x230) —
     기존에는 셋 다 `Label.new()`(이름+설명 텍스트) + `Button.new()`(공격/방어 버튼)를
     화면에 절대 좌표로 직접 흩뿌리는 거의 동일한 ~30줄짜리 코드를 각자 들고 있었는데,
     이제 카드 생성은 `ItemCardStyle.build_card()` 한 줄로 통일하고 버튼 생성·연결
     (대상 주머니 판정 `DiceItemPool.is_applicable()`, disabled 조건, 골드 차감 등
     화면마다 다른 로직)만 각 화면이 기존처럼 담당한다. 처음 로컬 `const CARD_WIDTH
     := ...`로 작성했다가, 이 프로젝트 코드베이스에 함수 내부 지역 `const` 선언
     전례가 전혀 없어(다른 모든 지역 상수는 `var`로 선언) 안전하게 `var`로 바꿈
     (GDScript 자체는 지역 const를 지원하지만, 굳이 새 패턴을 검증 없이 들여올
     이유가 없다고 판단).
  3. **시각 검증**: `bash scripts/qa_shot.sh combat_test 1500
     qa_out/combat_test_reward_cards.png "" 1`(승리 보상 화면, 카드 2개가 금테
     패널로 나란히 표시, 버튼/설명 전부 카드 안에 정상 배치), `bash scripts/qa_shot.sh
     shop 5 qa_out/shop_cards.png`(상점 기본 상태, 골드 0이라 전부 "골드 부족"
     비활성 버튼, 2x2 카드 그리드 + 우측 DeckPanel과 안 겹침), `bash scripts/qa_shot.sh
     shop 5 qa_out/shop_cards_no_upgrade.png _debug_force_no_upgrade_target`(골드
     999 + 공격/방어 주머니 전부 D6로 강제해 "다이스 승급" 카드만 "승급 대상 없음"
     비활성 버튼 2개로 표시되는 것 확인, 나머지 3장은 정상 "구매" 가능), `bash
     scripts/qa_shot.sh event 5 qa_out/event_cards.png`(특수 이벤트, 카드 2개 가로
     배치) 네 스크린샷 전부 크래시 없이 저장 성공, 레이아웃 정상(카드 겹침/잘림
     없음, disabled 버튼도 카드 스타일과 자연스럽게 어울림). `bash scripts/qa_shot.sh
     dice_test 5 qa_out/dice_test.png`로 기존 판정 스위트 전체(신규 회귀 3종 포함)
     PASS 재확인(이번 변경은 순수 UI 레이어라 판정 로직에는 손대지 않았음을 재확인).
  4. **경위**: INBOX.md 미처리 항목(몬스터 성격/다이스 특이 특징)은 여전히 사람의
     추가 기획이 필요해 착수 불가였고, 지난 4개 이터레이션(17~21)이 연속으로 "코드
     재감사, 새 버그 없음"만 반복해 진행 가능한 콘텐츠를 다시 찾아봤음. 큐 3번에
     "카드형 비주얼로 다듬을지는 사람 피드백 필요"로 미뤄져 있던 항목을, 새 밸런스/
     기획 결정 없이도 착수 가능한 순수 시각 다듬기로 재해석해 진행함. 다이스 면
     이미지를 카드 안에 넣는 것까지는 하지 않음(아이템 종류마다 어느 다이스/면을
     바꾸는지 특정되지 않는 경우가 많아 미리보기가 애매함) — 이 정도 카드 비주얼이
     사람이 원한 수준인지는 실제로 보고 판단 필요.
- **2026-09-07 (21)**: INBOX.md/큐에 곧바로 진행할 콘텐츠가 없어 코드 재감사 + 문서
  정정을 함. 코드 변경은 없음.
  1. **코드 재감사**: 직전 이터레이션(20)이 커밋한 `dice_item_pool.gd`/
     `event_item_pool.gd`(`random_choices()`의 양쪽 주머니 필터링)와 `event.gd`
     (필터링된 `random_choices()` 호출부)를 신선한 눈으로 다시 읽음 — 로직 정확함
     (필터링 → 부족하면 폴백 → 셔플 순서, `is_applicable()` 재사용 방식 모두 문제
     없음). 이어서 아직 "보상 아이템인데 조용히 손해" 관점으로 다시 읽지 않았던
     `customize_panel.gd`(눈금 선택 → 다이스 선택 → 면 선택 → 즉시 교환, 밀려난
     값 인벤토리 복귀)와 `dice_bag.gd`(roll/roll_detailed/add_die/replace_die/
     set_face_value)를 점검함 — 초과 눈금이 다이스 면 개수로 잘려 적용되는 것은
     주석대로 의도된 동작(초과분은 버려짐, 큰 눈금은 큰 다이스에 써야 유리하도록
     유도하는 설계)이지 버그가 아님을 확인. 마지막으로 `combat_test.gd`의 전투 루프
     (`_run_battle()`/`_do_exchange()`)를 "동시 처치(양쪽 HP가 같은 교환에 동시에
     0)" 같은 엣지 케이스 관점으로 다시 읽음 — 플레이어가 항상 먼저 공격하고
     `battle_over`가 true가 되면 그 즉시 `_run_battle()`의 while 루프가
     `break`하므로 동시 사망 자체가 구조적으로 발생 불가함을 확인(버그 아님, 설계상
     안전). 패배 후 전투 종료 화면에서 커스터마이징 토글 버튼이 다시 보이는 것도
     `RunState.reset_run()`이 `pip_inventory`까지 함께 초기화하므로 "패배 직전에
     눈금을 써서 손해 보는" 상황으로 이어지지 않음을 확인. **결론: 새 버그 없음.**
     `scripts/qa_shot.sh dice_test`로 기존 판정 스위트 전체 PASS 재확인.
  2. **"알려진 이슈" 문서 정정**: 위 감사 도중, "알려진 이슈" 목록의 "QA 창이 실제
     창을 띄우므로 오래 떠 있으면 우발적으로 클릭될 수 있다" 항목이 낡았음을 발견함.
     이 항목은 2026-09-01(`9321bfd`)에 실제로 겪은 문제로 기록됐는데, 바로 다음날인
     2026-09-02(`e1a7d35` "QA 캡처 시 창을 화면 밖으로 이동해 사람 작업 방해 방지")에
     `code/qa/visual_qa.gd`의 `_ready()`가 QA 모드 진입 시 `get_window().position =
     Vector2i(-4000, -4000)`로 창을 화면 밖 좌표로 옮기는 코드를 **다른 목적**(사람이
     PC를 쓰는 동안 QA 창이 포그라운드로 튀어나와 방해되지 않게 하려는 것, 우발적
     클릭 방지가 원래 목적은 아니었음)으로 추가했음. 그 뒤로 아무도 이 known issue를
     그 커밋과 다시 연결해 확인하지 않아 계속 "미해결"로 남아있었던 것으로 보임 —
     화면 밖 좌표로 이동한 창은 (극단적인 다중 모니터 배치가 아닌 한) 실제 마우스가
     물리적으로 그 위치에 있을 수 없으므로 부수 효과로 사실상 이미 해결된 상태.
     아래 "알려진 이슈" 섹션에 취소선으로 반영(코드 변경은 하지 않음, git 히스토리
     교차 확인으로 판단한 것이라 실제 재현 테스트로 확인한 것은 아님을 명시해둠).
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
- **2026-09-04 (19)**: 큐 3번에 이전 이터레이션(17)이 남겨둔 개선 여지("다이스 승급
  아이템이 승급 대상 없을 때 UI에서 애초에 제시하지 않는 것이 더 나은 처리일 수
  있다")를 구현함.
  - `code/systems/dice_item_pool.gd`에 `is_applicable(item: Dictionary, bag: DiceBag)
    -> bool`을 추가. `upgrade_die` 종류는 기존 `_find_smallest_die(bag,
    item["new_sides"])`가 대상을 못 찾으면(즉 apply()가 실제로는 아무 일도 안 하는
    상황) false를 반환하고, 그 외 종류(`add_die`/`boost_weak_face`/`uniform_faces`)는
    주머니가 비어있지 않은 한(항상 그렇다) 항상 true.
  - `code/scenes/combat_test.gd`(`_show_reward_ui`), `code/scenes/shop.gd`
    (`_rebuild_items`), `code/scenes/event.gd`(`_rebuild_items`) 3곳 모두 공격/방어
    버튼을 만들 때 `DiceItemPool.is_applicable(item, RunState.player_attack_bag /
    player_defense_bag)`을 각각 독립적으로 확인해, 대상이 없으면 그 버튼만
    `disabled=true` + 텍스트 "승급 대상 없음"으로 바꿈(공격/방어 주머니 구성이 서로
    다를 수 있으므로 한쪽만 비활성화될 수 있음). `shop.gd`는 기존 "골드 부족" 판정과
    함께 우선순위(골드 부족 > 승급 대상 없음 > 정상 구매 가능 텍스트)를 정해 합침.
  - `code/scenes/dice_test.gd`에 `is_applicable` 회귀 테스트 3개 추가(대상 없음→false,
    대상 있음→true, upgrade_die가 아닌 종류→항상 true). `scripts/qa_shot.sh dice_test`로
    기존 판정 스위트 전체(신규 포함) PASS 확인.
  - 시각 검증: `shop.gd`에 QA 전용 훅 `_debug_force_no_upgrade_target()`(공격/방어
    주머니를 D6x3으로 강제 교체 + 골드 999 지급 후 목록 재구성)을 추가해
    `bash scripts/qa_shot.sh shop 5 qa_out/shop_no_upgrade_target.png
    _debug_force_no_upgrade_target`로 확인 — "다이스 승급 (D6)" 아이템만 두 버튼
    모두 "승급 대상 없음"으로 회색 비활성화되고, 나머지 3종(다이스 추가/약한 면
    강화/모든 면 통일)은 골드가 충분하니 정상 활성화되어 의도대로 동작함을 확인.
  - 회귀 확인: `combat_test`(교환 진행 후 승리 보상 화면, 초기 D4 주머니라 D6 승급
    버튼 정상 활성)와 `event`(특수 이벤트, D20/D10 대승급 모두 초기 D4 주머니 기준
    정상 활성) 둘 다 스크린샷으로 크래시 없음/레이아웃 정상 확인.
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
- **2026-09-04 (18)**: INBOX.md/큐에 곧바로 진행할 콘텐츠가 없어 두 가지를 함. 코드
  변경은 없음(감사 + 검증만).
  1. **코드 재감사**: 이전 이터레이션이 `dice_item_pool.gd`에서 "보상 아이템인데
     조용히 손해"류 버그를 찾은 것과 같은 시각으로, 아직 그런 관점으로 다시 읽지
     않았던 시스템 파일들을 전부 훑음 — `dice_bag.gd`(roll/roll_detailed/add_die/
     replace_die/set_face_value/min_possible/max_possible), `dice_item_pool.gd`
     (boost_weak_face/uniform_faces가 "이미 최댓값인 면을 다시 최댓값으로" 같은
     엣지 케이스에서 데이터 손실 없이 안전한지), `run_state.gd`, `shop.gd`,
     `event_item_pool.gd`, `story_event_pool.gd`+`story_event.gd`(골드 증감 로직),
     `event.gd`, `dungeon_map.gd`(방 노출 확률 시드가 실제 진행과 MapStrip 미리보기
     사이에 어긋나지 않는지), `die_d4.gd`(D4~D20 지오메트리 생성 전부 + 재질 적용
     순서), `deck_panel.gd`/`shape_die_chip.gd`(add_child 이전에 프로퍼티를 설정해도
     `_ready()`가 나중에 값을 올바르게 반영하는지), `customize_panel.gd`(눈금-면
     교환 시 인덱스 어긋남 없는지), `combat_test.gd`의 보상/커스터마이징 관련 함수들.
     **결론: 새 버그 없음** — 전부 의도대로 동작. `scripts/qa_shot.sh dice_test`로
     기존 판정 스위트(신규 회귀 테스트 포함) 전체 PASS 재확인.
  2. **큐 2번 미검증 항목 실측**: "다이스 개수가 많아지면 서로 겹쳐서 시야를 가릴
     수 있는지"는 이전 이터레이션들이 "물리적으로 벽 밖 이탈은 없다"까지만 확인하고
     "겹쳐서 안 보이는지"는 사람 판단 영역으로 미뤄뒀던 부분. `GAME_QA_ROOM_OVERRIDE=10`
     (`bash scripts/qa_shot.sh combat_test 200 qa_out/combat_test_dice_overlap_stress.png
     "" 1 "" 10`)으로 몬스터(강화 강화 슬라임, 공격 7×D8/방어 4×D8/HP40)와의 교환 중
     "몬스터 공격 7×D8 vs 플레이어 방어 3×D4" = 다이스 10개가 한 교환에 동시에 굴러가는
     실제 배틀 상황을 만들어 스크린샷으로 확인함. 결과: (a) 벽 밖으로 나간 다이스 없음
     (room4에서 확인된 결론이 10개에서도 유지), (b) 물리적으로 다이스들이 뒤섞여도
     공격(몬스터색)/방어(기본색) 두 그룹이 색으로는 계속 구분됨, (c) 실제 판정값은
     물리 다이스의 착지 면이 아니라 화면 하단 `ShapeDieChip`(2D 도형 칩)에서 항상
     읽으므로(기존 "알려진 이슈"에 남아있던 설계) 3D 다이스가 촘촘해져도 정보 손실은
     없음. 카메라 배율상 다이스 뭉치 자체가 화면에서 작아 보이는 것은 크래시나 정보
     손실이 아닌 순수 "느낌"의 문제라 코드를 건드리지 않고 사람 피드백 영역으로 남김
     (임의로 카메라/간격을 조정하면 오히려 의도한 손맛을 해칠 위험 판단).
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
- **2026-09-04 (17)**: 캐릭터 선택/던전 맵/상점/특수 이벤트/스토리 이벤트/전투
  6개 화면을 `scripts/qa_shot.sh`로 재점검(회귀 없음, 전부 정상 레이아웃) 후,
  `code/systems/dice_item_pool.gd`를 읽다가 "다이스 승급" 계열 아이템의 실제 버그를
  발견해 고침.
  - 문제: `upgrade_die` 종류 아이템(상점 "다이스 승급 -> D6" 20골드,
    `code/systems/event_item_pool.gd`의 특수 이벤트 "다이스 대승급 -> D10")은
    `_find_smallest_die(bag)`로 주머니에서 면 개수가 가장 작은 다이스를 찾아
    `DiceBag.replace_die(idx, new_sides)`로 교체하는데, `replace_die()`는 항상 면
    값을 표준 1..N으로 새로 채운다. `_find_smallest_die()`가 "찾은 다이스가 실제로
    new_sides보다 작은지"는 확인하지 않아서, 주머니의 모든 다이스가 이미 new_sides
    이상이면(예: D4가 하나도 안 남은 상태에서 D6 승급 아이템을 또 고르거나, 이미 다
    D10 이상인 상태에서 D10 대승급 아이템을 고르는 경우) 그중 "가장 작은" 다이스를
    골라 **같은 면 개수로 재교체**해버려서, 다이스 크기는 그대로인데 `CustomizePanel`
    눈금 교환으로 키워둔 면 값만 표준값으로 조용히 리셋되는 손해가 났다(보상 아이템을
    골랐는데 실질적으로는 손해를 보는 상황).
  - 수정: `_find_smallest_die(bag, below_sides = -1)`에 임계값 인자를 추가해,
    `below_sides`가 주어지면 면 개수가 그보다 작은 다이스 중에서만 고르도록 함.
    `apply()`의 `upgrade_die` 분기가 `_find_smallest_die(bag, item["new_sides"])`를
    호출하도록 바꿔, 실제로 더 커지는 다이스가 있을 때만 교체하고 없으면 아무 일도
    하지 않는다(아이템을 헛되이 소모하게 되는 건 남지만, 최소한 손해는 아니게 됨 —
    더 나은 처리는 UI에서 애초에 이런 상황엔 이 아이템을 제시하지 않는 것일 수 있는데,
    이번엔 최소 수정으로 손해만 우선 막음. 남은 개선 여지는 큐 3번에 남겨둠).
  - 검증: `code/scenes/dice_test.gd`에 회귀 테스트 추가 — D6 다이스 1개짜리 주머니의
    면 0을 99로 세팅해두고(표준 범위 밖 마커 값) `upgrade_die new_sides=6`을 적용,
    면 값이 99로 그대로인지 확인(수정 전 코드였다면 표준값 1로 리셋되어 FAIL했을
    로직). `scripts/qa_shot.sh dice_test`로 기존 판정 스위트 전체(새 테스트 포함)
    PASS 확인 — 기존 "apply(upgrade_die): max=14" 테스트(승급 대상이 있는 정상
    케이스)도 그대로 통과해 회귀 없음.
  → INBOX.md 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
- **2026-09-04 (16)**: QA 하네스에 `GAME_QA_ROOM_OVERRIDE` 환경변수를 추가함
  (`code/scenes/combat_test.gd`의 `_room_index_for_monster_config()`,
  `scripts/qa_shot.sh`의 7번째 인자). 지난 이터레이션의 "몬스터 다이스 면 개수
  D4->D6->D8 스케일링"이 실제로 후반 방(다이스 개수/크기가 늘어남)에서 물리적으로
  문제없는지 실제 배틀 경로(정지 감지 포함)로 빠르게 검증할 방법이 없었던 간극을
  메움 — `RunState.rooms_cleared`를 실제로 여러 판 이겨서 올리지 않고도, 원하는
  room_index의 몬스터 구성으로 즉시 전투를 시작시킬 수 있다(RunState 자체는
  건드리지 않아 부작용 없음). `bash scripts/qa_shot.sh combat_test 200
  qa_out/combat_test_room4_real.png "" 1 "" 4`로 room4(다크 나이트, HP22, 공격
  4D8/방어 2D8)까지 실제 진행시켜 검증한 결과, 다이스가 전부 바닥에 정상 안착하고
  벽(combat_test.tscn Wall*, x=±2.55/z=±1.8) 밖으로 나가는 문제는 없음을 확인함
  (새 버그를 발견하진 못했지만, 앞으로 몬스터 스케일링을 더 키울 때 재검증 도구로
  남음). 시행착오: 처음엔 `freeze` 없이 다이스만 새로 스폰하고 GAME_QA_CALL로
  즉시 캡처하는 방식(`_debug_spawn_room4_monster_dice_live`, 이후 제거)을 먼저
  써봤는데, GAME_QA_CALL은 캡처 직전 1프레임만 지나 호출되어 다이스가 낙하할
  시간이 없어 "공중에 뜬 것처럼" 보이는 오탐을 겪음 — `_ready()` 자체의 몬스터
  구성을 바꿔 기존 정지 감지 파이프라인(`_run_battle()` -> `_do_exchange()` ->
  `_wait_for_dice_to_settle()`)을 그대로 타게 하는 `GAME_QA_ROOM_OVERRIDE` 방식으로
  교체해 해결함. 기존 회귀 스위트(`dice_test` PASS)와 room0 베이스라인(`combat_test
  140`, 오버라이드 미지정)도 재확인해 정상 플레이 경로에는 영향 없음을 검증.
  INBOX.md의 미처리 항목 2개(몬스터 성격/다이스 특이 특징)는 이번에도 사람의 추가
  기획이 필요해 착수하지 않음 — 다음 할 일 큐 8/9번에 그대로 남김.
*(이터레이션 14 이전의 더 오래된 완료 기록은 `docs/STATUS_ARCHIVE.md`에
보관돼 있음 — 이 파일에는 최근 10개만 유지해 매 이터레이션 읽기 비용을 줄임,
2026-09-08 정리.)*

## 알려진 이슈 / 막힌 것

- **`.claude/settings.json`(과 `settings.local.json`) 쓰기가 이 세션에서 항상
  "승인 필요"로 거부됨** — `Write`/`Edit`가 `permissions.allow`에 일반 허용되어
  있어도 이 두 설정 파일만은 예외적으로 매번 실제 승인을 요구하는 것으로 보임(자기
  자신에게 권한을 부여하는 것을 막는 안전장치로 추정). 사람이 없는 무인 세션에서는
  승인을 받을 방법이 없어 이 파일들을 못 고침. 그 결과 `scripts/qa_shot.sh`를
  `Util/` 밑으로 완전히 옮기지 못하고 루트에 예외로 남겨둠(위 "완료 기록"의 "프로젝트
  폴더 재구성" 참고). 사람이 직접 `.claude/settings.json`의
  `"Bash(scripts/qa_shot.sh *)"`를 `"Bash(Util/scripts/qa_shot.sh *)"`로 바꿔주면,
  다음 세션에서 `scripts/qa_shot.sh`를 `Util/scripts/qa_shot.sh`로 옮기고 루트에서
  완전히 제거할 수 있음.
- **`GAME_QA_FRAME`(렌더 프레임 카운트)과 실제 물리/게임 진행 시간이 실행마다 다르게
  디커플링될 수 있음** → **완화됨** (2026-09-03 (9), 위 "완료 기록" 참고).
  `GAME_QA_SETTLE=1`(`scripts/qa_shot.sh`의 5번째 인자)을 켜면 `combat_test.gd`
  같이 `_qa_is_settled()`를 구현한 씬에서는 다이스가 실제로 멈춘 뒤에 캡처하므로
  "다이스가 한창 구르는 중간 순간을 찍는" 문제는 없앨 수 있다. 다만 "전투가 몇 번째
  교환까지 진행됐는가"(프레임 수 대비 실제 진행된 교환 횟수)는 여전히 실행마다
  달라질 수 있음 — 이건 프레임 카운트 자체의 한계라기보다 "그 시점에 정확히 어떤
  상태를 보고 싶은지"에 달린 문제라, 특정 교환 시점을 정확히 잡고 싶으면 여전히
  스크린샷을 열어 확인하거나 `GAME_QA_CALL` 디버그 훅으로 상태를 직접 확인하는 게
  안전함. 아래 원본 서술은 발견 당시 기록 그대로 보존:
  `_debug_show_mixed_dice_shapes`를
  `GAME_QA_FRAME=30`으로 두 번 연속 실행했는데 둘 다 전투가 이미 "승리!"까지 끝난
  상태에서 디버그 콜이 실행됨 — 원래 이 헬퍼는 "스폰 직후 1프레임" 정도를 가정하고
  만들어졌던 것(코드 주석 참고)인데, 실제로는 30 프레임 시점에 이미 여러 번의
  교환(각 교환마다 최대 4초까지 걸리는 정지 감지 대기 포함)이 끝나 있었음. 같은
  씬을 `GAME_QA_FRAME=5`로 실행하면 아직 첫 교환이 진행 중인 상태로 잡히는 것으로
  보아, "프레임 번호"가 곧 "그 프레임 수만큼의 실시간"을 보장하지 않고(오프스크린
  창 렌더링 속도가 실행마다 달라 물리 틱 수 대비 렌더 프레임 수 비율이 변하는
  것으로 추정) 실행마다 달라지는 것으로 보임. QA용 디버그 헬퍼로 "전투 진행 중인
  특정 상태"를 잡고 싶다면 `GAME_QA_FRAME` 값만 믿지 말고 실제 스크린샷을 열어
  원하는 상태인지 확인할 것. 게임 로직 자체는 정지 감지로 실제 정지 후 진행하므로
  문제 없음 — 아래 항목과 같은 계열의 "타이밍은 매번 다르지만 로직은 안전함" 현상.
- 물리 시뮬레이션 특성상 다이스가 멈추는 시점이 매번 달라짐 → `GAME_QA_FRAME`을
  고정값으로 쓰면 다이스가 아직 구르는 중일 수 있음 (QA 스크린샷 자체는 여전히 고정
  프레임에 찍으므로, 어느 시점을 찍든 "게임 진행 중인 어느 한 순간"을 보게 될 뿐 —
  게임 로직 자체는 정지 감지로 실제 정지 후 진행하므로 문제 없음).
- ~~다이스가 14개 이상으로 한 주머니에 몰리면 정지 감지가 사실상 항상 4초 안전장치로
  빠짐~~ → **해결됨** (이번 이터레이션, 위 큐 2 참고). "전체 공용 카운터" 방식을
  "다이스별 독립 카운터" 방식으로 바꿔, 서로 맞닿은 다이스 중 하나가 접촉 잔진동으로
  순간 튀어도 이미 조용해진 다른 다이스들의 카운터까지 함께 리셋되지 않도록 함.
  14개(교환당 15개)로 재검증한 결과 2.7~3.4초 안에 자연 정지 감지됨(이전에는 항상
  4초). 남은 것은 다이스가 많을 때 서로 겹쳐 구르는 "과정"의 시각적 체감(사람 판단
  필요) 뿐.
- **물리 다이스의 착지 면과 실제 판정값이 무관함.** `combat_test.gd`는 화면에 굴러가는
  `DieD4`를 순수 연출로만 쓰고, 실제 공격/방어 합계는 `DiceBag.roll()`의 RNG로 별도
  계산한다 (물리 다이스가 어느 면으로 착지했는지 읽는 기능이 없음 — D4 정사면체는
  "위를 향한 면"이 모호해서 판독 자체가 까다로움). 플레이어가 보기에 "다이스 눈이랑
  로그의 숫자가 안 맞는다"고 느낄 수 있음. 사람이 실제로 플레이해보고 위화감이 있는지
  INBOX.md에 남겨줘야 판단 가능한 영역.
- ~~`res://scenes/dungeon.tscn`은 2D+3D 파이프라인 검증용 스파이크 씬으로 남아있고,
  `res://scenes/combat_test.tscn`이 새로 전투 전용 씬으로 분리됨. 던전 방 이동 등
  본게임 구조가 생기면 두 씬의 이름/역할을 정리해야 함.~~ → **해결됨** (이번
  이터레이션). 본게임 구조(캐릭터 선택 → 던전 맵 → 전투/상점/이벤트)가 이미 다 갖춰진
  뒤라 `code/scenes/dungeon.tscn`(2D+3D 파이프라인 검증이 끝난 뒤로는 아무 코드/씬도
  참조하지 않던 스파이크 씬)을 `git rm`으로 제거함. 아래 "완료 기록" 참고.
- ~~D4 충돌 사운드는 배선만 되어 있고 실제 재생되는 소리가 없음~~ → **부분
  해결됨** (이번 이터레이션, 위 큐 4 참고). `plastic.tres`의 `impact_sound`는
  여전히 비어 있지만, `code/dice/die_d4.gd`가 이제 실제 에셋이 없으면
  `code/dice/procedural_sound.gd`로 합성한 임시 타격음을 자동으로 대신 재생한다.
  남은 것: 이 합성음이 실제로 들어봤을 때 괜찮게 들리는지, 실제 녹음/제작 에셋으로
  교체할지는 여전히 사람이 실제로 들어봐야 판단 가능한 영역 (INBOX.md 피드백 필요).
  교체하려면 `res://resources/materials/plastic.tres`의 `impact_sound`만 채우면
  되고(그쪽이 항상 우선함) 코드 수정은 불필요.
- ~~전투가 끝나면(승/패) 화면이 그 상태로 멈춘 채 끝남~~ → 해결됨. 승/패 후
  `NextButton`으로 `dungeon_map.tscn`으로 돌아갈 수 있음 (위 완료 기록 참고).
- ~~`scripts/qa_shot.sh`는 `--headless`를 쓰지 않고 실제 창을 띄운다. `GAME_QA_FRAME`을
  크게 잡아(예: 1500) 창이 화면에 오래(수십 초) 떠 있으면, 그동안 마우스가 실제로
  클릭 가능한 버튼 위에 있을 경우 우발적으로 클릭되어 씬이 전환될 수 있음 (당시
  실제로 겪음 — 다만 결과적으로 버튼 동작 검증에는 도움이 됐음). QA 목적이라면
  필요한 최소 프레임만 쓰는 것이 안전.~~ → **사실상 해결된 것으로 보임** (2026-09-07
  (21), git 히스토리 교차 확인 — 재현 테스트로 직접 확인한 것은 아님). 이 항목은
  2026-09-01(`9321bfd`)에 기록됐는데, 바로 다음날 2026-09-02(`e1a7d35` "QA 캡처 시
  창을 화면 밖으로 이동해 사람 작업 방해 방지")에 `code/qa/visual_qa.gd`의
  `_ready()`가 QA 모드 진입 시 `get_window().position = Vector2i(-4000, -4000)`로
  창을 화면 밖 좌표로 옮기는 코드를 다른 목적(QA 창이 포그라운드로 튀어나와 사람의
  작업을 방해하지 않게)으로 추가했고, 그 뒤로 이 known issue와 다시 연결해 확인한
  적이 없어 계속 "미해결"로 남아있었음. 화면 밖 좌표로 이동한 창은 극단적인 다중
  모니터 배치가 아닌 한 실제 마우스가 물리적으로 그 위치에 있을 수 없으므로, 부수
  효과로 이미 해결됐을 가능성이 높음 — 다만 실제로 우발적 클릭을 재현해서 확인한
  것은 아니므로, 혹시 다시 이 문제를 겪으면 이 판단이 틀렸다는 뜻이니 이 취소선을
  되돌리고 다시 조사할 것.
- ~~던전 맵에는 아직 "전투 방" 하나만 있고 반복 재입장만 가능함~~ → 부분 해결됨.
  이제 "전투 방" / "상점" 2개 선택지가 있음. 비전투 선택지 이벤트, 3번째 이상의 방
  종류는 여전히 미구현 (다음 할 일 큐 1, 11번 참고).

---
*이 파일 갱신을 건너뛰면 다음 세션은 아무 기억 없이 처음부터 다시 파악해야 한다.
반드시 "무엇을 했고, 무엇이 남았고, 어디서 막혔는지"를 남길 것.*
