# 게임 디자인 문서 (DESIGN.md)

> 이 문서는 "무엇을 만드는가"를 정의한다. 모든 이터레이션은 이 문서를 먼저 읽고 시작한다.
> 최초 작성: 2026-08-31 (사용자 인터뷰 기반)
> 갱신: 2026-08-31 (전투 시스템 문답 반영)

## 장르

로그라이크 던전 크롤러 + **다이스 빌딩**

## 참고작 / 톤

슬레이 더 스파이어(Slay the Spire), 백팩 히어로(Backpack Hero)와 유사한 톤.
카드 대신 **주사위**로 빌드를 짠다는 점이 핵심 차별점.

## 핵심 루프 (던전 진행)

```
방 선택 → 이벤트 발생 (전투 / 상점 / 선택지 이벤트 등) → 보상 획득 → 다음 방 이동
```

## 코어플레이 재미 축

- **다이스 주머니 빌드업**: 전투 전후 얻는 아이템으로 주사위를 개조한다.
  (예: 모든 면을 6으로 만들기, 6면체를 8면체로 교체하기)
- **손맛/피드백**: 여러 개의 다이스가 굴러가며 나는 소리와 움직임 자체가 재미 요소.
  구르는 다이스의 개수·비주얼·사운드는 QA 스크린샷/플레이 검증에서 중요하게 다룰 것.

## 전투 시스템

### 다이스 주머니 (덱 역할)

- **공격 주머니**와 **방어 주머니**, 두 개의 독립된 다이스 풀을 가진다.
  (덱빌딩 게임의 "덱 두 개"라고 생각하면 됨 — 서로 다른 아이템/개조로 따로 빌드된다)
- 각 주머니는 매 턴 **전체를 굴리고 소모한 뒤 다시 채운다** (재보충형, 슬더스 스타일 턴제).

### 값 반영 방식

- **족보(포커식 조합)·슬롯 배치는 사용하지 않음.**
- **다이스 눈의 합계를 그대로 사용**한다 (높은 값이 곧 좋은 값).
- 공격/방어는 양쪽(플레이어·몬스터) 모두 각자의 공격 주머니 / 방어 주머니를 가지는
  **대칭 구조**다:
  - 공격 측: 공격 주머니 굴림 → 공격 합계
  - 방어 측: 방어 주머니 굴림 → 방어 합계
  - **최종 데미지 = max(0, 공격 합계 − 방어 합계)**
  - (몬스터도 방어 주머니를 가지므로, 플레이어 공격도 몬스터의 방어 합계만큼 감소한다)

### 턴 순서

```
1. 내 공격턴  → 내 공격 주머니 굴림 vs 몬스터 방어 주머니 굴림
              → 데미지 = max(0, 내 공격 합계 - 몬스터 방어 합계) → 몬스터 HP 감소
2. 몬스터 공격턴 (= 내 방어턴) → 몬스터 공격 주머니 굴림 vs 내 방어 주머니 굴림
              → 데미지 = max(0, 몬스터 공격 합계 - 내 방어 합계) → 내 HP 감소
3. 1~2 반복 (적 또는 플레이어 HP가 0이 될 때까지)
```

- 몬스터도 플레이어와 동일하게 공격/방어 주머니를 굴려서 값을 낸다 (완전 무작위 고정
  데미지가 아니라, 다이스를 굴려 매번 값이 달라짐). 다만 몬스터의 주머니 구성 자체는
  고정되어 있고 개조되지 않는다는 점이 플레이어와 다르다 (아래 "확정된 세부 사항" 참고).

### 빌드업 (다이스 개조)

- 전투 전후로 얻는 아이템을 통해 주사위를 개조한다.
- 개조 예시:
  - 특정 면의 값을 올리기 (예: 1을 6으로)
  - 주사위 자체를 교체 (6면체 → 8면체 등, 면 개수 자체를 늘려 기댓값 상승)
  - 공격 주머니 / 방어 주머니에 새 주사위 추가
- 개조는 공격 주머니와 방어 주머니에 각각 독립적으로 적용 가능해야 한다
  (플레이어가 "공격에 몰빵" 또는 "방어에 몰빵" 같은 빌드 방향을 선택할 수 있게).

## 확정된 세부 사항 (첫 프로토타입 기준)

### 초기 다이스 구성 (플레이어)

- **기본값(견습 모험가, 기믹 없음)**: 공격 주머니 4면체(D4) 3개 / 방어 주머니 4면체(D4)
  3개.
- **2026-09-15부터 캐릭터별로 시작 다이스 개수 배분이 다름**(INBOX.md 2026-09-14
  "캐릭터별 현재 개성 및 스타일에 맞추어 시작 주사위를 다르게 한다" 반영, 면 개수는
  5종 전부 D4로 동일하고 공격/방어 다이스 "개수" 배분만 다름) — 광전사(공격4/방어2,
  극단형 공격 몰빵) / 수호자(공격2/방어4, 안정형 방어 몰빵) / 폭발병(공격4/방어3,
  공격 다이스 최댓값 스택을 더 자주 쌓게) / 주술사(id: shieldbearer, 공격3/방어4,
  방어 다이스 최댓값 스택을 더 자주 쌓게, 2026-09-24 리스킨 — 구명 "방패병"→"침묵의
  무녀"→"주술사", id/기믹/수치는 그대로). 정의는 `code/systems/character_profiles.gd`
  (`CharacterProfiles.PROFILES`의 `attack_count`/`defense_count`)에 있고,
  `RunState.reset_run()`이 이 값으로 주머니를 생성한다. 배분 수치는 감으로 잡은
  잠정값 — 실제 밸런스는 사람 플레이 피드백 필요.

### 시작 몬스터 (첫 적, 테스트용)

- **공격 주머니**: 4면체(D4) 2개 (2D4)
- **방어 주머니**: 4면체(D4) 1개 (1D4)
- (몬스터도 플레이어와 동일한 공격/방어 주머니 구조를 따른다. 다만 구성이 고정되어 있고
  개조/성장이 없다는 점이 플레이어와의 차이. 몬스터별로 이 구성이 달라질 수 있음 — 지금은
  테스트용 몬스터 하나만 확정)

### 체력(HP) / 승패 조건

- 승리/패배 조건: **HP가 0이 되면 패배/처치**
- 플레이어 초기 HP: **20**
- 테스트용 몬스터 HP: **10**

### 2D/3D 혼합 구조

- **다이스만 3D**로 물리 시뮬레이션 (구르고, 부딪히고, 재질별 소리가 남).
- **캐릭터, 배경, UI 등 나머지는 2D**로 진행.
- 구현 확정: 2D 씬 안에 3D 다이스용 SubViewportContainer + SubViewport
  (transparent_bg=true)를 두어 3D 물리 다이스를 2D 배경/UI 위에 합성한다
  (`combat_test.tscn` 등에서 실제로 검증 완료, 다른 방식은 검토 불필요).

### 다이스 연출: 물리 시뮬레이션 + 재질별 사운드

- 다이스는 **물리 시뮬레이션**으로 굴러간다 (연출용 애니메이션이 아니라 실제 물리 바디로
  떨어지고, 구르고, 서로 부딪힘).
- 재질(Material) 개념이 있고, 재질에 따라 충돌/구르는 소리와 **시각 색**이 달라진다.
  - 기본(시작) 재질: **플라스틱**
  - 유리/나무/철제 재질을 추가함(물리 bounce/friction 값, 합성 충돌음, 시각 색까지
    재질별로 구현됨 — 플라스틱=연회백/광택, 나무=갈색/무광, 유리=옅은 청록 반투명,
    철제=은회색/금속. 시각 색은 2026-09-03에 추가 — 그 전에는 재질이 물리/소리만
    다르고 넷 다 같은 무채색 회색이라 다면체를 얻어도 눈으로 재질 차이를 못 느끼는
    문제가 있었음). 다만 플레이어가 재질을 직접 고르거나 획득하는 시스템은 아직
    없어서, 지금은 다이스 면 개수(sides)에 따라 코드가 잠정 배정한다(D4/D6=플라스틱,
    D8=나무, D10=유리, D12/D20=철제 — "개조로 다이스가 커질수록 고급 재질처럼
    보인다"는 감으로 잡은 잠정 매핑). 실제로 그럴듯한지, 재질을 플레이어가 직접
    고르는 것으로 바꿀지는 사람 피드백 필요.
- 구현 시 유의점 (다음 이터레이션 참고):
  - 바닥 충돌, 다이스-다이스 충돌, 구르는 동안의 마찰 소리를 재질별로 트리거해야 함
  - Godot에서는 RigidBody3D(또는 2D 프로젝트면 RigidBody2D + 의사-3D 연출) +
    PhysicsMaterial + 충돌 시 AudioStreamPlayer로 구현하는 방향을 우선 검토
  - 시각 QA(`GAME_QA_FRAME`)는 다이스가 굴러서 완전히 멈춘 시점 이후로 프레임을 잡아야
    스크린샷이 의미 있음 — 물리 시뮬레이션 특성상 멈추는 시점이 매번 조금씩 다를 수 있음.
    **결정: "정지 감지 후 캡처" 방식으로 확정, 구현 완료** (2026-09-03). 씬이
    `_qa_is_settled() -> bool`을 구현하면(예: `combat_test.gd`), QA 실행 시
    `GAME_QA_SETTLE=1`을 켰을 때 `GAME_QA_FRAME`에 도달해도 그 값이 true가 될 때까지
    캡처를 미룬다(`code/qa/visual_qa.gd`). 이 훅이 없는 씬은 기존과 동일하게
    `GAME_QA_FRAME`에서 바로 캡처(하위 호환). `scripts/qa_shot.sh <scene> <frame>
    [out] [qa_call] 1`의 5번째 인자로 켤 수 있음.

## 현재 구현 콘텐츠 (기획자 참고용)

> 이 섹션은 코드를 열지 않고도 "지금 게임에 실제로 뭐가 들어있는지" 확인하기 위한
> 것이다 (INBOX.md 2026-09-14 요청 반영: "현재 게임에 적용된 캐릭터/특수 이벤트를
> 확인해볼 수 있도록 디자인md에 기술한다"). 아래 수치·문구는 전부 코드에서 그대로
> 옮겨 적은 것이고, 코드가 바뀌면 이 섹션도 같이 갱신해야 한다(자동 동기화 아님).

### 플레이어블 캐릭터 (7종)

정의 위치: `code/systems/character_profiles.gd` (`CharacterProfiles.PROFILES`).
2026-09-24 [대형 기획 4]로 매혹사/곡예사 2종이 신규 추가되고 기존 "방패병"이
"주술사"로 리스킨되어 5종 → 7종이 됐다(id/기믹/수치는 리스킨 전후 불변).

| id | 이름 | 컨셉 | 시작 다이스 (공격/방어, 전부 D4) | 시작 기믹 |
|---|---|---|---|---|
| novice | 견습 모험가 | 기본형, 능력 차이 없음 | D4x3 / D4x3 | 없음 |
| berserker | 광전사 | 극단형, 공격 몰빵 하이리스크 | D4x4 / D4x2 | 공격+방어 다이스 전부 최소/최댓값만 나옴(중간값 없음) |
| guardian | 수호자 | 안정형, 방어 몰빵 | D4x2 / D4x4 | 방어 다이스 1개가 항상 고정값(면 평균 반올림)만 나옴, 안 굴림 |
| explosive | 폭발병 | 폭발형, 공격 스택형 | D4x4 / D4x3 | 공격 다이스가 최댓값을 보일 때마다 스택 적립, 3스택에서 다음 공격 1턴만 1D20 |
| shieldbearer | 주술사 (2026-09-24 리스킨, 구명 "방패병"→"침묵의 무녀"→"주술사" — id/기믹/수치 불변, "방어형" 표현은 저주·술식으로 재서술) | 음침하고 말이 없는 주술사, 저주 술식형 | D4x3 / D4x4 | 방어 다이스가 최댓값을 보일 때마다 저주 술식 적립, 3술식에서 다음 방어 1턴만 1D20 |
| enchantress | 매혹사 (2026-09-24 신규) | 섹시하고 도발적인 매혹사, 균형형 | D4x3 / D4x3 | 매 턴(공격/방어 모두) 그 턴에 굴린 다이스 중 가장 낮은 값 1개를 그 다이스의 최댓값으로 전환, 스택/임계치 없음 |
| juggler | 곡예사 (2026-09-24 신규) | 알록달록한 서커스 곡예사, 가벼운 느낌 | D4x3 / D4x2 | 런 시작 1회만, 공격 다이스 무작위 1개와 방어 다이스 무작위 1개를 통째로 맞바꿈(`DiceBag.swap_random_dice()`) |

- **캐릭터 스킬 부여 이벤트 (INBOX.md [미니 기획 C], 2026-09-16 전체 완료 + 고유
  스킬 5/5종, 2026-09-17 완료)**. 기존 "특수 이벤트" 방(방 종류 추가 없이)이 30%
  확률로 아이템 대신 "스킬 이벤트"가 되어, 스킬 후보 2개 중 1개를 골라 익힐 수
  있다(`code/systems/skill_pool.gd`의 `SkillPool.SKILLS`=공용 2종 "심호흡"/"여분",
  `SkillPool.UNIQUE_SKILLS`=캐릭터 전용 고유 스킬, 지금은 광전사 "광기 심화"/수호자
  "수호 심화"/폭발병 "연쇄 폭발"/방패병 "연쇄 방어"/견습 모험가 "임기응변" 5종 —
  이제 5개 캐릭터 전원의 고유 스킬이 갖춰짐). 고른 스킬 id는
  `RunState.skill_flags`(문자열 배열, `reset_run()`마다 초기화)에
  쌓이고, 이미 보유한 스킬은 다음 번 후보에서 자동 제외된다. **`combat_test.gd`가
  `RunState.skill_flags`를 읽어 실제 전투 보너스를 적용한다**(`_do_exchange()`에서
  배선):
  - **심호흡**(`deep_breath`): 이번 전투의 첫 방어턴 한 번만, 방어 다이스 결과값 전부에
    +1(다이스별 면 개수가 상한 — `DiceBag.apply_flat_bonus()`).
  - **여분**(`spare_die`): 폭발 보너스 턴이 아닌 평소 공격턴마다 매번, 여분 다이스
    (공격 주머니 0번째 다이스와 같은 면 개수) 1개를 추가로 굴려 이번 공격에서 가장
    낮았던 다이스 값보다 높으면 그 자리를 대체한다(`combat_test.gd`의
    `_apply_spare_die()`) — "이번 런 내내 유지"이므로 스킬을 보유한 한 매 공격턴 계속
    적용됨.
  - **광기 심화**(`frenzy_deepen`, 광전사 전용 고유 스킬): 광전사 본인 기믹은
    `min_max_only`라 원래 `explosive_stack` 계열 스택 메커니즘이 없지만, 이 스킬을
    획득하면 폭발병과 같은 스택 파이프라인이 열린다(공격 다이스가 최댓값을 보일 때마다
    스택 적립, 3스택에서 보너스 공격턴). min_max_only라 최댓값이 뜰 확률이 표준
    다이스보다 높아 스택이 잘 쌓이는 시너지가 있고, 보너스 턴 자체도 폭발병의 "1D20
    한 번"보다 강화되어 **1D20을 두 번 굴려 더 높은 값을 채택**한다
    (`combat_test.gd`의 `player_frenzy_active` 분기).
  - **수호 심화**(`guard_deepen`, 수호자 전용 고유 스킬, 2026-09-16 신규): 광기
    심화와 완전히 대칭 구조(공격 대신 방어). 수호자 본인 기믹은 `fixed_defense_die`
    (방어 다이스 하나가 항상 고정값)라 원래 `guard_stack` 계열 스택 메커니즘이
    없지만, 이 스킬을 획득하면 방패병과 같은 스택 파이프라인이 열린다(방어 다이스가
    최댓값을 보일 때마다 스택 적립, 3스택에서 보너스 방어턴). 광기 심화와 달리
    수호자 자신의 min/max 확률 시너지는 없지만(수호자는 min_max_only가 아님),
    "방어 몰빵" 컨셉대로 방어 다이스 개수 자체가 많아(D4x4) 최댓값을 볼 기회가
    늘어난다. 보너스 턴은 방패병의 "1D20 한 번"보다 강화되어 **1D20을 두 번 굴려
    더 높은 값을 채택**한다(`combat_test.gd`의 `player_guard_deepen_active` 분기).
  UI는 [미니 기획 C]-5 지시대로 새 위젯 없이 `ItemCardStyle`를 그대로 재사용한다.
  - **연쇄 폭발**(`chain_explosion`, 폭발병 전용 고유 스킬, 2026-09-17 신규): 폭발병은
    이미 자기 기믹(`explosive_stack`)으로 스택 파이프라인을 갖고 있어 광기/수호 심화의
    "없던 파이프라인을 열어준다" 패턴을 그대로 못 쓴다 — 대신 파이프라인 자체를
    강화하는 방향으로, 폭발 스택 임계치를 3에서 2로 낮춰 보너스 공격턴(1D20)을 더
    자주 받게 한다(`combat_test.gd`의 `player_chain_explosion_active` /
    `_player_explosive_threshold()` 분기). 보너스 턴 자체는 광기 심화처럼 "두 번
    굴려 채택"으로 강화하지 않고 기존 폭발병과 동일한 1D20 한 번 — 임계치를 낮추는
    것만으로 이미 광기/수호 심화와는 다른 축의 강화이기 때문(둘 다 넣으면 과할 수
    있어 임계치만 건드림, 실제 강도는 사람 피드백 필요).
  - **연쇄 방어**(`chain_guard`, 방패병 전용 고유 스킬, 2026-09-17 신규): 연쇄
    폭발과 완전히 대칭 구조(공격 대신 방어). 방패병은 이미 자기 기믹
    (`guard_stack`)으로 스택 파이프라인을 갖고 있어 광기/수호 심화의 "없던
    파이프라인을 열어준다" 패턴을 못 쓴다 — 연쇄 폭발과 같은 방향으로, 수호
    스택 임계치를 3에서 2로 낮춰 보너스 방어턴(1D20)을 더 자주 받게 한다
    (`combat_test.gd`의 `player_chain_guard_active` / `_player_guard_threshold()`
    분기). 연쇄 폭발과 마찬가지로 보너스 턴 자체는 강화하지 않고(1D20 한 번)
    임계치만 낮춤 — 실제 강도는 사람 피드백 필요.
  - **임기응변**(`versatile_surge`, 견습 모험가 전용 고유 스킬, 2026-09-17 신규,
    INBOX.md 2026-09-17 기획자 결정): 견습 모험가는 기믹이 아예 없는 "만능형"
    캐릭터라, frenzy_deepen/guard_deepen의 "없던 파이프라인을 열어준다" 패턴을
    공격+방어 양쪽에 동시에 적용한다(`combat_test.gd`의 `player_versatile_active`
    분기) — explosive_stack과 guard_stack 파이프라인이 함께 열려 공격 다이스가
    최댓값을 보일 때마다 폭발 스택이, 방어 다이스가 최댓값을 보일 때마다 수호
    스택이 각각 쌓인다. 두 파이프라인 모두 기존 기본 임계치(3)와 기본 보너스(1D20
    한 번 굴림)를 그대로 쓴다 — 다른 4종처럼 임계치를 낮추거나(연쇄 폭발/연쇄
    방어) 보너스 턴을 강화(광기/수호 심화)하지 않는다. 한 캐릭터가 공격+방어 두
    축을 동시에 얻는 것 자체가 이미 다른 4종 대비 강력하므로, 축마다의 강화까지
    겹치면 과할 수 있다는 판단(기획자 결정 원문) — "넓지만 얕게"가 견습 모험가의
    정체성. 이로써 5개 캐릭터 전원의 고유 스킬이 완성됨.
- **스킬 강화 이벤트([미니 기획 D], 2026-09-17 전체 완료 — 1~5번)**:
  기존 7종 스킬(공용 2종+고유 5종) 각각의 "+" 강화판이 `SkillPool.UPGRADE_SKILLS`
  (7종, 이름은 전부 "<기존 이름>+")로 정의돼 있고, 특수 이벤트 방이 20%(`event.gd`의
  `SKILL_UPGRADE_EVENT_CHANCE`, 잠정값) 확률로 "이미 익힌 스킬을 강화" 이벤트가 되어
  최대 2개 후보 중 1개를 골라 익힐 수 있다(`event.gd`의 `_setup_skill_upgrade_event()`,
  기존 스킬 획득 카드 UI를 그대로 재사용). 강화 후보가 하나도 없으면(스킬을 아직
  하나도 안 얻었거나 이미 다 강화한 경우) 이 확률과 무관하게 항상 기존 스킬/아이템
  이벤트로 대체된다(`SkillPool.available_upgrade_choices()`가 빈 배열을 돌려줄 때).
  **전투 배선(-4번)은 7종 전부(공용 2종 + 고유 5종) 완료됨** — `combat_test.gd`의
  `_do_exchange()`가 "`+` > base > 없음" 우선순위로 분기한다:
  - **심호흡+**(`deep_breath_plus`): base(첫 방어턴 1회)와 달리 **매 방어턴마다**
    방어 다이스 결과값 +1(상한은 base와 동일하게 다이스별 면 개수).
  - **여분+**(`spare_die_plus`): base(여분 다이스 1개)와 달리 여분 다이스를
    **2개 굴려 더 높은 값**으로 가장 낮은 공격 다이스를 대체
    (`combat_test._apply_spare_die()`에 `count` 매개변수를 추가해 base/+ 공유).
  - **광기 심화+**(`frenzy_deepen_plus`, 광전사 전용): base(보너스 공격턴 1D20
    두 번 굴려 최댓값 채택)와 달리 **세 번 굴려 최댓값** 채택(임계치·스택 조건은
    그대로, 보너스 턴 굴림 품질만 강화).
  - **수호 심화+**(`guard_deepen_plus`, 수호자 전용): 광기 심화+와 완전히 대칭
    (공격 대신 방어) — 보너스 방어턴 1D20을 두 번이 아니라 **세 번 굴려 최댓값**
    채택.
  - **연쇄 폭발+**(`chain_explosion_plus`, 폭발병 전용): base(chain_explosion)는
    폭발 스택 임계치만 3→2로 낮췄을 뿐 보너스 공격턴 굴림 자체는 그대로 1D20
    한 번이었는데, "+"는 임계치는 2로 유지한 채 보너스 공격턴을 광기 심화와
    같은 방식(1D20 한 번 더 굴려 최댓값 채택, 총 2번)으로 강화한다.
  - **연쇄 방어+**(`chain_guard_plus`, 방패병 전용): 연쇄 폭발+와 완전히 대칭
    (공격 대신 방어) — 보너스 방어턴을 1D20 두 번 굴려 최댓값 채택으로 강화.
  - **임기응변+**(`versatile_surge_plus`, 견습 모험가 전용, 2026-09-17 신규 배선):
    다른 4쌍은 전부 "보너스 턴 굴림 품질"을 올리는데, base(임기응변)가 이미
    "넓지만 얕게"(임계치·보너스 강화 없음)였던 만큼 "+"는 base와 같은 축인
    "발동 빈도"를 마저 강화한다 — chain_explosion/chain_guard와 같은 방식으로
    공격/방어 두 파이프라인의 임계치를 동시에 3에서 2로 낮춘다
    (`combat_test._player_explosive_threshold()`/`_player_guard_threshold()`가
    `player_versatile_plus_active`도 함께 확인).
  네 쌍(광기/수호 심화+, 연쇄 폭발/방어+) 모두 `combat_test._apply_bonus_
  reroll(bag, values, extra_rolls)` 공용 헬퍼로 구현 — `_apply_spare_die()`와
  같은 이유(물리 시뮬레이션 없이 순수 함수로 단위 테스트 가능)로 분리했다.
  **UI 강조(-5번)도 완료** — `ItemCardStyle.build_card()`에 `highlight: bool =
  false` 매개변수를 추가해 true면 카드 테두리를 등급색 대신 두껍고(4px) 노란색
  (`HIGHLIGHT_BORDER`)으로 그린다. `event.gd`의 `_setup_skill_upgrade_event()`가
  자기 자신을 부를 때 `_is_skill_upgrade_event = true`로 세팅하고(QA 훅처럼
  `_ready()`를 거치지 않고 직접 호출해도 항상 정확하도록 자기 완결적으로 만듦),
  `_show_skill_offer()`가 이 플래그를 `highlight` 인자로 그대로 넘긴다 — 새 스킬
  획득 카드(`_setup_skill_event()`)는 이 플래그가 false라 기존 등급색 테두리
  그대로 나온다. 선택 항목(-6번, DeckPanel 보유 스킬 목록)은 급하지 않아 미착수
  — [미니 기획 D] 완료 기준(1~5번)은 이걸로 전부 충족됨.
- **시작 스킬 선택([미니 기획 E], 2026-09-17 전체 완료 — 1~4번, 5번은 선택이라
  미착수)**: 위 "캐릭터 스킬 부여 이벤트"([미니 기획 C], 런 중 무작위 획득)와는
  별개의 레이어 — 런 "시작 시점"에 캐릭터 선택 화면에서 확정으로 고르는 로드아웃.
  `SkillPool.STARTING_SKILLS`(6종, 각각 `character_ids`로 공유 캐릭터 표시)가
  원형이고, 캐릭터당 정확히 2개(슬롯 0=항상 해금, 슬롯 1=해당 캐릭터
  `AchievementManager.is_unlocked("clear_" + character_id)` 해금 시에만 선택
  가능)를 고를 수 있다 — 견습 모험가=[확장, 정예], 광전사=[맹공, 수집가],
  수호자=[철벽, 강철 방비], 폭발병=[맹공, 확장], 방패병=[철벽, 정예]. 선택은
  `RunState.chosen_starting_skill_id`에 저장되고 `reset_run()`이 `SkillPool.
  grant()`로 런 시작과 동시에 `skill_flags`에 넣는다. `combat_test.gd`의
  `_do_exchange()`가 6종 전부에 실제 전투 보너스를 건다(전부 `DiceBag.
  apply_flat_bonus()` 재사용, 새 다이스 연산 없음):
  - **맹공**(`start_aggro`): 공격 다이스 개수 > 방어 다이스 개수면 공격 다이스
    결과값 전체 +1.
  - **철벽**(`start_wall`): 방어 다이스 개수 > 공격 다이스 개수면 방어 다이스
    결과값 전체 +1.
  - **확장**(`start_expand`): 공격+방어 다이스 합계 8개 이상이면 공격/방어
    다이스 결과값 전체 +1(공격턴/방어턴 양쪽에 대칭 적용).
  - **정예**(`start_lean`): 같은 합계가 6개 이하로 유지되면 공격/방어 다이스
    결과값 전체 +1(확장과 반대 방향 조건).
  - **수집가**(`start_hoard`): `RunState.pip_inventory.size() >= 5`이면 공격
    다이스 결과값 전체 +1.
  - **강철 방비**(`start_ironclad`): 공격/방어 두 주머니 중 어디든 철제 재질
    (D12/D20, `combat_test._material_for_sides()` 판정)인 다이스를 1개 이상
    보유하면 방어 다이스 결과값 전체 +1.
  **알려진 이슈**: 방패병의 "정예" 슬롯은 방패병 기본 다이스 합계(공격3+방어4=7개)가
  이미 조건(6개 이하)을 넘어서 있고, 게임 안에 다이스 "개수"를 줄이는 수단이 전혀
  없어 이론상 절대 발동할 수 없는 죽은 선택지다 — docs/STATUS.md "알려진 이슈"에
  기록, 임계값/슬롯 배정을 바꿀지는 사람 결정 필요.
- **이벤트 주사위(`event_die_sides`, 2026-09-15부터)**: 공격/방어 주머니와 별개로,
  특수 이벤트 "위험을 감수하기" 판정에 쓸 전용 다이스 면 개수가 캐릭터마다 있음
  (`CharacterProfiles.PROFILES`, `RunState.event_die_sides`로 복사됨). 지금은 5종
  전부 D6로 동일(INBOX.md [미니 기획 B] "직업별로 다른 이벤트 주사위" 요청의 구조만
  먼저 열어둔 상태, 실제 차등은 이벤트 특화 캐릭터가 생기면 값만 바꾸면 됨). 화면에는
  로마 숫자(I~VI) 육각 칩(`code/scenes/event_die_visual.gd`의 `EventDieVisual`)으로
  표시해 일반 다이스와 구분하고 커스터마이징 대상이 아님을 암시 — 캐릭터 선택 상세
  패널에 샘플(I)이 상시 표시됨. **이 다이스를 실제로 굴려서 판정하는 로직(난이도 체크,
  안전/위험 선택지)은 아직 없음** — "다음 할 일 큐"의 "특수 이벤트 개편" 참고.
- 이 "기믹"(사실상 유일한 캐릭터별 스킬)에는 2026-09-15부터 유형별 아이콘이 붙는다
  (`code/scenes/skill_icon.gd`의 `SkillIcon`, INBOX.md 2026-09-14 "캐릭터 스킬에
  아이콘을 추가한다" 반영). 없음=빈 원, 극단(min_max_only)=빨간 다이아몬드,
  고정 방어(fixed_defense_die)=파란 방패, 폭발 스택(explosive_stack)=주황 별,
  수호 스택(guard_stack)=회색 겹방패. 캐릭터 선택 화면의 상세 패널과 `DeckPanel`
  캐릭터 정보 섹션 둘 다에 표시된다.
- 시작 재질은 5종 전부 기본값(플라스틱)에서 시작(재질 획득 시스템 자체가 아직 없음).
- 캐릭터 실루엣은 도형 조립 플레이스홀더(`character_portrait_placeholder.gd`)이고,
  캐릭터별로 머리색/원피스색만 다르게 재사용 중 — 실제 일러스트 없음.

### 던전 몬스터 (5종)

정의 위치: `code/scenes/combat_test.gd` (`MONSTER_PROFILES`). 방 번호(`room_index`)에
따라 5종이 순환 배정되고(5방을 넘어가면 "강화 " 접두어가 붙으며 재사용), 방이 진행될수록
공격/방어 다이스 개수·면 개수(D4→D6→D8)·HP가 함께 커진다(공식은 `_monster_config_for_room()`,
DESIGN.md "아직 정해지지 않은 것" 참고 — 스케일링 수치 자체는 잠정값). 마지막 방의 몬스터는
"보스"로 추가 강화됨(공격+2/방어+1/HP×2). 성격(`personality` 필드, 2026-09-16 추가)과
다이스 기믹은 INBOX.md 2026-09-15 [미니 기획 A]가 확정한 배정을 그대로 따른다 — 기믹은
몬스터 다이스 "결과값"에 개입하는 방식이라 몬스터 주머니 구성 자체(개수/면 개수)는 그대로
유지된다(몬스터는 플레이어처럼 아이템으로 개조되지 않음, DESIGN.md 확정 사항).

| 이름 | 성격 | 다이스 기믹 | 기믹 효과 |
|---|---|---|---|
| 슬라임 | 무기력하고 단순함 | 없음 | 가장 약한 몬스터라 기믹 없이 그대로 둠 |
| 고블린 | 성급하고 화를 잘 냄 | `anger_stack` (분노 스택) | 공격 다이스가 최댓값을 보일 때마다 분노가 쌓이고, 임계치에서 다음 공격 1턴만 1D20 |
| 해골 전사 | 감정 없이 명령대로만 움직이는 병사, 늘 같은 힘으로 정확하게 타격 | `fixed_value` (고정값) | 공격/방어 다이스가 항상 같은 값만 나옴(면 개수 절반 근처로 반올림, 안 굴림) |
| 오크 | 힘만 믿고 저돌적으로 날뛰는 성격, 전부 아니면 전무 | `min_max_only` (극단) | 공격/방어 다이스가 최소·최댓값만 나옴(중간값 없음) |
| 다크 나이트 | 차갑고 노련하며 방어에서 흔들리지 않는 기사 | `steady_guard` (철벽 방어) | 방어 다이스 굴림 "결과"가 면 개수 절반(올림) 미만이면 그 값으로 보정(면 값 자체는 그대로, 매번 다르되 하한선만 보장) |

- 성격/기믹 문구는 전투 화면 몬스터 HP바 아래 디버그 정보 줄(`_monster_debug_info_text()`)에
  이름·다이스 구성과 함께 그대로 노출된다(개발용 QA 표시 — 2026-09-14 요청, "추후 제거되거나
  간소화될 수 있음").
- 몬스터 외형은 캐릭터와 마찬가지로 도형 조립 플레이스홀더(뿔 달린 둥근 블롭)이고, 몬스터별로
  색만 다르게 재사용 중 — 실제 일러스트/무스메 디자인은 없음.

### 던전 방 이벤트 콘텐츠

던전 방은 "전투 / 상점 / 특수 이벤트 / 스토리 이벤트" 중 일부가 방마다 노출된다(전투는
항상, 상점은 2번째·4번째 방 고정, 특수 이벤트/스토리 이벤트는 각각 50% 확률 — 둘 다
캐릭터와 무관하게 모든 플레이어에게 동일하게 노출됨).

#### 특수 이벤트 — `code/scenes/event.gd` (아이템 정의: `code/systems/event_item_pool.gd`)

- 대상: 모든 캐릭터 공통(캐릭터별 차등 없음).
- **흐름(2026-09-16부터, INBOX.md [미니 기획 B] 2~3번)**: 방마다 선택지는 항상 2개로
  고정된다 — "안전하게 넘어가기"(리스크 없음, `EventItemPool.random_safe_item()`으로
  C/B급 중 가중치 무작위 1개 확정 획득, C가 훨씬 흔하고 B는 드묾) / "위험을 감수하기"
  (캐릭터의 이벤트 주사위를 1회 굴려 `DC = min(5, 3 + room_index / 2)`와 비교 — 방
  0~1은 DC3, 2~3은 DC4, 4 이상은 DC5. 굴린 값이 DC 이상이면 성공(`random_risky_item()`
  으로 B/A/S급 중 가중치 무작위 1개 획득, 상위 등급일수록 흔함), 미만이면 실패(아이템
  없이 "아쉽게도..." 문구만 보여주고 방 소비만 인정 — 전투 밖 영구 체력 개념이 없어
  HP 페널티는 없음, `code/scenes/event.gd`). 아이템을 실제 주머니(공격/방어)에
  넣거나 인벤토리에 담는 카드 UI는 이전과 동일(add_die는 주머니 선택 버튼,
  upgrade_die/gain_pips는 즉시 획득 버튼 하나).
  **[기획자 결정 - 2026-09-16, 해결안 (b)]** 안전/위험 풀은 원래 각각 C만/A·S만이라
  B급 아이템(D8)이 어느 풀에도 안 걸리는 사각지대가 있었는데, 등급 경계를 B에서
  겹치게 넓혀(안전=C~B, 위험 성공=B~A~S) 해결했다(`EventItemPool.SAFE_GRADE_WEIGHTS`
  ={C:5, B:1}, `RISKY_GRADE_WEIGHTS`={B:1, A:2, S:3} — 가중치는 잠정값).
- **내러티브(상황 설명 문구, "flavor" 필드)는 2026-09-16부터 아이템마다 1문장씩
  있음**(`EventItemPool.ITEMS`, 아래 표의 "내러티브" 열 — 카드에서 제목 바로 아래,
  효과 설명보다 먼저 표시됨). 안전/위험 중 어느 쪽으로 얻었든 같은 문구가 그대로
  쓰인다(안전/위험 전용 문구 분리는 하지 않음).

| 이름 | 내러티브 | 효과 | 등급 |
|---|---|---|---|
| 다면체 주사위 획득 (D8) | 구석에서 낯선 팔각 주사위가 은은하게 빛난다. 조심스레 손을 뻗는다. | 선택한 주머니에 D8 다이스 1개 추가 | B |
| 다면체 주사위 획득 (D12) | 제단 위에 놓인 정교한 다면체 주사위가 눈길을 끈다. 값을 매길 수 없어 보인다. | 선택한 주머니에 D12 다이스 1개 추가 | A |
| 다이스 대승급 (→ D10) | 정체 모를 상인이 다듬어진 주사위 하나를 건넨다. "이걸로 바꿔보게." | D10 다이스 1개를 인벤토리로 획득(커스터마이징에서 나중에 직접 교체) | A |
| 다면체 주사위 획득 (D20) | 번쩍이는 결정 조각 안에 스무 개의 면이 새겨진 주사위가 갇혀 있다. | 선택한 주머니에 D20 다이스 1개 추가 | S |
| 눈금 주머니 획득 | 바닥에 흩어진 작은 눈금 조각들을 주머니 가득 쓸어 담는다. | 눈금 3~5개(각 1~6 무작위 값)를 인벤토리에 추가 | C |

#### 스토리 이벤트 — `code/scenes/story_event.gd` (시나리오 정의: `code/systems/story_event_pool.gd`)

- 대상: 모든 캐릭터 공통. 다이스는 전혀 건드리지 않고 골드만 증감하는 순수 텍스트 2택
  선택지 — 8종 시나리오 중 매번 무작위로 하나가 나온다.

| 제목 | 내러티브 | 선택지 A | 선택지 B |
|---|---|---|---|
| 낡은 보물상자 | 먼지 쌓인 상자를 발견했다. 함정일 수도 있다. | 상자를 연다 (60% 확률 +25 / 40% 확률 -10) | 그냥 지나간다 (효과 없음) |
| 떠돌이 상인 | 지친 상인이 여비를 빌려달라 부탁한다. | 도와준다 (-10 확정) | 무시한다 (효과 없음) |
| 휴식처 | 안전해 보이는 공터에서 짐을 정비했다. | 짐을 정비한다 (+5 확정) | 서둘러 지나간다 (효과 없음) |
| 수상한 도박꾼 | 골드를 걸고 내기를 하자는 낯선 이를 만났다. | 내기를 받는다 (50% 확률 +20 / 50% 확률 -10) | 거절한다 (효과 없음) |
| 무너진 제단 | 오래된 제단에 동전을 바치면 소원이 이루어진다는 전설이 있다. | 동전을 바친다 (-5 확정) | 미신을 무시한다 (효과 없음) |
| 숨겨진 샛길 | 지도에 없는 샛길을 발견했다. 지름길일 수도, 함정일 수도 있다. | 샛길로 들어간다 (50% 확률 +15 / 50% 확률 -15) | 정석대로 간다 (효과 없음) |
| 부상당한 여행자 | 길가에 쓰러진 여행자가 도움을 요청한다. | 치료해준다 (-8 확정) | 모른 척 지나간다 (효과 없음) |
| 버려진 야영지 | 누군가 급히 떠난 듯한 야영지에 짐이 흩어져 있다. | 뒤져본다 (55% 확률 +18 / 45% 확률 -8) | 손대지 않는다 (효과 없음) |

- 골드 손실은 보유량 아래(0)로 내려가지 않게 클램프됨.
- 골드 증감폭·확률·등장 시나리오 8종 모두 감으로 잡은 잠정값 — 사람 피드백 필요.

## "이 정도면 됐다" (Definition of Done) — 이터레이션별 판단 기준

시각 QA는 두 층위로 판단한다.

### 1. 자동 판단 (스크린샷 레벨, AI가 매 이터레이션 스스로 확인)

- 지정한 씬(`GAME_START`, 예: `dungeon`)이 **크래시 없이 로드**된다.
- 지정한 프레임(`GAME_QA_FRAME`)에서 **스크린샷이 정상적으로 저장**된다 (파일 존재, 0바이트 아님).
- 이 두 조건을 통과하지 못하면 그 이터레이션은 실패로 간주하고, 다음 이터레이션에서 반드시
  원인을 고쳐야 한다. (`scripts/qa_shot.sh <scene>` 로 검증)

### 2. 사람 판단 (플레이 피드백 레벨, 사용자가 직접 플레이해서 판단)

- 자동 기준을 통과했더라도, 최종 판단은 사용자가 직접 플레이해보고 느끼는 개별 피드백이다.
- UX, 밸런스, 비주얼, 재미(특히 다이스 굴러가는 손맛), 버그 등 무엇이든
  `docs/feedback/INBOX.md` 에 남긴다.
- AI는 매 이터레이션 시작 시 INBOX.md를 **가장 먼저** 읽고 **최우선으로** 반영해야 한다.

## 아직 정해지지 않은 것 (다음 이터레이션들이 채워나갈 부분)

- 상점/선택지 이벤트의 구체적 내용
- 다이스 개조 아이템의 종류와 강도 곡선
- 아트 스타일 (참고작 톤은 정해졌으나 구체적 리소스/팔레트는 미정)
- 던전 길이 (방 개수), 런 전체 구조 — 잠정으로 `RunState.TOTAL_ROOMS = 5`를 코드에
  넣어 "런에 끝이 있다"는 최소 구조만 검증함 (실제 방 개수/구조는 아직 미확정,
  상점/선택지 방이 생기면 다시 정해야 함)
- 두 번째 이후 몬스터의 공격/방어 주머니 구성 — 첫 몬스터(공격 2D4 / 방어 1D4 / HP10)만
  확정. 잠정적으로 코드(`combat_test.gd`의 `_monster_config_for_room()`)에 방 번호 기반
  스케일링 공식(공격 주사위 2방마다 +1개, 방어 주사위 3방마다 +1개, HP는 매 방 +3,
  다이스 면 개수는 1~2방 D4 -> 3~4방 D6 -> 5방부터 D8, 2026-09-03에 추가)을 넣어
  "던전이 진행될수록 몬스터가 강해진다"는 최소 구조만 검증함. 실제 곡선이 적당한지는
  사람이 플레이해보고 판단 필요.
이 항목들은 확정되는 대로 이 문서에 추가하고, `docs/STATUS.md`의 "다음 할 일 큐"에 반영한다.

---
*이 문서가 진실의 원천(source of truth)이다. 방향이 바뀌면 INBOX.md로 지시하고,
AI는 반영 후 이 문서 자체도 갱신한다.*
